# Memory: Kindle Butch Gen (Translation & E-Book Compiler)

## 1. Поточний статус проекту (Тестування завершено)
Спринт **TASK-4..7** повністю реалізовано та протестовано на мобільному телефоні. Зауваження щодо рендерингу термінала, завантаження обкладинок/картинок PDF-книг та очищення SVG просторів імен успішно вирішено в коді.

### Статус завдань:
*   **TASK-4 (Checkbox Logic)**: 🟢 **DONE** (Коміт `c87d96a`).
*   **TASK-5 (Focus Flow & Layout Refactoring)**: 🟢 **DONE / VERIFIED** (Коміт `b996fe1`). Підтверджено роботу модалок та інлайн-терміналів (миготіння при оновленні списку книг виправлено кешуванням логів).
*   **TASK-6 (Unified stages.html Viewer Pattern)**: 🟢 **DONE / VERIFIED** (Коміт `6ed7313`). Підтверджено роботу єдиного вікна перегляду та перемикання Original/Translated. Додатково налагоджено завантаження зображень та очищення SVG префіксів для PDF книг.
*   **TASK-7 (Remove Hardcoded Credentials)**: 🟡 **IN_PROGRESS - NEEDS MANUAL ACTION** (Коміт `abb870a`). Код вилучення хардкоду готовий. Фактичне застосування (видалення `web_credentials.json` або встановлення змінної оточення) заблоковано до моменту ручного підтвердження від користувача, щоб уникнути втрати доступу.
*   **TASK-8 (Адаптивна візуальна тема)**: ⚪ **PREPARED / NOT STARTED**. Джерела блокноту NotebookLM `kbg-redesign` повністю синхронізовані (додано актуальні версії `app_py.txt`, `dashboard_html.txt` та `stages_html.txt`).

---

## 2. Запуск та Автентифікація (TASK-7)
*   **Веб-сервер**: Запускається вручну через `./kbg.sh serve --port <PORT>` (або з флагом `--debug` у режимі розробки).
*   **Пріоритет перевірки пароля**:
    1. `KBG_WEB_PASSWORD` (змінна оточення) — якщо задана, **завжди** перевизначає та ігнорує будь-які збережені файли облікових даних.
    2. `web_credentials.json` — якщо змінна оточення відсутня, зчитується збережений хеш.
    3. Автогенерація випадкового токену (якщо файл відсутній) — записується у `web_credentials.json` та виводиться у консоль при старті.

---

## 3. Інструкція для відновлення та ручних дій

### Для застосування нових налаштувань безпеки (після підтвердження):
*   **Варіант А (Рекомендований): Встановити ENV змінну при запуску**
    ```bash
    KBG_WEB_PASSWORD="ваш_новий_безпечний_пароль" ./kbg.sh serve --port 5000
    ```
*   **Варіант Б: Скинути поточний файл авторизації**
    ```bash
    rm /home/vokov/projects/kindle-butch-gen/web_credentials.json
    ```

### Для переходу до TASK-8 (Візуальна тема):
1. Переконатися, що сесію тестування на телефоні завершено та отримано підтвердження працездатності TASK-5/6/7.
2. Блокнот NotebookLM `kbg-redesign` (ID: `0c18b603-73d7-44ba-bc44-34bfe8080327`) повністю готовий і містить актуальні джерела (`app_py.txt`, `dashboard_html.txt`, `stages_html.txt`). Можна переходити до Кроку 2 (Structured Delegation) для генерації UI-теми.
3. **Git Remote**: Remote origin переключено з HTTPS-токена на SSH (`git@github.com:maxfraieho/kindle-butch-gen.git`) на dev-сервері для стабільного пушу.

