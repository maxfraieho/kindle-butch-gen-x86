# Tasks Log

## [x] TASK-1: Refactor Codebase & Consolidate Utilities
* **Problem:** `get_hash`, `split_into_segments`, and `to_xml_format` were duplicated across 5 different scripts, violating DRY.
* **Solution:** Extracted them into [common/utils.py](file:///data/data/com.termux/files/home/kindle-butch-gen/common/utils.py) and updated imports in:
  * [translate_epub.py](file:///data/data/com.termux/files/home/kindle-butch-gen/translate_epub.py)
  * [translate_stage.py](file:///data/data/com.termux/files/home/kindle-butch-gen/translate_stage.py)
  * [audio_stage.py](file:///data/data/com.termux/files/home/kindle-butch-gen/audio_stage.py)
  * [clean_cache.py](file:///data/data/com.termux/files/home/kindle-butch-gen/clean_cache.py)
  * [kbg_web/app.py](file:///data/data/com.termux/files/home/kindle-butch-gen/kbg_web/app.py)
* **Verification:** Compiled all files successfully.

## [x] TASK-2: Extract Inline Templates & Apply UI/UX Polish
* **Problem:** Monolithic `kbg_web/app.py` contained inline HTML string templates, and transitions had default/linear easings with bad hover/active properties and broken modal overlay fade animations.
* **Solution:**
  * Created `kbg_web/templates/` folder.
  * Extracted dashboard HTML to [kbg_web/templates/dashboard.html](file:///data/data/com.termux/files/home/kindle-butch-gen/kbg_web/templates/dashboard.html).
  * Extracted visualizer HTML to [kbg_web/templates/stages.html](file:///data/data/com.termux/files/home/kindle-butch-gen/kbg_web/templates/stages.html).
  * Replaced transitions with hardware-accelerated, specific CSS properties using custom snappy ease curves (`cubic-bezier(0.23, 1, 0.32, 1)`).
  * Fixed the modal display:none transition bug using `pointer-events: none` and `opacity` properties.
  * Added responsive `@media (hover: hover) and (pointer: fine)` gates for touch/mobile devices.
  * Added organic staggered entry animations for book lists and visualizer cards.
  * Refactored [kbg_web/app.py](file:///data/data/com.termux/files/home/kindle-butch-gen/kbg_web/app.py) to use Flask's `render_template` instead of `render_template_string`.

## [x] TASK-3: Fix llama-server CLI Argument
* **Problem:** `start-translation-server.sh` used obsolete `--n-parallel 1` argument, causing newer versions of llama-server to crash.
* **Solution:** Replaced with `--parallel 1` in `~/start-translation-server.sh`.

## [x] TASK-4: UI/UX Labeling & Checkbox Logic
* **Problem:** Чекбокси стадій у `dashboard.html` використовують подвійне заперечення (`id="noaudio-${book.slug}"`), що плутає оператора.
* **Solution:** Перейменувати лейбли на позитивні (напр. "Generate Audio", "Build Ebook"). У JS-функції `runConversion` зчитувати їхній стан, інвертувати його (`const no_audio = !document.getElementById(...)`) і лише після цього відправляти у payload `/api/run/<slug>`.
* **Verification:** Перевірити правильність інвертування булевих значень та відправки payload, щоб не зламати контракт з `app.py` (де очікується `no_audio`).
* **Type:** `direct`

## [x] TASK-5: Focus Flow & Layout Refactoring
* **Problem:** Форма додавання книги займає перший екран на мобільному телефоні, прогрес-бари з'їдають вертикальний простір, термінал відірваний від контексту книги.
* **Solution:**
  1. Перенести `<form id="addBookForm">` у модальне вікно (перевикористати існуючий клас `.modal-overlay`).
  2. Згорнути 5 прогрес-барів у єдиний багатосегментний бар або текстовий індикатор "Поточна стадія".
  3. Перемістити `div id="terminalCard"` всередину розширеного стану картки книги, щоб лог був видимий одразу под кнопкою "Run / Stop".
* **Verification Method:** Manually verified in browser on mobile device by user. Fixed terminal log rendering to cache outputs and prevent flickering during background refreshes.
* **Type:** `delegate`

## [x] TASK-6: Unified stages.html Viewer Pattern
* **Problem:** У `stages.html` існують три різні архітектури перегляду: `manga-viewer` (3 колонки), `paragraphs-list` (список карток) та `page-split-viewer` (два iframes). На мобільному телефоні side-by-side працює погано.
* **Solution:** Створити єдиний патерн `Toggle Viewer`. На екрані є лише одне вікно перегляду контенту (зображення, iframe або текст) та перемикач внизу (Original | Processed/Translated). Для манги додати проміжний стейт "Cleaned". Навігаційні стрілки (Попередня/Наступна) уніфікувати для всіх типів контенту. Оновити JS-функції `renderManga` та `loadEpubPage`, щоб вони монтували дані у цей єдиний DOM-вузол.
* **Verification Method:** Manually verified in browser on mobile device by user. Fixed EPUB asset routing (images/CSS) and dynamic <base> tag injection (including SVG namespace cleanup) to support previewing compiled PDF-to-EPUB books.
* **Type:** `delegate`

## [x] TASK-7: [SECURITY] Remove Hardcoded Auth Credentials
* **Problem:** У `app.py` хардкодиться пароль "0523" для користувача "vokov", якщо файл `web_credentials.json` відсутній або пошкоджений.
* **Solution:** Замість фолбеку на хардкод пароль, зчитувати значення з ENV-змінної (наприклад, `KBG_WEB_PASSWORD`), або, якщо файл відсутній, генерувати випадковий пароль, виводити його в консоль при запуску Flask і записувати у файл.
* **Verification Method:** The fallback to insecure hardcoded password "0523" has been eliminated in code. However, since 'web_credentials.json' already exists on the dev server with the hash of "0523", the old password remains active. Real-world mitigation requires either setting the `KBG_WEB_PASSWORD` env variable or deleting the old json file to let it generate a secure token on start.
* **Closed 2026-07-17 (manual action completed, verified live):** user-chosen password exported as `KBG_WEB_PASSWORD` in the phone's `~/.bashrc` (before the autostart block so every Flask start inherits it), stale `web_credentials.json` (hash of "0523") deleted and confirmed not regenerated (env takes precedence per app.py:61-64), Flask restarted. Verified BOTH locally and through the public domain `kindle.exodus.pp.ua`: old password → 401, new password → 200.
* **Type:** `direct`

## [x] TASK-8: OLED Adaptive Contrast Visual Theme
* **Problem:** Поточна тема не оптимізована для OLED (багато blur, напівпрозорі картинки, низький контраст меж) та складні умови освітлення (наприклад, під сонячним світлом або вночі). Також відсутні специфікації фокус-станів для клавіатурної та тачпадної навігації, а прогрес-бари використовують забагато різних яскравих кольорів.
* **Solution:** 
  1. Впроваджено OLED-оптимізовану систему токенів: глибокий OLED-чорний фон, непрозорі поверхні карток, чіткі межі без розмиття.
  2. Підвищено контрастність шрифтів до WCAG AA+ для легшого зчитування на сонці.
  3. Уніфіковано кольори статусів (idle/active/done/error), прив'язавши їх до головного фіолетового брендового кольору замість "веселки".
  4. Замінено багатокольоровий градієнт загального прогрес-бару на фіолетовий градієнт бренду.
  5. Створено чіткі фокус-стани `:focus-visible` для кнопок, чекбоксів та елементів введення.
* **Verification Method:** Manually verified by user on OnePlus 13 screen.
* **Type:** `delegate`

## [x] TASK-9: Terminal Log Cache (Flicker Fix) (DONE - RETROACTIVELY DOCUMENTED)
* **Problem:** Термінал логів оновлюється періодично, що призводить до повного очищення DOM і перерендерингу. На мобільних пристроях це викликає мерехтіння і скидає позицію прокрутки.
* **Solution:** Додати кешування логів у JavaScript (`lastLogsCache`), щоб при повторному рендерингу спочатку показувати попередньо завантажений текст, а потім оновлювати його лише при зміні контенту.
* **Verification Method:** Manually verified on OnePlus 13 in Termux. Rendered terminal logs are fully cached per-book slug and updated smoothly. (Commit `69c2b73`).
* **Type:** `direct`

## [x] TASK-10: PDF-to-EPUB Asset Serving and Namespace Cleanup (DONE - RETROACTIVELY DOCUMENTED)
* **Problem:** При перегляді сконвертованих PDF-книг (які компілюються в EPUB) у вікні stages/viewer виникає чорний екран або порожній вміст через те, що: (а) бінарні ресурси (зображення, стилі) не завантажуються з OPF-директорії всередині EPUB, (б) SVG-зображення містять префікси просторів імен (напр., `ns1:svg`), які є некоректними для стандарту HTML5 і не рендерилися браузером.
* **Solution:** 
  1. Оновити `app.py` для пошуку EPUB-файлів у теці `output` книги, якщо вони відсутні в `input`.
  2. Додати пряме сервування бінарних ресурсів (зображення, CSS) із Zip-файлу EPUB через Flask-ендпоінт.
  3. Впровадити автоматичне очищення SVG просторів імен та префіксів перед рендерингом HTML-сторінки.
  4. Додати динамічне впорскування тегу `<base>` для правильного дозволу відносних шляхів ресурсів.
* **Verification Method:** Manually verified on OnePlus 13. PDF-to-EPUB conversion previews successfully, assets are served dynamically, and namespaces are stripped cleanly without a black screen. (Commit `69c2b73`).
* **Type:** `direct`

## [x] TASK-11: No-Translate Suffix Fix (DONE - RETROACTIVELY DOCUMENTED)
* **Problem:** При запуску конвеєра з прапорцем `--no-translate` суфікс вихідних файлів розраховувався некоректно, що спричиняло збій у генерації назв результуючих файлів.
* **Solution:** Завжди додавати суфікс `_translated_<lang>`, якщо цільова мова відрізняється від початкової, незалежно від наявності прапорця `no_translate` в аргументах.
* **Verification Method:** Verified on OnePlus 13 during translation runs. (Commit `e775e46`).
* **Type:** `direct`

## [x] TASK-12: TTS Preview Stressification
* **Problem:** При прослуховуванні прев'ю тексту (TTS Preview) через дашборд слова вимовляються без наголосів, оскільки текст відправлявся в TTS-рушій напряму, минаючи стадію Stress.
* **Solution:**
  1. Додано підтримку обробки поодиноких inline-рядків (`--inline <text>`) в утиліту `bin/stressify_batch.py` без використання тимчасових JSON-файлів (запобігає конфліктам з фоновими конвертаціями).
  2. Оновлено роут `/api/tts-preview/<slug>` в `app.py`: якщо мовою є українська (`uk`), перед синтезом текст пропускається через `stressify_batch.py` всередині PRoot Ubuntu контейнера.
  3. Додано безпечний fallback (якщо контейнер або наголошувач недоступні, прев'ю генерується на вихідному тексті без помилок).
* **Verification Method:** Manually verified by user via TTS preview audio on OnePlus 13.
* **Type:** `direct`

## [ ] TASK-14: Audio Pauses, Markdown Cleaning, and Preview Pagination (IN_PROGRESS - needs manual audio verification)
* **Problem:** 
  1. Аудіокниги містять замало пауз між реченнями та заголовками, а іноді виникають кліки або зрізи складів на кінцях чанків.
  2. Розмітка (таблиці, mermaid-діаграми, коментарі HTML) попадає в аудіо-синтез та прев'ю.
  3. Перегляд списку чанків у веб-інтерфейсі обмежувався лише першими 30 чанками (TOC), і не давав доступу до решти книги, до того ж запити були повільними через O(N) операції дискової перевірки на кожен запит.
* **Solution:**
  1. Реалізовано генерацію WAV-файлів тиші (500мс для звичайних переходів, 3000мс для заголовків розділів) та накладання 15мс fade-out на кінці чанків через ffmpeg у PRoot контейнері перед склеюванням.
  2. Збільшено безпечний trailing-ліміт зрізання тиші в `tts_helper.py` для непунктуйованих чанків зі 100мс до 250мс для уникнення зрізів.
  3. Додано повне вилучення HTML-коментарів та Markdown-таблиць у `PlaceholderManager.strip_formatting`.
  4. Додано пагінацію списку чанків (по 30 на сторінку) у веб-роут `/api/preview/book/<slug>` та інтерфейс `stages.html`, оптимізовано час обробки до 10мс шляхом обмеження дискових і кеш-перевірок лише для поточної сторінки.
* **Verification Method:** In progress (synthesis running on phone). Needs manual audio listening to verify pause length, fade-out, lack of clicks, and markdown text removal.
* **Type:** `direct`



## [x] TASK-15: Deployment Script for OnePlus
* **Problem:** Non-professional users struggle to set up the environment and models on Termux manually. Large model files (like the 4.4GB GGUF translator and Supertonic 3 TTS) are prone to download interruptions on mobile/Wi-Fi networks, and missing critical packages (like `stress-uk` or `num2words`) inside PRoot Ubuntu cause the text stressifier to fail silently.
* **Solution:**
  1. Updated `deploy_oneplus13.sh` to include an interactive service autostart setup and a comprehensive model downloader.
  2. Implemented a robust `check_and_download` helper function with resume support (`curl -C -`) and strict size verification checks to prevent corrupt downloads.
  3. Added checks and automatic installation for `stress-uk` and `num2words` inside the PRoot Ubuntu setup script (Step 3).
  4. Updated `README_Termux.md` to clearly explain downloaded files, expected sizes, and system storage requirements prior to showing the one-line installer command.
* **Verification Method:** Verified size check, download, and resume functionality by simulating partial downloads and checking logs. Verified that `stress-uk` is correctly installed and imported during deployment tests.
* **Type:** `direct`

## [x] TASK-16: Manga export to Kindle-compatible format (AZW3 via Mapaki)
* **Problem:** There was no option to export translated manga in a Kindle-compatible format. Additionally, when exporting manga, the web interface displayed an irrelevant audio/TTS settings block.
* **Solution:**
  1. Integrated the native Go-based `Mapaki` tool into the manga translation pipeline inside the PRoot Ubuntu container.
  2. Implemented Pillow-based downscaling prior to packing so that any image exceeding 1920px in height is scaled down to exactly 1920px (preserving aspect ratio) to prevent Scribe/Kindle blank page render bugs.
  3. Added a new `--left-to-right` CLI option to `translate_manga.py` and passed it dynamically to Mapaki.
  4. Modified `kbg_web/templates/dashboard.html` to hide the audiobook/TTS settings section for manga books (where `book.progress.is_manga` is true).
  5. The Flask app output scanner automatically detects and exposes the resulting `.azw3` file alongside the `.cbz` archive for download.
* **Verification Method:** Verified on the OnePlus 13 phone by successfully compiling Mapaki, running the updated `translate_manga.py` on Frieren manga, confirming image downscaling from 1500x2250 to 1280x1920, and successfully producing a valid 1.4MB `test_manga_out.azw3` file. Verified that the dashboard successfully hides the settings block when loading manga.
* **Type:** `direct`

## [x] TASK-17: Onboarding sweep — delete actions, page-jump pagination, verify UI-model-management, remove Proofreading stage
* **Problem:** Following `AGENT_ONBOARDING_kindle-butch-gen.md`'s 7-item list:
  1. Books could not be removed from the dashboard once added.
  2. Output files (per-book and in the newly-added Downloads Archive) could not be deleted.
  3. The chunk/paragraph previewer only had Prev/Next (30 chunks/page), so reaching page 100+ of a long book required dozens of clicks.
  4. Repeat manga conversion with `--no-translate` was re-copying the *original* untranslated page into output, discarding prior translation work.
  5. The Proofreading stage (`edit_epub.py`, a second-pass LLM re-check of translated text run via `kbg.sh edit <slug>`) turned out not to be needed in practice, but its backend logic and UI (progress-bar step, Editor Model selector) were still present.
* **Solution:**
  1. Added `POST /api/delete/<slug>` (refuses while running) + a Delete button on each book card.
  2. Added `POST /api/delete-file/<slug>/<filename>` (reuses the existing path-traversal guard from `download_output_file`) + delete buttons in `downloads.html` and the per-book downloads list in `dashboard.html`.
  3. Added a numeric page-jump input + Go button to `stages.html`'s paragraph pagination controls, reusing the existing `fetchParagraphsPage()`/`/api/preview/book/<slug>?page=` machinery — no backend change needed.
  4. Recovered and applied a stale-but-correct fix found uncommitted on the dev server: `--no-translate` now looks up the already-translated image first, falling back to the original only if it hasn't been translated yet.
  5. Confirmed `/api/models`, `/api/models/configure`, `/api/models/start`, `/api/models/stop` (run the translation model server from the UI) were already implemented — no new code needed, just live-verified.
  6. Deleted `edit_epub.py`, removed the `kbg.sh edit` action, and stripped `edit_percent`/`edit_progress.json`/`editor_model` from `status_helper.py`, `app.py`, `monitor_book.sh`, and the dashboard's progress tracker + Settings modal.
  7. Also recovered/committed a global Downloads Archive page (`/downloads`, `kbg_web/templates/downloads.html`) and a `dashboard.html` bug fix (missing null-checks in the `formValues` restore logic) found alongside the manga fix on the dev server.
* **Verification Method:** `python3 -m py_compile` on all touched Python files; grepped the full repo to confirm no dangling `edit_epub`/`edit_percent`/`editor_model` references after removal; deployed to the OnePlus 13 via scp (git pull wasn't possible — phone has no SSH key to reach GitHub or the dev server) and restarted `kbg_web`; live-verified via `https://kindle.exodus.pp.ua`: `/api/status` no longer returns `edit_percent`, `/api/models` no longer returns `editor_model`, `/api/downloads` and `/downloads` respond correctly, `/api/delete*` correctly reject nonexistent targets, and `/api/preview/book/vibe-programming` confirmed real-world need for item 4 (169 pages of 30 chunks each). Did not destructively test delete against real book data.
* **Type:** `delegate` (dev-server WIP recovery) + `direct` (remaining items)

## [x] TASK-18: [BUG][FIXED] Race condition in /api/models/start allows duplicate llama-server processes
* **Problem:** `/api/models/start` kills any existing `llama-server` on port 8081 (`pkill -f "llama-server.*8081"`) then launches `~/start-translation-server.sh`, which *itself* does its own pkill + 2s sleep before launching. Two Start requests fired close together (e.g. an impatient double-tap on the "Start Server" button) race past both pkill checkpoints and each spawns its own server. Reproduced empirically: firing two `/api/models/start` calls 0.3s apart left **two full 4.4GB Hy-MT2-7B llama-server processes running simultaneously** (confirmed via `ps aux` on the phone), and the state did not self-correct over a 60s observation window — required a manual `kill -9` to clean up. On a memory-constrained phone this is a real resource-exhaustion risk.
* **Solution:**
  1. Replaced `pkill -f "llama-server.*8081"` pattern-matching with a PID file (`~/llama-server-8081.pid`) as the single source of truth for the tracked process — `_read_llama_pid()` / `_stop_llama_server()` in `app.py` verify liveness via `os.kill(pid, 0)` and always clear the file afterward so a stale entry never lingers.
  2. Added an `O_CREAT|O_EXCL` start lock (`~/llama-server-8081.lock`) around the stop-old+launch-new critical section in `/api/models/start`. A concurrent start request now gets an explicit `409 "A start is already in progress"` instead of silently racing through. The lock self-clears if older than 15s (far longer than the section normally takes), so a crashed request can't permanently block future starts.
  3. Removed the duplicate `pkill`+`sleep 2` from `~/start-translation-server.sh` (phone-only, not tracked in git) — stopping is now exclusively the API layer's responsibility; the script only launches and writes its own PID to the file path passed in as `$1`.
  4. `dashboard.html`'s Start Server button now stays disabled ("Starting...") for up to 90s, polling `/api/models` until it reports `running: true`, instead of re-enabling right after the fire-and-forget POST returns — closes the double-click window client-side too.
* **Verification Method:** All via `curl`+SSH against the live phone (agent-workspace/AGY3 still unreachable). (a) Fired two `/api/models/start` 0.2s apart → first got `200`, second got `409 "A start is already in progress"`; after model load, exactly 1 `llama-server` process and the PID file matched it. (b) Normal Stop→Start: confirmed PID file removed on stop (and process actually gone via `ps aux`, not just a UI illusion), then correctly re-created with the new PID on start. (c) Simulated a stale lock (manually aged to 20s) and confirmed the next start self-healed past it (`200`, not `409`) instead of being permanently blocked. Device restored to normal running state afterward. Full report: ai-memory `notes/2026-07-15-models-manager-test-report.md` and follow-up fix notes (project `kindle-butch-gen`).
* **Type:** `direct`

## [x] TASK-19: Point-edit methodology for text/audio (EDIT_METHODOLOGY_kindle-butch-gen.md, Phase 1+2+4)
* **Problem:** No way to fix a specific mistranslated paragraph or mispronounced chunk without either living with it or re-running the whole book's translation/synthesis (the exact automatic-full-re-pass approach TASK-17 already found and removed as unwanted). `EDIT_METHODOLOGY_kindle-butch-gen.md` specs a non-destructive, point-edit system across 6 phases.
* **Solution:** Investigated the actual codebase (3 parallel explore passes) before committing to scope, since the doc's own assumptions needed verification:
  - Confirmed text/audio point-editing (doc's Phase 1/2/4) is feasible and reuses existing machinery (`tts_helper.py`'s stdin-JSON single/multi-chunk contract, the same one `/api/tts-preview` already uses; `clean_cache.py`'s existing load-mutate-write-back pattern for `translate_cache.json`).
  - Found manga bubble-editing (doc's Phase 3) is higher-risk than the doc assumed: `translate_manga.py` has **no** per-page bubble geometry/OCR persistence at all — it's fully ephemeral, discarded after typesetting each page inline. A single-page regen isn't a refactor, it needs a new `<page>.bubbles.json` metadata layer first. **Deferred to a follow-up session**, scoped separately.
  - Found and fixed a correctness gap the doc missed: `run_conversion_batches.py`'s merge step unconditionally rebuilds `merged_translated_<lang>.md` from per-batch translated files whenever a book still has its source PDF, regardless of `--no-translate` — patching only the merged file would have let a later re-run silently discard an edit. Approve now patches the merged file **and** any per-batch file containing the old text.
  - Implemented: `kbg_web/edit_store.py` (per-book `edits/edits.json`, pending → regenerated → approved), `PUT /api/edit/text`, `PUT /api/edit/stress`, `POST /api/edit/regenerate-audio` (single-chunk resynthesis into the real `chunks_<voice_slug>/` dir), `GET /api/edit/queue`, `POST /api/edit/approve` (patches `translate_cache.json` best-effort by value, `merged_translated_<lang>.md`, and per-batch files; fails loudly with the edit left `pending` if the old text can't be located anywhere rather than silently reporting success). `stages.html` gained a per-paragraph edit form (translated text + stress marks), a Regenerate Audio button, and a "Pending Edits" tab with a word-level diff and Approve buttons.
  - **Deferred, not silently dropped** (flagged for future sessions): Phase 3 manga bubble-editing (needs the bubble/OCR persistence layer above), Phase 5 live-mode editing during an active run (needs `translate_stage.py`/`audio_stage.py` to poll the edit queue mid-run), orphan `.wav` TTL cleanup (edited chunks get a new content hash, old `.wav` files under the old hash are never deleted — not urgent, but will accumulate).
* **Verification Method:** Backend logic fully unit-tested via Flask's test client against a throwaway fixture book (in an isolated venv, since Flask isn't installed on the dev host) before deploying — caught and fixed a real bug this way (the cache-patch step was hashing the wrong string; `translate_cache.json` is keyed by hash of the *original source* text, not the translated chunk, so it needed a find-by-value fallback instead). Also caught and fixed an XSS/HTML-injection-adjacent bug where book text was being smuggled through an `onclick="..."` attribute via `JSON.stringify` — fixed by reading the original value from the edit textarea's `defaultValue` instead. Deployed via scp + Flask restart (phone has no `git pull` access), then ran a full real end-to-end test against a live paragraph in `vibe-programming`: created a text edit (the hash-mismatch guard caught an actual copy-paste typo on the first attempt), regenerated its audio (verified the resulting `.wav` is real and downloadable, ~421KB), approved it (confirmed `/api/preview/book` immediately reflected the new text — the overlay became canonical), and confirmed on-device via SSH that **both** `merged_translated_uk.md` and the specific per-batch `..._translated_uk.md` file were patched (this book has `batches/`, so it's exactly the at-risk scenario the investigation flagged). Reverted the edit through the same edit→approve mechanism afterward and cleaned up the orphaned test `.wav` + edit-history entries, leaving the book byte-for-byte as it was before testing. Did not run a full pipeline re-run (`--no-translate`) as a live survival test — judged not worth the device time/risk given the batch-file patch was already directly confirmed, and the no-PDF branch's safety was already confirmed by direct code reading (unconditional existence check before touching the merged file).
* **Type:** `direct`

## [x] TASK-20: Automatic manga typeset QA/fix pass (TASK-19_manga_typeset_autofix.md)
* **Problem:** Three visible manga typeset defects (double/overlapping text in a bubble, translated text overflowing the bubble, font too small or disproportionately large) had unverified root causes. `TASK-19_manga_typeset_autofix.md` traced each to specific code in `translate_manga.py` and specified 5 automatic fix stages to run before any future manual bubble-editing UI (that UI itself remains deferred, unchanged from TASK-19).
* **Solution:** Verified every claim in the doc against the real source (not assumed) before touching anything, and ran real GitNexus `impact()` calls via the MCP HTTP API (proper `initialize`/session handshake this time, not just grep) confirming LOW risk for `fit_text`/`wrap_text` (single caller each: `main`). Implemented 5 new functions and wired them into `main()`'s existing per-page loop, preserving the CLI/file-I/O contract `kbg_web/app.py` and `translate_epub.py` depend on:
  1. **`dedupe_blocks()`** (Stage A) — union-find IoU merge (threshold 0.3) of overlapping detector blocks before OCR, so one physical bubble never gets OCR'd/translated/typeset twice.
  2. **`robust_inpaint()`** (Stage B) — TELEA radius 3px→9px plus a per-block flat-color fill pass (only where the surrounding background is flat enough to trust) for the "ghost text" defect.
  3. **`get_bubble_box()`** (Stage C) — typeset box derived from `mask_refined`'s connected components instead of the tight OCR bbox. First attempt picked "the component under the block's geometric center," which broke on real multi-line text (line gaps split it into several disconnected mask components, so the center often lands in a gap) — caught via a visual box-comparison test against a real image before it shipped, fixed to union all components overlapping the original detection instead.
  4. **Fixed `fit_text`/`wrap_text`** (Stage D) — removed the dead fallback that returned the *entire* untranslated-into-lines text as one unwrapped 8px line when no size fit both constraints (now wraps at a 12px floor instead); added character-level hard-wrap with hyphenation for single words wider than the box alone (Ukrainian runs 20-40% longer than EN/JA source); clamped the font-size search to `[12, min(80, box_h*0.4)]`.
  5. **`post_render_check()`** (Stage E) — flags remaining overflow/floor-size hits into `quality_flags.json` per book, a future input for a manual-review queue (not built in this pass).
* **Verification Method:** Local pure-logic unit tests (`dedupe_blocks` synthetic overlap/chain/edge cases, `wrap_text` hard-wrap+hyphenation with no character loss, `fit_text` floor fallback, `post_render_check` flag/no-flag cases) extracted via `ast` from the real function source and run against real Pillow in an isolated venv — `cv2`/`comic_text_detector` aren't installed on this dev host, only inside the phone's PRoot Ubuntu container. Real-image tests there against `scratch/test_manga.jpg` (phone-only fixture, not in git): the Stage C box-comparison catch described above; confirmed Stage B's inpaint output actually differs from the old radius-3 call (not a no-op) with no regression on the test image's already-clean bubble; a full `--no-ebook` end-to-end run through the wired pipeline completed with zero crashes and produced correctly hyphenated typeset Ukrainian text ("Давайте перекла-\nдемо цю мангу!"), confirmed visually.
* **Type:** `direct`

## [x] TASK-21: Manual bubble-editing UI for manga (TASK-21_manga_manual_edit_ui.md)
* **Problem:** TASK-20 shipped an automatic typeset QA/fix pass but explicitly deferred manual bubble editing. `/api/preview/manga/<slug>` only returned file-name lists (source/cleaned/translated pages) — no bbox, no stable bubble identity — so no UI could draw clickable zones over a page or target a specific bubble for correction. The core engineering risk (flagged by the spec doc itself): `dedupe_blocks` (TASK-20) can legitimately change block count/order between pipeline runs, so a naive index-based bubble ID would silently lose a human edit on the very next regeneration.
* **Solution:** Ran the doc-mandated GitNexus `impact()` checks (proper `initialize`→`Mcp-Session-Id`→`notifications/initialized` handshake) before changing `translate_batch_llm`'s signature and before adding a new route beside `preview_manga` — both LOW risk, single call site each — and folded the results into the reviewed plan rather than pausing mid-task, per explicit user sign-off.
  1. **Stable bubble identity**: `assign_bubble_ids(page_stem, blocks)` sorts blocks by `(y1, x1)` and assigns `f"{page_stem}_b{idx:02d}"` in that reading order (returned parallel to input order, not sorted order). `write_bubbles_meta()` persists `bubbles_meta/<page_stem>.json` (bbox + original/translated text + quality_flags) for every page on a normal full-book run, not just regenerated ones — this is what makes the overlay UI work for freshly-converted manga too.
  2. **`process_page()`** extracted from `main()`'s inline per-page body (the same extract-function move TASK-20 already used for stages A-E, one level up), now shared by both the normal per-book loop and the new single-page regen path.
  3. **`translate_batch_llm(..., overrides=None)`** — texts present in the override map are spliced into the result and skipped in the LLM batch call entirely, so a human edit is never silently overwritten by a fresh (and possibly different) LLM translation on regeneration.
  4. **`match_bubbles_iou(old_entries, new_entries, iou_threshold=0.5)`** — greedy best-IoU-first reconciliation between the old and new `bubbles_meta.json` after a regen. A vanished bubble maps to `None` (edit marked `orphaned`, surfaced for manual review) rather than being silently dropped; an added bubble simply has no old counterpart, which is fine.
  5. **New `--regenerate-page`/`--overrides-json` CLI mode** (`regenerate_single_page()`) — re-extracts the single source page, runs it through `process_page()` with the override map, writes the new typeset image over the existing `translated/`/`cleaned/` files, writes the new `bubbles_meta.json`, patches only this page's entries in the book-wide `quality_flags.json`, and prints the IoU reconciliation mapping as JSON on stdout for `app.py` to parse.
  6. **New `app.py` routes**: `GET /api/preview/manga-bubbles/<slug>/<page>` (404 if unprocessed), `GET /api/preview/manga-quality-flags/<slug>` (`{flags: []}` if absent, not 404), `PUT /api/edit/manga-text/<slug>/<page>` (reuses `edit_store.py` unchanged — its `mode`/`target_id`/`field` schema was already generic enough; `target_id` = `"<page_filename>#<bubble_id>"`), `POST /api/edit/regenerate-manga-page/<slug>/<page>` (gathers this page's pending manga edits, builds the overrides map, shells out to the CLI mode above the same way `run_conversion_api` already invokes the pipeline, parses the reconciliation result, marks matched edits `regenerated` and unmatched ones `orphaned`), `POST /api/edit/discard/<slug>/<edit_id>` (the doc's UI called for a Discard button without ever defining its endpoint in the spec table — added the obvious complement to `approve`). `GET /api/edit/queue/<slug>?mode=manga` needed **zero** code changes — it already forwarded `mode` straight through.
  7. **`stages.html`**: clickable bubble overlay on the "translated" panel (absolute-positioned divs scaled from natural to rendered image size, red/yellow/clean border from `quality_flags`), a side panel (not a full-screen modal — the doc explicitly called out mobile modal-covers-the-image as the reason) with a canvas crop of the clicked bubble, read-only OCR text, an editable translation textarea, and Save; a "Regenerate page" button gated on ≥1 pending edit for the current page; a Pending Edits "Manga" section (page→bubble grouping, word-diff, Approve/Regenerate/Discard) plus a separate "Auto-flagged" subsection reading `quality_flags.json` directly, so a reviewer has a "go look at this" list even for bubbles no human has touched yet.
* **Bugs found and fixed before shipping:**
  1. **`numpy.int32` not JSON-serializable** — the very first real pipeline run crashed writing `bubbles_meta.json`. Root cause: `get_bubble_box`'s `cv2.connectedComponentsWithStats` stats-array indexing returns numpy scalar types, not native Python `int`. Fixed by wrapping both return paths with explicit `int(...)` casts; verified by redeploying and rerunning the full pipeline clean.
  2. **JS cross-scope `ReferenceError`** (caught by code review before any live test, not by a crash) — `escapeHtml`, `wordDiffHtml`, and `window.approveEdit` (from TASK-19) were nested inside `showParagraphsTab()`'s closure, which manga-only books never call, so the new manga Pending-Edits code referencing them would have thrown at runtime on first use. Fixed by promoting `escapeHtml`/`wordDiffHtml` to top-level scope and adding a manga-specific `window.approveMangaEdit` instead of reusing the text-mode-specific `window.approveEdit`. Verified via `node --check` on the extracted `<script>` and a systematic grep of every `onclick="X"` against its `window.X` definition.
* **Verification Method:** Local pure-logic unit tests (`assign_bubble_ids` reading-order ranking with scrambled input, `write_bubbles_meta` round-trip, `match_bubbles_iou`'s 3 required scenarios — identical set / block added / block vanished — plus a no-double-claim integrity check, `translate_batch_llm`'s all-overridden-never-touches-network path) extracted via `ast` from the real source and run in an isolated venv, since `cv2`/numpy/Flask aren't installed on this dev host. Real end-to-end test on the phone's PRoot Ubuntu container against `scratch/test_manga.jpg`: edited one bubble, ran `--regenerate-page`, visually confirmed the edited bubble showed the manual edit verbatim while other bubbles were freshly retypeset correctly. **Live production E2E test on a real book** (`frieren`, backed up before touching, restored byte-for-byte after): submitted a real edit via `PUT /api/edit/manga-text/frieren/...` (`e_7c9973ef5d59`), called the real `POST /api/edit/regenerate-manga-page/frieren/...` endpoint (not the CLI bypass) — response showed all 5 bubbles on the page matched 1:1 via IoU (`bubble_id_mapping`), the edited bubble's `translated_text` was the manual edit verbatim ("Поза межами кінця мандрів (ручна правка)"), the other 4 were freshly LLM-retranslated (e.g. "Frieren"→"Фрірен"); confirmed the edit transitioned to status `regenerated` via `GET /api/edit/queue`; fetched and visually inspected the actual regenerated PNG — clean typesetting, no overflow/artifacts, edited text rendered exactly as submitted. Test survived an unrelated infra hiccup (phone SSH died mid-test when the screen locked, matching a known Termux flakiness pattern; the Cloudflare-tunneled web app stayed reachable throughout, so the API-level test continued unaffected). Restored `frieren`'s original `translated/`/`cleaned/` page files from a pre-test backup (byte-identical per `md5sum`), removed the seeded `bubbles_meta/` directory, and reset `edits/edits.json` to empty — book left exactly as found.
* **Type:** `direct`

## [x] TASK-23: Live editing during active generation (TASK-23_live_editing_phase5.md, Phase 5)
* **Problem:** TASK-19–21 built point-editing that's only safe post-generation (the main generator is no longer running when an edit lands). `TASK-23_live_editing_phase5.md` — flagged by its own author as the riskiest task in the TASK-18..23 series, since it touches the main generation entry points used by every future book, not just an edit overlay — asked for the same capability while a book is still `status=running`, and named three concrete races: (1) a resource conflict if an on-demand edit-regen fires while the main generator is also using the GPU/OCR/LLM/TTS engine; (2) a write race on shared per-batch translation files; (3) a stale-queue risk where the generator might synthesize from pre-edit text after a user already saved a fix.
* **Investigation before touching anything:** the doc explicitly required reporting GitNexus `impact()` blast-radius results and stopping if HIGH/CRITICAL before proceeding. Ran the checks — both `run_conversion_batches.py:main` and `translate_manga.py:main` came back GitNexus-LOW, but that's an artifact of the tool's static call graph not seeing subprocess invocation (these are launched as CLI subprocesses from `kbg_web/app.py`, not direct Python calls) — the real blast radius (every future book conversion) had to come from reading the actual execution model, not trusting the raw number. Doing that reading surfaced two things that changed the plan:
  1. **A real, currently-shipped, unmitigated version of race #1**, live in production before this task touched anything: `edit_regenerate_audio` and TASK-21's `regenerate-manga-page` fired their own subprocess with zero check against whether the main pipeline was already running for that book.
  2. **The doc's assumed "one shared generic worker with a priority queue, chunk-boundary preemption" doesn't fit audio's real architecture.** `audio_stage.py` computes the *entire* missing-chunk backlog once and makes a *single* subprocess call to `bin/tts_helper.py`, which loads the TTS model once and loops internally — there is no existing external per-chunk boundary to preempt at, unlike the genuine per-page loop (manga, already built in TASK-20/21) and per-PDF-batch loop (`run_conversion_batches.py`) that already exist for the other two pipelines.
* **Decision:** rejected the doc's literal one-worker design (forcing a shared abstraction onto three structurally different pipelines was itself the highest-risk part of an already-high-risk task, for no real benefit) in favor of three small, pipeline-appropriate additive changes that deliver the same safety guarantees by extending what already exists:
  1. **Gate the on-demand endpoints** (`edit_regenerate_audio`, `regenerate-manga-page`) on the existing `is_book_process_running(slug)` — if a generator is already running for that book, don't fire a second subprocess; return `{"status": "queued"}` instead (the edit is already durably `pending` in `edit_store.py` from TASK-19, so nothing new needs to be built to track it).
  2. **Manga & batch-translation**: extend `translate_manga.py:main()`'s per-page loop and `run_conversion_batches.py:main()`'s per-PDF-batch loop to check for pending edits against already-completed pages/batches at their own natural iteration boundary, and apply them in-process — reusing TASK-21's `process_page()`/`regenerate_single_page()` machinery (refactored into a shared `_persist_regenerated_page()` helper) and a new `common/edit_patch.py:patch_batch_translation()` (extracted from `edit_approve`'s existing batch-patch logic) respectively.
  3. **Audio**: since there's no external per-chunk hook, extended the *actual* boundary that exists — `tts_helper.py`'s own `for i, chunk in enumerate(chunks)` loop now checks a small `audio_priority_<voice_slug>.json` file once per iteration and splices new entries in, reusing the *already-loaded* model instance instead of spinning up a second one.
  4. **File locks**: new `common/file_lock.py` (`os.O_EXCL` sentinel, bounded retry) wraps `translate_stage.py`'s batch-file write and `patch_batch_translation`'s read-modify-write, closing race #2.
  5. **UI banner** on preview pages while `status=running`, polling `/api/status`, showing queue position.
* **A second real bug found and fixed mid-implementation** (not in the original doc, found while tracing the audio path): `audio_stage.py` had its own redundant "update cache / save cache" step *after* the `tts_helper.py` subprocess call, using a `cache` dict loaded *before* that call ran. `tts_helper.py` already writes the TTS cache incrementally per chunk (the actual source of truth for both engines) — `audio_stage.py`'s post-call overwrite was harmless before this task (the two chunk sets were always identical), but would have **silently clobbered any live-edit chunk `tts_helper.py` spliced in from the priority queue**, since `audio_stage.py`'s own `cache` dict never learned about it. Removed the redundant block; live-tested (below) that all cache entries, including the spliced one, survive to the final file.
* **Verification Method:** Local pure-logic tests (`common/file_lock.py` acquire/release/contention-timeout/context-manager, `patch_batch_translation` find-and-patch/not-found/missing-dir/no-tearing-under-concurrency, `tts_helper.py`'s `_splice_audio_priority` splice/dedup/clear-after-pickup) run directly on this dev host (no cv2/numpy needed for these specific pieces). **First live E2E attempt was invalid and is recorded here for honesty**: ran a full test against a throwaway 6-page manga book and a throwaway 50-paragraph audio book *before remembering to actually `scp` the new code to the phone* — the results looked plausible (edit applied, page correct) but file-timestamp forensics proved a **second `translate_manga.py --regenerate-page` process really did spawn and race the main pipeline** (the exact bug this task exists to fix), because the phone was still running the pre-TASK-23 code with no gate. Caught via `grep -c` on the deployed file showing 0 matches for the new function names — deployed properly (verified by md5sum matching between local and phone copies) and reran both tests from scratch. **Second (valid) live E2E on the actually-deployed code**: manga — created a throwaway 6-page book (duplicated `frieren`'s 2 source pages, no real user data touched), triggered a real conversion via `POST /api/run/`, submitted a live edit against page 0 while pages 1-5 were still processing, called `POST /api/edit/regenerate-manga-page/` and got back the literal `{"status":"queued",...}` response with the process count unchanged at 1 both before and after (confirmed further by the complete *absence* of the on-demand path's own `manga_regen_overrides_*.json` side-effect file — proof it never got past the gate); the main loop's own `apply_pending_manga_edits()` picked up and applied the edit 9 seconds after page 0 finished (`[LiveEdit] Applied 1 pending manga edit(s)...` in the log), all 6 pages processed correctly, 0 leftover processes at the end. Audio — created a throwaway 50-paragraph audio-only book, triggered a real conversion, submitted a live stress-edit against an already-synthesized chunk while `tts_helper.py` was actively mid-run, got back the literal `"queued"` response with the `tts_helper.py` process count unchanged at 1; confirmed via log line `[TTSHelper] Picked up 1 live-edit chunk(s) from priority queue.` that the already-running process spliced it in; after the full run finished, verified the edited chunk's wav file exists **and** — the specific scenario the second bug fix targets — the final `tts_cache_*.json` contains all 51 entries (50 original + 1 spliced), not just the original 50. Both throwaway test books deleted via the production `/api/delete/` endpoint afterward; no real book data was touched by either test. Not live-tested: the batch-translation-loop check (`apply_pending_text_edits` in `run_conversion_batches.py`) against a real multi-batch PDF book with live LLM translation — judged disproportionate phone-time/risk given it reuses the exact same `patch_batch_translation`/`edit_store` functions already proven correct by the local tests and the manga live test's structurally identical in-loop pattern.
* **Type:** `direct`

## [x] TASK-25: Backfill bubbles_meta.json for manga translated before TASK-20/21
* **Problem:** `frieren` (194 pages) and any other manga translated before TASK-20/21 shipped has no `bubbles_meta/` directory, so TASK-21's manual-edit overlay UI has nothing to draw bounding boxes from — it silently shows no clickable bubbles, with no error to explain why.
* **Solution:** New `backfill_bubbles_meta(slug, lang, detector, mocr)` in `translate_manga.py`, invoked via `--backfill-bubbles-meta --slug <slug>` — **strictly read-only w.r.t. `cleaned/`/`translated/` PNGs and never re-translates via LLM**. For each already-translated page without existing `bubbles_meta`: re-runs detect→dedupe→`get_bubble_box` on the untouched *source* image (same Stage A/C calls `process_page()` makes), OCRs the source crop for `original_text`, and OCRs the *existing* translated PNG crop for `translated_text` (never a fresh translate call). Every entry gets `quality_flags: null` (TASK-20's `post_render_check` never ran against these pages' actual historical render, so approximating it would be a lie) and `backfilled: true`. Idempotent — skips pages that already have a `bubbles_meta` file. Required installing `tesseract-ocr-ukr` in the PRoot Ubuntu container (only `eng`+`osd` were present; OCR-ing Ukrainian translated text with the English model would have produced garbage) — maps `target_lang` → Tesseract language code generically (`uk`→`ukr`, `ru`→`rus`, `en`→`eng`), not hardcoded to this one book. `stages.html` updated to render backfilled bubbles with a distinct grey border and explanatory panel text instead of looking identical to a genuinely-clean checked bubble, plus a `confirm()` warning before Regenerate on a backfilled page (regenerating reflows the *entire* page through the current pipeline, not just the edited bubble).
* **Real-world result:** live-ran against `frieren` and immediately surfaced a real geometric bug in the crop/overlay code (see TASK-27) that blocked reliable OCR of the old translated PNGs. After discussion, decided **not** to chase perfect backfill precision for `frieren`'s specific historical data — the relationship between its old `translated/` files and the current `source/` files turned out not to be a simple recoverable scale (confirmed empirically, see TASK-27) — and instead accelerated straight to a full from-scratch regeneration (below), for which the backfill mechanism itself is not needed. The backfill feature remains committed and correct for any *other* older manga book where source/translated dimensions genuinely match or scale uniformly from the origin.
* **Step 8 (full from-scratch regeneration) completed successfully**, after being interrupted and resumed roughly half a dozen times by the phone's screen-lock/Termux-crash pattern (each time cleanly resumed via the pipeline's existing per-page skip-if-already-done logic, with a `termux-wake-lock` fix added mid-session — see TASK-28 — to reduce future interruptions) and after TASK-29/TASK-30 were found and fixed live against real output from this same run. Final result across all 194 pages, target resolution 1072×1448 (Kindle 11th-gen/2024 basic model, confirmed via web search): **1,293 total bubbles, 16 flagged (1.2%) — zero genuine overflow (`overflow_ratio > 1.0`), all 16 are small bubbles that hit the 12px font-size floor while still comfortably fitting (ratios 0.14–0.82)**. Both `frieren_translated_uk.cbz` (285MB) and `frieren_translated_uk.azw3` (66MB) generated successfully. Full pre-existing backup preserved at `scratch/frieren_full_backup_pretask25/` on the phone.
* **Important, deliberately prominent disclosure**: `frieren`'s translated text is now **completely different** from whatever any earlier session recorded — every bubble was re-translated by the LLM from scratch as part of this regeneration. This was an explicit, agreed tradeoff (see the task's own step 8 instructions) to get a clean, fully-current test bed for TASK-19–31, not a bug or data loss. If a future session sees different Ukrainian text on `frieren` than an earlier session's notes describe, this is why.
* **Type:** `direct`

## [x] TASK-27: [BUG][FIXED] Manga bubble overlay/crop geometry ignored downscaled translated/cleaned images
* **Problem:** Found while live-testing TASK-25's backfill on `frieren`: the manual-edit side-panel crop was visibly offset on real pages, and the "Переклад" field sometimes showed truncated fragments of otherwise-correct text. Root cause traced to `get_bubble_box()` always computing `bbox` in the *source* image's pixel space (true for both `process_page()`-generated and backfilled entries), while `stages.html`'s `renderBubbleOverlayDivs()`/`selectBubble()` assumed 1:1 correspondence between `bbox` and whatever `<img>` happened to be on screen — wrong whenever that image (`cleaned`/`translated`) had actually been downscaled relative to the page `bbox` was computed against. Measured on `frieren`: **156 of 193 pages** have `source` ≠ `translated` pixel dimensions.
* **Solution:** Every bubble writer (`process_page()`, `backfill_bubbles_meta()`) now records `bbox_ref_size: [w, h]` — the exact pixel dimensions of the image `bbox` was computed against — alongside each entry. `stages.html`'s overlay and crop code both now scale against `bbox_ref_size` (falling back to the old 1:1 behavior only for pre-fix entries lacking the field, so nothing crashes on stale data). `backfill_bubbles_meta()`'s own translated-text OCR crop step got the same scaling fix (it had the identical bug internally, independent of the UI).
* **A deeper finding changed the fix's scope mid-task:** confirmed via code reading that the *current* pipeline **never** downscales `translated_dir` files (the downscale pass only ever touches the temp CBZ-packaging directory, after `translated_dir`'s full-size copy is already written) — so `bbox_ref_size` will always exactly match for anything the current pipeline produces going forward (fresh conversions, live edits, regenerations). The mismatch is specific to `frieren`'s **historical** `translated/` files, written by some now-unknown older pipeline version. Empirically tested whether that historical relationship was a simple scale-from-origin (the naive fix's assumption) by locating a real reference feature (a panel-divider bar) at its true pixel position in both images — the measured ratio (~0.80–0.83) did not match the naive `translated_width/source_width` ratio (~0.91), meaning an offset/crop is also involved, not just a uniform scale. Rather than keep guessing at an unrecoverable historical transform, decided to accept `frieren`'s backfilled-page OCR/crop precision as best-effort and move straight to TASK-25's full-regeneration option instead, which structurally cannot hit this bug (see TASK-25).
* **Verification Method:** Manual pixel-level empirical testing (not just visual impression) using Pillow directly against the real fetched `source`/`translated` PNGs — cropped the same `bbox` region from both images, visually confirmed the source crop showed the correct, complete bubble text while the naive-scaled translated crop showed a mix of partial text and unrelated artwork; confirmed the `bbox_ref_size` mechanism itself is populated correctly on fresh backfill output (sampled a freshly-written entry, `bbox_ref_size` present and correct). Not further hand-verified pixel-perfect on `frieren`'s specific old pages, per the decision above to move to full regeneration instead — `bbox_ref_size` is verified structurally correct for all current/future-pipeline output, which is what matters going forward.
* **Type:** `direct` (bug found during TASK-25's live testing, fixed same session)

## [x] TASK-28: [BUG][FIXED] Typeset text overflows bubble bounds — stroke_width omitted from all width/height measurements
* **Problem:** Found live-testing `frieren`'s full regeneration (TASK-25 step 8): typeset text visibly overflowed its bubble on real pages (example: `frieren` page 6, bubble "ЦЕ ПЕРШИЙ РАЗ, КОЛИ Я ЗУСТРІЧАЮ ЖЕРЦЯ..."), despite `fit_text()`'s own binary search supposedly guaranteeing a fit.
* **Root cause (confirmed via ordered diagnostic checks before any fix, per explicit instruction not to guess):** `draw_text_centered()` renders every line with `stroke_width=2` (a white outline around black text, for readability against varied backgrounds — a real feature, not a manual bold hack; confirmed only one `draw.text()` call site). But **every** width/height measurement in the typeset path — `wrap_text`, `_hard_wrap_word`, `fit_text`'s `_measure()`, `draw_text_centered`'s own centering math, and `post_render_check`'s overflow-ratio calculation — called `font.getbbox(text)` with **no `stroke_width` argument**, silently measuring the *unstroked* glyph outline while the actual render is `2×stroke_width = 4px` wider and taller per line than what was measured. `fit_text` was therefore always picking a font size that fit the *unstroked* measurement, guaranteed to overflow once the stroke was actually drawn. The same omission in `post_render_check` meant TASK-20's own QA/auto-fix pass under-measured `overflow_ratio` too, letting some genuinely-overflowing bubbles narrowly avoid the `>1.05` flag threshold and ship unflagged.
* **Solution:** New module-level `TEXT_STROKE_WIDTH = 2` constant as the single source of truth for both the render call and every measurement call (8 call sites total across `wrap_text`, `_hard_wrap_word`, `fit_text`, `draw_text_centered`, and `post_render_check`) — eliminates the class of bug entirely rather than just patching the one reported bubble, since the render and measurement values can no longer drift apart.
* **Verification Method:** Wrote a standalone numeric diagnostic (`ImageFont.getbbox()` with vs. without `stroke_width=2`, using the real `LiberationSans-Bold.ttf` font file and the exact reported bubble text) — confirmed a consistent **4px wider / 4px taller** underestimate at every tested font size (20/28/36px) before touching any code. Also confirmed `font_path` genuinely exists (not a silent PIL fallback to a different font) on both the dev host and the phone. After the fix: re-ran `--regenerate-page` for the exact reported page (`frieren` p005/"page 6") — the flagged bubble's `quality_flags` came back `{}` (clean) where it previously overflowed; fetched and visually inspected the actual regenerated PNG crop — "ЦЕ ПЕРШИЙ РАЗ, КОЛИ Я ЗУСТРІЧАЮ ЖЕРЦЯ, ЯКИЙ МОЖЕТЬ ЗАБЛОКУВАТ-И МОЮ МАГІЮ." now sits entirely within the bubble's oval outline (the hyphenated break on "ЗАБЛОКУВАТ-И" is expected wrapping behavior, not a defect). User reported the fix looked unresolved from the live browser after this; investigation traced it to two separate causes rather than a real regression: (a) the phone went unreachable mid-verification (screen-locked, matching the known Termux SSH-dies-on-lock pattern), and (b) the translated PNG is overwritten in place at the same URL, which browsers cache aggressively — a hard refresh was needed to see the corrected image. Re-confirmed server-side data and a fresh (non-cached) image fetch showed the fix genuinely holds.
* **Follow-up bug found from the same disconnect**: the full `frieren` regeneration this fix was being live-tested against **died mid-run** (~page 37/194) when the phone's screen locked. Root cause: `translate_manga.py` never acquired a `termux-wake-lock`, unlike `audio_stage.py` which already does this for its long TTS runs — Android kills backgrounded processes without one on screen-lock. Fixed by wrapping the `if __name__ == "__main__":` entrypoint (not `main()` internally, to avoid re-indenting ~250 lines) in acquire/release, covering the full-book path, `--backfill-bubbles-meta`, and `--regenerate-page` alike since all three can run long. Confirmed `termux-wake-lock`/`-unlock` are reachable from *inside* the PRoot Ubuntu container (proot's bind-mounts expose them) before relying on it. Regeneration resumed correctly from page 38 (confirmed via `"Page N already translated. Skipping."` log lines for pages 1-37) rather than restarting from scratch.
* **Type:** `direct` (bug found during TASK-25 step 8's live testing, fixed same session)

## [x] TASK-29: [BUG][FIXED] Manga dialogue rendered in ALL CAPS instead of normal sentence case
* **Problem:** Reported live against `frieren`'s regeneration: translated dialogue text rendered as full ALL CAPS (looking like a shouted heading) instead of normal Ukrainian sentence case. Root cause: the source text is OCR'd from ALL-CAPS Western comic-book lettering, and the translation LLM was naturally mirroring that input style rather than producing normal prose case.
* **Solution attempted first, and why it wasn't sufficient on its own:** added an explicit system-prompt instruction telling the LLM to output normal sentence case. Tested directly against the real live LLM on the exact reported page before trusting it — **results were inconsistent even within a single bubble** (one line came back fully uppercase, another had all-caps except its last word, a third had only its first two words capitalized). This specialized translation model doesn't reliably follow style instructions layered on top of translation, so prompting alone was not a fix.
* **Real fix:** added `_normalize_sentence_case(text, glossary)` as a post-processing pass on every LLM-translated string: if the ratio of uppercase-to-total letters is ≥0.7, rewrite the whole string in sentence case (lowercase, then capitalize the start of the string and of each sentence after `.`/`!`/`?`), then re-capitalize any glossary term (character names) found in it to its exact glossary casing — the only reliable proper-noun signal available after translation has already happened. Strings already close to normal case (ratio <0.7) are left untouched, so this doesn't fight already-correct LLM output.
* **Known limitation, disclosed rather than silently accepted**: a string with only a short ALL-CAPS *prefix* within an otherwise normal-case sentence won't cross the 0.7 ratio threshold and stays untouched — verified with a real example from this session (`"ЦЕ БОЖІСЬКЕ Благословення, надане жерцям."` was left as-is). Judged an acceptable tradeoff over the alternative (lowering the threshold, which risks incorrectly rewriting genuinely correct strings that happen to contain several capitalized proper nouns).
* **Verification Method:** Local unit test extracted via `ast` from the real source, run against 6 real strings captured from this session's live LLM tests (4 needing normalization, 1 already correct, 1 glossary-name case) — 4/6 correctly normalized including the exact originally-reported bubble, 1/6 correctly left untouched, 1/6 hit the known prefix limitation above (disclosed, not hidden). Re-tested against the real live LLM afterward (not just the local simulation) on the same reported page — all 4 bubbles came back in proper sentence case; fetched and visually confirmed the actual rendered PNG.
* **Type:** `direct` (bug found during TASK-25 step 8's live testing, fixed same session)

## [x] TASK-30: [BUG][FIXED] Long words visually press against oval bubble's curved edge despite passing the rectangular overflow check
* **Problem:** Reported live against `frieren` page 6, immediately after TASK-29's fix landed: a long word (`"заблокувати"`) that now fit as one unbroken line (thanks to proper sentence case using narrower lowercase glyphs) sat visually close to/touching the bubble's curved inner edge, even though `post_render_check`'s `quality_flags` reported clean (`overflow_ratio` under 1.0). This is a *different* bug from TASK-28's stroke-width issue, and it only became visible now because ALL-CAPS text previously hyphenated this exact word across two lines, so this specific word-length-vs-curve collision never occurred before.
* **Root cause:** `get_bubble_box()` returns a **rectangle** approximating what's usually an **oval** speech bubble. Text near the vertical center of an oval has the box's full width available; text near the top/bottom has measurably less real usable width than the rectangle suggests. `fit_text`/`wrap_text`/`post_render_check` all correctly check against the *rectangle's* full width — which is geometrically correct for the rectangle, but not tight enough for the true oval interior.
* **Solution:** new `TEXT_WIDTH_SAFETY_MARGIN = 0.85` constant. `fit_text` is now called with a width narrowed to 85% of the box's real width (uniform safety margin, not position-aware), while `draw_text_centered` still centers and renders against the box's *full* width — so text gets proactively wrapped/sized into a narrower column with breathing room on both sides everywhere, not just at the true vertical center. `post_render_check`'s hard-ceiling overflow check deliberately still uses the full box width unchanged — it's a different, complementary safety layer (proactive margin vs. hard "never exceed the rectangle at all" backstop).
* **Verification Method:** Numeric, before touching the render pipeline: extracted `fit_text` via `ast` and compared font-size/wrap output for the real reported bubble text against the real bbox width, with vs. without the margin — old behavior picked 44px with `"заблокувати"` alone on one line; new behavior picked 39px and correctly hyphenated it across two lines (`"заблокуват-"` / `"и"`), exactly the safer outcome intended. Re-ran `--regenerate-page` against the real live pipeline afterward and visually confirmed the actual rendered PNG shows the word wrapped with clear margin from the bubble's curve.
* **Type:** `direct` (bug found during TASK-25 step 8's live testing, fixed same session)

## [x] TASK-31: Page-jump control for the manga viewer, matching regular books
* **Problem:** Regular books already had a numeric page-jump input + Go button next to Prev/Next (TASK-17), but the manga viewer only had Prev/Next/arrow-key navigation — no way to jump directly to an arbitrary page.
* **Solution:** Added the same numeric-input + Go button pattern to the manga viewer's controls row, wired in `wireMangaViewer()` (Enter key and click both call `showPage(clampedIndex - 1)`). Materially simpler than the books version: manga pages are already all loaded client-side in `bookData` at book-load time (via the existing `/api/preview/manga/<slug>` call), so jumping is a direct `showPage()` call with no network fetch needed, unlike the books version's `fetchParagraphsPage()` which re-fetches a new 30-chunk page from the server. Verified `showPage()` only touches the inner `#viewer-viewport`/`#page-indicator` elements, not the outer controls div, so the one-time listener attachment in `wireMangaViewer()` (called once per viewer-tab render) stays valid across every subsequent page change.
* **Verification Method:** `node --check` on the extracted `<script>` block. Live-deployed and confirmed via a fresh (non-cached, `cache-control: no-store` confirmed via response headers) fetch of `/view/frieren` that the new `manga-page-jump`/`manga-jump-btn` elements are actually served — this surfaced an important, previously-unnoticed infra fact: **this Flask instance does not auto-reload Jinja templates**, so template-only file deployments (`stages.html`) silently don't take effect until Flask is explicitly restarted, unlike Python route changes which obviously need a restart anyway. Confirmed restarting Flask does not affect an in-progress background `translate_manga.py` regeneration (verified process survived, page count unchanged across the restart).
* **Type:** `direct`

## [x] TASK-32: Evolve deploy_oneplus13.sh into deploy.sh — self-sufficient one-curl installer (hardware-tested on the real device; fresh-device llama path spun off as TASK-47)
* **Problem:** User read `deploy_oneplus13.sh` directly and identified 6 concrete gaps preventing it from being a genuine self-sufficient one-curl installer for a *new* device: (1) Step 4 did `mkdir -p`+`chmod +x kbg.sh || true` with no real `git clone` — silently assumed the project already existed by some other means; (2) `cmake .. -DGGML_OPENCL=ON` was missing `-DGGML_OPENCL_USE_ADRENO_KERNELS=ON`; (3) `termux-wake-lock` was only ever acquired inside the future autostart block's Flask snippet, not around the script's own long GPU-compile run; (4) llama.cpp compilation unconditionally `rm -rf`'d and rebuilt every run, regardless of whether working binaries already existed; (5) zero GPU/Adreno detection — Step 2/3 unconditionally assumed Adreno bind-mounts would succeed, no CPU-only fallback; (6) no GitNexus reindex step for a fresh clone; plus a 7th ask to rename the file to `deploy.sh` and fix the README's curl instruction.
* **TASK-22/Vulkan not found:** the user's instructions referenced "check TASK-22 regarding current Vulkan-flag state" — searched this repo and `TASKS.md` exhaustively for "TASK-22" and any Vulkan reference; found nothing. Rather than fabricate a status, implemented exactly the literally-specified `-DGGML_OPENCL_USE_ADRENO_KERNELS=ON` addition and flagged the missing TASK-22 reference explicitly instead of guessing.
* **Solution (all code-complete, `bash -n` syntax-verified):**
  1. New STEP 3 does a real `git clone https://github.com/maxfraieho/kindle-butch-gen.git` (public HTTPS, not the repo's own SSH remote) if `$HOME/kindle-butch-gen/.git` doesn't exist, else `git pull --ff-only` — inserted *before* the OpenCL/compile step (renumbered STEP 4), fixing a latent structural bug: the old Step 3's heredoc wrote into `$HOME/kindle-butch-gen/ubuntu_setup.sh`, a directory that was only ever created *later* in the old Step 4 — the script could never have worked as a true from-scratch installer.
  2. `cmake .. -DGGML_OPENCL=ON -DGGML_OPENCL_USE_ADRENO_KERNELS=ON` now only used on the Adreno-detected path; CPU-only fallback uses plain `cmake ..` with neither flag.
  3. Whole script wrapped in `termux-wake-lock`/`termux-wake-unlock` via a `trap ... EXIT INT TERM` acquired right after the interactive autostart-choice prompt — fires on normal completion, Ctrl+C, and any `set -e` early exit alike, so the device is never left permanently locked-from-sleep. The pre-existing autostart-block wake-lock (scoped to *future* Flask logins) was left untouched as a separate, correctly-scoped concern.
  4. llama.cpp compile now checks `[ -x /usr/local/bin/llama-server ] && [ -x /usr/local/bin/llama-cli ]` (the persistent install destination, not a `/tmp` build marker that Android wipes on reboot) and skips the clone+build entirely if both are already present — correctly distinguishes "genuinely done" from "SIGTERM'd mid-build, nothing installed yet."
  5. Two-stage GPU detection: Stage 1 (Termux-side, before Step 2) checks `[ -e /vendor/lib64/libOpenCL.so ]` — this is the real gate for whether Step 2's Adreno bind-mount launcher and Step 4's OpenCL ICD config + Adreno cmake flags run at all; if absent, prints an explicit message and builds CPU-only instead of crashing. Stage 2 (inside the container, after `apt install clinfo`) re-verifies via `clinfo | grep -q -i platform` and downgrades to CPU-only cmake flags even if Stage 1 passed but the runtime GPU access is actually broken.
  6. Final summary block now prints (not executes) the GitNexus reindex command for a fresh clone — per explicit user choice ("Просто друкує інструкцію"): purely informational text, no network call or SSH attempt from the deploy script itself, since GitNexus only runs on the LAN dev server and a genuinely new device may not even be reachable from it.
  7. `git mv deploy_oneplus13.sh deploy.sh`; updated the actionable curl instructions in `README_Termux.md:33` and `docs/plans/deploy_oneplus13_guide.md` (both live install instructions) to the new filename. Deliberately left `DEPLOY_SCRIPT_VERIFICATION.md`'s historical references to the old name untouched — it's a dated report of a specific past test run, not a live instruction, and rewriting it would misrepresent what was actually tested when.
* **Testing explicitly NOT done, deferred rather than skipped (single consolidated blocked item — a+b+c together, not three separate tasks):** the task's own explicit requirement was real hardware verification, not "code compiles." **Why blocked:** this Claude Code session is executing on the OrangePi PC2 (confirmed via `uname`, `/proc/meminfo` ≈995MB, IPs `192.168.3.161`/`100.65.225.122`), which has no `proot-distro`, no `termux-wake-lock`, no `/vendor/lib64/libOpenCL.so`, no `/dev/kgsl` — it cannot run this Termux/Android-specific script at all, and simulating the test elsewhere would give a false sense of verification. User confirmed: phone unreachable this session, pause tests rather than fake them, do not mark TASK-32 done. **Expected device availability: this evening (2026-07-16).**
  * **(a)** clean `proot-distro` container + real public curl install with zero manual intervention.
  * **(b)** SIGTERM mid-compile + restart, verifying git does `pull` not a duplicate `clone` and llama.cpp does not wastefully recompile.
  * **(c)** full backup of `$HOME/kindle-butch-gen` (contains `frieren`, `vibe-programming`, `three-days-of-happiness`, and the `frieren_full_backup_pretask25` backup) → `rm -rf` → reinstall via the actual public curl command → confirm genuine self-sufficiency → restore backup.
  * Resume as a continuation of this same task (not a new one) once the OnePlus 13 is reachable.
* **Type:** `direct` — code-complete, hardware-test-blocked (see PENDING_HARDWARE_TEST above)

## [x] TASK-34: [BUG] Manually edited manga bubble translation on frieren was lost — traced to client-side, before the save request ever reached the server
* **Problem:** Volodymyr manually edited a bubble's translation on `frieren` via the manga side panel (💾 Save button), but the "Pending Edits" tab later showed "Немає pending-правок для манги" — empty. (Explicitly not to be confused with the separate, already-known "Auto-flagged" list of 16 min-font-size records from TASK-31 — that list is unrelated benign geometry flags, not a lost human edit.)
* **Investigation, in order, each step's real result before concluding anything:**
  1. Queried the live `edit_store` directly via `GET /api/edit/queue/frieren` (no mode/status filter, so it can't be a filter bug hiding a real record) against the production app (`kindle.exodus.pp.ua`) — result: **`[]`, completely empty**. No manga edit, no edit of any mode, exists for `frieren` in `edits/edits.json`. This rules out "UI/query filter bug hiding an existing record" (the user's own hypothesis #2) outright — there was nothing to hide.
  2. Since `edit_regenerate_manga_page()` only ever pulls edited text *from* `edit_store` (`edit_store.list_edits(slug, mode="manga", status="pending")`), and that store is empty, the exported `frieren_translated_uk.cbz`/`.azw3` (confirmed via `Last-Modified` header: both rebuilt today, 2026-07-16 08:44 UTC) **cannot** contain the edit either — this follows deductively from the code path, verified without needing to unzip and diff the 285MB archive.
  3. Read the client-side Save handler (`kbg_web/templates/stages.html:926-945`, `saveBubbleEdit()`): it `fetch()`s `PUT /api/edit/manga-text/<slug>/<page>` with the correct `bubble_id`/`translated_text` body; on failure it sets a visible `"Error: " + err.message"` in the status line — not a silent failure by design.
  4. **Asked Volodymyr directly** (per this task's own instruction) whether he saw any status message after clicking Save. His answer: *"я клікав на Save, кнопка змінювалась, але ніяких повідомлень не було"* — he clicked Save, saw the button's own click reaction, but **no status text appeared at all** — not "Saving...", not "Saved as pending edit.", not "Error: ...".
* **Conclusion (confirmed vs. theorized, kept explicitly separate):**
  * **Confirmed:** the edit never reached `edit_store` — it was lost before or at the point the save request would have been sent to the server. This is a **client-side loss**, not a server-side or export-pipeline loss.
  * **Confirmed:** `saveBubbleEdit()`'s very first action after validating non-empty input is a synchronous `statusEl.textContent = "Saving..."`, executed *before* the network call. Since no such message was ever seen, the JS most likely never reached (or never completed) that line — consistent with the fetch call never firing at all.
  * **Theorized, not confirmed (disclosed as such, not stated as fact):** the most likely concrete mechanism is a JS-level failure in the click handler itself (e.g. the inline `onclick` handler throwing before entering the function body, or a stale/unavailable DOM reference) — but this could **not be pinned down definitively**: this session has no SSH access to the phone (browser devtools/console unreachable), no Flask access-log endpoint exists in the app to check server-side arrival independently, and `pinchtab` (browser automation) is not installed on this host, so the bug could not be reproduced live to capture the actual exception.
  * **Ruled out:** server-side failure (would have shown a visible `"Error: ..."` message, and none appeared), the exported files being stale from before the edit (not a valid theory once #1/#2 already show `edit_store` itself never received the record — an empty store can't produce a "the export just needs rebuilding" fix).
* **Real gap found while reading this code, independent of pinpointing the exact bug:** `closeBubblePanel()` (`stages.html:920-924`) has **no unsaved-changes guard** — clearing/closing the panel or navigating to another bubble/page silently discards any typed-but-unsaved text with zero warning. Whether or not this specific incident was that exact path (Volodymyr's answer suggests he did click Save, so this may not be the cause *here*), it's a real, confirmed-by-reading UX gap that can cause this same class of silent loss again and is worth hardening regardless.
* **Answer given to Volodymyr:** the edit was lost, on the client side, before the save request was ever recorded server-side — not an export/regeneration/UI-filter issue. The exact JS mechanism could not be conclusively identified without live browser reproduction, which wasn't available this session. He will need to re-make the edit.
* **Root cause CONFIRMED (upgraded from theorized), via live DevTools console:** user reproduced the failure and reported the exact browser console error: `Uncaught SyntaxError: missing ) after argument list (at frieren:1:27)`. Cross-checked against the real live data (`GET /api/preview/manga/frieren`): every one of frieren's 187 `translated_pages` filenames is derived from the scanlator archive name and contains a literal apostrophe — e.g. `"Frieren - Beyond Journey's End - c118 (v13) - p000 [VIZ Media] [Digital] [1r0n].jpg"`. Two spots in `kbg_web/templates/stages.html` interpolated this raw, unescaped filename into a single-quoted inline `onclick=""` attribute: `:908` (`saveBubbleEdit`, the Save button) and `:1069` (`regeneratePageFromQueue`). The apostrophe prematurely closes the JS string literal inside the attribute, so the browser can never parse the handler at all — and an inline handler that fails to parse **never executes on click**. This is a total, universal failure for this book (and any book whose filenames contain an apostrophe): every Save click on every bubble, on every page, silently did nothing, explaining both TASK-34's original report and Volodymyr's later report that repeated fixes kept reappearing (nothing had ever actually saved, not once).
* **Fix implemented:** added `jsAttrEscape()` (`stages.html`, next to the existing `escapeHtml()`) — JS-string-literal escaping (backslash-escapes `\` and `'`), explicitly distinct from `escapeHtml()`'s HTML-entity escaping, which does **not** solve this: `'` → `&#39;` still HTML-decodes back to a literal `'` before the JS parser ever sees it. Applied at both vulnerable call sites.
* **Verified numerically before deploying** (not just `node --check` syntax validation): wrote a script (`verify_fix.js`) that reproduces the exact reported error using `new Function()` with frieren's real filename against the **unfixed** template — got the identical `SyntaxError: missing ) after argument list`. Against the **fixed** template (with `jsAttrEscape`), parsing succeeds and the value `saveBubbleEdit` receives round-trips to the exact original filename (apostrophe correctly restored via the JS engine's own escape-sequence decoding) — confirmed for the real frieren filename, a backslash+quote edge case, and a plain filename with no special characters (no regression).
* **Deployment surfaced a separate, significant infrastructure problem:** attempted to deploy via the normal `git push` (to `origin/master`, done) + `git pull` (on the phone) workflow used all session — but the phone's `~/kindle-butch-gen` checkout can reach **neither** of its two configured remotes non-interactively: `origin` (`git@github.com:...`) fails with `Permission denied (publickey)`, and `server-projects` (`ssh://vokov@192.168.3.184/...`) fails with `Permission denied (publickey,password,keyboard-interactive)`. The phone's local HEAD was stuck at commit `cec9c11` — far behind `origin/master` — with several genuine uncommitted local modifications (`audio_stage.py`, `bin/tts_helper.py`, `kbg.sh`, `books/vibe-programming/...`) never pushed either. TASK-19 through TASK-31's live fixes earlier this same session almost certainly reached the phone via direct local edits (a prior Claude Code instance running in Termux on the device itself, before this session continued on the OrangePi per TASK-32's investigation) rather than through git — meaning **git push from this OrangePi session does not, by itself, deploy anything to the phone**, and every future fix needs either a working SSH credential fix on the phone or a direct `scp` (as done here) until that's resolved. Diffed the live file against the local fixed copy before deploying to confirm the only differences were the three intended fix lines — then `scp`'d `stages.html` directly to `~/kindle-butch-gen/kbg_web/templates/stages.html`, killed and restarted the Flask process (`kbg_web/app.py --port 5000` — this app doesn't hot-reload templates, confirmed back in TASK-31), and verified via the public tunnel that the served page now contains the fix.
* **Real confirmation, not just "code compiles" (per this task's own explicit requirement):** Volodymyr retried the edit live — UI showed "Saved as pending edit.", the "📝 Pending Edits" tab correctly displayed the new entry grouped under the real page filename with Before/After diff and Approve/Discard buttons. Independently confirmed server-side via `GET /api/edit/queue/frieren`: a real `pending` record now exists, `target_id` = `"Frieren - Beyond Journey's End - c118 (v13) - p003 [VIZ Media] [Digital] [1r0n].png#...c118 (v13) - p003 [VIZ Media] [Digital] [1r0n]_b00"` — the apostrophe-containing filename correctly preserved end-to-end, exactly matching what the numeric verification predicted.
* **Type:** `direct` (bug found live, root cause confirmed via reproduced console error, fixed, numerically verified, deployed to production via `scp` due to broken git remotes on-device, and confirmed working via a real user-performed save + independent server-side API check)

## [x] TASK-35: [INVESTIGATED, NOT A BUG] "Regenerate page" reported "No pending edits" for a page that visibly showed one in the Pending Edits UI
* **Problem:** Immediately after TASK-34's fix, `frieren` page `c118 (v13) - p003 [VIZ Media] [Digital] [1r0n].png` — clicking "🔄 Regenerate page" returned `{"status": "error", "message": "No pending edits for this page - nothing to regenerate"}`, even though the Pending Edits tab visibly showed 1 real edit for that exact page with a correct Before/After diff. User's own hypothesis, in priority order: (1) URL-encoding mismatch between the frontend's fetch request and how the page is keyed in `edit_store`/`bubbles_meta` (apostrophe/spaces/parens/brackets in the filename), (2) if not that, a divergent matching criterion between the list endpoint (`/api/edit/queue`) and the regenerate endpoint (`/api/edit/regenerate-manga-page`).
* **Investigation, in the order requested — encoding checked FIRST, exactly as instructed:**
  1. Reconstructed the exact fetch URL `regeneratePageFromQueue()` builds (`stages.html:1165`, plain template-literal interpolation, no `encodeURIComponent`) and ran it through Node's WHATWG `URL` parser (spec-identical to what Chrome/Firefox use) to see the real percent-encoded request, then simulated Werkzeug's standard percent-decoding of the resulting path segment. Result: **byte-for-byte match** against the real filename — no encoding bug.
  2. Confirmed the apostrophe in the real stored `target_id` is a plain ASCII `'` (U+0027), not a Unicode smart quote — ruled out a subtler character-identity mismatch.
  3. Issued the actual live POST request via `curl` with the exact same percent-encoding the simulation predicted, against production — reproduced the identical `"No pending edits..."` response, confirming this is a real, repeatable server-side condition (not a browser-only quirk).
  4. Given `app.py` is the shared source of truth for this session's earlier `stages.html` mismatch (TASK-34), first verified `app.py` itself wasn't *also* stale on the phone the same way `stages.html` had been — `scp`'d the live `kbg_web/app.py` off the phone and diffed against the local repo: **identical, no diff**. Ruled out a stale-deploy explanation for the regenerate route's own logic.
  5. Went to the actual source of truth on-device rather than continuing to reason about it: read `books/frieren/edits/edits.json` directly over SSH. **The edit's `status` was `"regenerated"`, not `"pending"`**, `applied_at` ≈ 4 minutes after `created_at`. Cross-checked `books/frieren/bubbles_meta/<page>.json` for bubble `_b00`: `translated_text` was already `"Вступ"` — the edited value, not the original OCR garbage.
* **Conclusion: not a bug.** The edit had already been successfully applied and consumed by the time the "No pending edits" response was seen — that response was **accurate**, not a false negative. `edit_regenerate_manga_page()` (`app.py`) checks `is_book_process_running(slug)` first; if `frieren`'s background `translate_manga.py` process was running around that time, a manual click would have returned `"queued"` instead, and that process's own per-page loop (`apply_pending_manga_edits`/`regenerate_single_page`, confirmed to exist via a GitNexus code-graph query against this repo) auto-applies any pending edit as it reaches that page — independent of the UI button. Either that automatic pickup, or an earlier manual click that succeeded without the user fully registering it, explains the sequence; both are legitimate, non-buggy paths, and the *only* way this could be seen as a UX gap is if the Pending Edits tab's view was stale (not re-fetched) between the edit being consumed and the user's later manual retry — not investigated further, since the underlying data is confirmed correct and no code defect was found.
* **No fix made** — there is nothing to fix; the reported symptom was a correct response to already-resolved state, not a defect in the matching logic. Explicitly did not touch the unrelated garbled BEFORE-text in this same edit, per instruction — that belongs to TASK-33.
* **Side effect of this investigation — GitNexus set up for real use on this project:** discovered via a live GitNexus query (once actually indexed and reachable, see below) that `translate_manga.py` has its own `apply_pending_manga_edits()`/`regenerate_single_page()`/`_persist_regenerated_page()` functions distinct from `app.py`'s route-level guard — useful context for future TASK-33 work in the same file. Per the user's standing instruction ("gitnexus використовуй завжди, це закон"), registered `gitnexus` as an HTTP MCP server (`http://192.168.3.184:4747/api/mcp`, via `~/.claude.json`) instead of a risky local `npx gitnexus` install (that install is documented to fail even on the stronger dev server — ~600MB onnxruntime/CUDA postinstall). Indexed `kindle-butch-gen` for the first time from this session (was previously undocumented in `exodus-infra`'s repo list despite already existing from an earlier session), then re-synced the dev server's own stale clone (`git pull`, was 5 commits / TASK-32-34 behind) and re-indexed once more so the graph reflects current `master`.
* **Type:** `direct` (investigation only, confirmed correct existing behavior — no code change)

## [x] TASK-36: Manual bubble-geometry editing (drag/resize) + box-overlap detection (Claude direct, AGY fleet quota-limited)
* **Two related but distinct problems, deliberately kept in one task:**
  1. **Awkward auto-computed text-zone shape** — reported example: "Вступ" on `frieren` page `c118 (v13) - p003`, bubble `_b09` — a narrow vertical column forcing a letter-by-letter wrap. Needs a direct tool for the human to widen the box themselves.
  2. **Confirmed real box-overlap between neighboring bubbles** (visually confirmed by the user on live screenshots, pages 3 and 5 of the same source) — a bug in the automatic box computation, not a UX nicety. Under separate investigation in parallel; manual editing (item 1) is also the immediate manual workaround for item 2's symptoms until the root cause in `get_bubble_box()` is confirmed/fixed.
* **Scope (full spec, verbatim from planning — see PROMPT below for the exact AGY brief):**
  1. New edit type `manual_bbox_override` (`{bubble_id, bbox: [x1,y1,x2,y2], ref_size: [w,h]}`) in `edit_store`/`bubbles_meta` — reuses the `bbox_ref_size` reference-scaling approach already established in TASK-27, not a new coordinate mechanism.
  2. UI: 8 resize handles (4 corners + 4 edge-midpoints) on the bubble overlay (same overlay as TASK-21/25). Drag a corner/edge to resize; drag the box body (not a handle) to move it. **Mobile-critical**: handle tap targets ≥32-40px even though the visual frame is thinner — small handles are unusable on a phone touchscreen. Live drag preview updates only the rectangle indicator (no expensive text re-render mid-drag). Separate "Save position" action from the existing text-Save (geometry and text edits are independent). Numeric x1/y1/x2/y2 fields as a precise fallback alongside drag, since precise finger-dragging on a phone is unreliable.
  3. `regenerate-manga-page` accepts a `{bubble_id: manual_bbox}` map and uses it **instead of** `get_bubble_box()` only for those specific bubbles — rest of the page processed normally, same override pattern already used for text edits.
  4. **Separate investigation, run first, before deciding on a fix approach**: IoU-based overlap detection across every bubble pair on every already-processed `frieren` page — real count needed before deciding whether this is a systemic `get_bubble_box()` padding bug (fix + `impact()` first, since it's been touched multiple times already — TASK-20, TASK-27) or a rare edge-case layout (flag-only, no algorithm change).
  5. New `box_overlap: true/false` + overlapping-bubble-id-list flag in `quality_flags.json` (same TASK-20 infrastructure) — surfaces in the "Auto-flagged" Pending Edits list as a priority candidate for manual geometry editing (item 2), directly connecting the two halves of this task: automatically-found overlaps become a ready-made worklist for where to manually nudge a box.
* **Pre-delegation GitNexus impact check (per this task's own request, run before handing off):**
  - `impact(get_bubble_box, downstream)`: **LOW risk, only 1 direct dependent found** (`_padded_bbox_fallback`). This is a *lower* blast radius than expected from direct code reading earlier this session (TASK-25/27/28 all touched call sites of `get_bubble_box` across `process_page()`/backfill/regenerate paths) — flagging this honestly as a probable **graph-coverage gap** (the indexed call/process graph likely doesn't fully resolve every caller of this function) rather than trusting it as a complete picture. AGY should not treat "LOW risk, 1 dependent" as license to change `get_bubble_box()`'s padding logic without also manually verifying real call sites via `grep -n "get_bubble_box("` before touching it.
  - `impact(edit_regenerate_manga_page, upstream)`: 0 dependents, LOW risk — expected and unremarkable, since this is an HTTP route entry point invoked externally, not called by other in-repo Python code.
* **Implementation:** originally planned for AGY3 delegation (fleet's usual pick for complex multi-step TS+Python work), but the AGY fleet is quota-limited right now — user redirected to have Claude implement directly this session instead.
* **Extra requirement added mid-task:** manual font-size override alongside geometry, since text sometimes overflows a bubble purely due to auto-picked font size, independent of box shape. Implemented as an optional `font_size` field on the same `manual_bbox_override` edit (either field can be set without the other) - `+`/`−` buttons and a numeric field next to the geometry controls, `fit_text()`'s own binary search is skipped in favor of the manual size (still passed through `post_render_check`, so a manual size that's still too big gets flagged, not silently hidden).
* **Overlap statistics (real, book-wide, run BEFORE deciding fix scope, per the task's own instruction):** 105 overlapping pairs (IoU>0) across `frieren`'s 187 pages before any fix; 87 pairs / 161 distinct bubbles at IoU≥0.02 (the threshold used everywhere downstream - below that is boundary-rounding noise). Confirmed **systemic**, not the 2 cases spotted by eye - proceeded to fix the padding algorithm itself, not just add a flag.
* **Root cause confirmed:** `get_bubble_box()`'s 15% padding was applied to every bubble completely independently, with zero awareness of neighboring bubbles on the same page - closely-spaced bubbles' padded boxes routinely overlapped.
* **Fix implemented and unit-tested (`translate_manga.py`):**
  1. Split `get_bubble_box()` into `_get_bubble_core_box()` (unpadded mask-union/OCR-bbox, unchanged logic) and `_apply_padding_neighbor_safe()` (padding, now clamped to a **midpoint split** with any neighboring core box within `min_gap=3px`). Backward compatible when no neighbors are passed.
     - Unit tests caught and fixed a real bug in the first attempt: clamping each box to "min_gap from the neighbor's raw edge" let both sides independently eat into the *same* shared gap and still overlap (a 10px-apart pair left a 4px overlap). Fixed by clamping to the midpoint between the two cores instead - each side then provably stays on its own half.
  2. `process_page()` and `backfill_bubbles_meta()` both restructured into two passes: compute every bubble's core box first, then pad each one against all the others - avoids the ordering dependency a single-pass neighbor-aware scheme would have.
  3. New `compute_box_overlap_flags()` (pairwise IoU) wired into `process_page()`'s end (flags future regens/backfills going forward) and a new `--backfill-box-overlap-flags` CLI mode (no model loading needed - pure JSON read/write) - **already run against frieren's real, existing data**: matches the pre-fix exploratory scan exactly (87 pairs, 161 bubbles), confirmed live via `/api/preview/manga-quality-flags/frieren` (161 `box_overlap` entries alongside the 16 pre-existing overflow/min_size ones, none clobbered).
  4. New `box_overlap`/`overlapping_with`/`iou` fields merged into each affected bubble's own `quality_flags` (what the manual-edit side panel reads) as well as the book-wide `quality_flags.json` (what "Auto-flagged" reads) - surfaces overlapping bubbles as priority manual-edit candidates, closing the loop between the two halves of this task. `bubbleFlagColor()` gives `box_overlap` its own blue marker, distinct from the existing red/orange render-geometry colors (a bubble can carry both at once).
* **Manual geometry/font-size editing - backend (`kbg_web/app.py`, `translate_manga.py`):**
  - New edit type `manual_bbox_override` (field, not a new `mode` - stays grouped with text edits under the same page in Pending Edits) via a new `PUT /api/edit/manga-bbox/<slug>/<page_filename>` route - `{bubble_id, bbox, ref_size, font_size}`, any subset of `bbox+ref_size` / `font_size` present.
  - Reuses TASK-27's `bbox_ref_size` reference-scaling approach exactly as instructed: the client captures bbox/font-size against whatever image was on screen at edit time; `_scale_bbox_overrides()` (unit-tested) scales both to the *actual* regen's working image dimensions once known (deferred to inside `translate_manga.py`, since app.py doesn't know that size ahead of time).
  - `process_page()` accepts `bbox_overrides`/`font_size_overrides` (keyed by `original_ocr_text`, same stable-key convention the existing text `overrides` already use, since positional bubble IDs aren't stable across a full re-detection) - used verbatim instead of `get_bubble_box()`/`fit_text()` for a matching bubble, and the manual box still counts as a neighbor for every other bubble's own padding clamp. Wired through both regeneration paths: `apply_pending_manga_edits` (in-process live regen) and `regenerate_single_page` (CLI/subprocess, via a new `--bbox-overrides-json` flag alongside the existing `--overrides-json`, kept as a separate file/flag rather than changing the existing text-override format).
- **Manual geometry/font-size editing - frontend (`kbg_web/templates/stages.html`):** 8 resize handles (4 corners + 4 edge-midpoints, drawn as `.bbox-edit-handle` divs) plus a draggable `.bbox-edit-body` for moving the whole box, all via the Pointer Events API (unifies mouse+touch, `touch-action:none` set explicitly). **Mobile-critical requirement met**: each handle's actual pointer-catching target is a fixed 36×36px (`HANDLE_TAP_SIZE`), well inside the requested 32-40px range, regardless of how thin the visible dashed-border frame is. Live drag preview repositions only the rectangle/handles (`renderResizeHandles()`) - no text/image re-render mid-drag, that only happens server-side on an actual Regenerate click. Numeric x1/y1/x2/y2 fields plus a font-size field (with `+`/`−` buttons) sit right below the drag area as the precise fallback the task asked for, bidirectionally synced with the same `editingBubbleBox` state the drag handles update. A separate "💾 Зберегти позицію" button (independent from the existing translation-text "💾 Save") posts to the new endpoint - either can be used without the other.
* **Real, live-tested confirmation (not synthetic) - and an important discovery mid-test:**
  - Test (a): manually widened the exact "Вступ" bubble (`c118 p003`, `_b00` - the live data confirmed this is the bubble the screenshot referred to; there is only one bubble on that page) from a 142×921px near-vertical column to 409×288px. **Mid-test, Volodymyr independently tried the newly-deployed drag UI live on his own phone** and saved his own geometry edit on the same bubble - unplanned, organic validation that the feature works standalone. This also surfaced a real, pre-existing (not introduced by this task) limitation: **a text edit whose status is no longer "pending" (already applied by an earlier regen) is NOT re-included the next time the same page is regenerated for an unrelated reason** (e.g. a later geometry fix) - since only currently-pending edits are read into `overrides`. Caught this because the "Вступ" text had reverted to the original garbled OCR after an intermediate regen; re-saved it as pending, then a final regenerate correctly applied both his geometry edit and the text fix together. Confirmed **visually** (not just via JSON) - "Вступ" now renders as one clean unbroken line, `quality_flags: {}`.
  - Test (b): the worst confirmed overlap by IoU (`c125 p147`, `_b02`↔`_b04`, IoU 0.36, `_b04` fully nested inside `_b02`'s vertical range) - moved `_b04` down below `_b02`'s bottom edge via the same API. Confirmed **visually**: "Батьку?" now sits cleanly separated below "Щось не так, батьк-у?", no more overlap. `_b02`/`_b04`'s `box_overlap` flag is gone (an unrelated, separate `_b01`↔`_b02` overlap on the same page - likely an OCR-fragmentation artifact of the same dialogue line getting detected as 2 pieces - was deliberately left untouched, out of this specific test's scope).
  - Test (c): covered by the book-wide backfill numbers above (87 pairs / 161 bubbles), not just the 2 visually-spotted cases.
* **Deployment note (same broken-git-remotes situation as TASK-34/35):** all three files (`translate_manga.py`, `kbg_web/app.py`, `kbg_web/templates/stages.html`) diffed against the live phone copies before overwriting (confirmed clean, no unrelated drift each time), `scp`'d directly, Flask restarted. GitNexus reindexed on the dev server after each push, per the now-standing rule.
* **Loose end found, not investigated further this task:** while testing, observed the phone under significant memory pressure (309MB free of 15GB total RAM, 3.7GB swap in use) during a page regeneration, and the server-side `subprocess.run(..., timeout=180)` did not actually kill the underlying `python3 translate_manga.py` process running inside the `proot-distro` container when it exceeded that timeout (only its direct wrapper) - the real regen kept running orphaned in the background for several more minutes past the point Flask/Cloudflare had already given up and returned an error to the client. Worth a closer look separately; not blocking, since the orphaned process did eventually complete correctly on its own both times this was observed.
* **Type:** `direct` - systemic bug found via a real book-wide scan (not assumed from 2 examples), root cause fixed and unit-tested (including a bug caught in the first attempt at the fix itself), new feature built end-to-end (backend + touch-friendly frontend), deployed to production, and confirmed with real before/after screenshots on two independent real cases plus one live, organic, unplanned user test.

## [x] TASK-36 follow-up: [BUG][FIXED] resolved manga edits didn't survive a LATER, unrelated regeneration of the same page
* **Problem, prioritized correctly over TASK-33 per explicit instruction:** the "loose end" flagged above turned out to be the most important finding in the whole TASK-36 report, not a footnote - `process_page()` re-runs a page's OCR+LLM translation completely from scratch on every regeneration, so an edit that had already reached `status="regenerated"` in a PAST run was invisible to app.py's/translate_manga.py's override-building code, which only ever looked at `status="pending"` edits. TASK-36 itself made this bug's real-world blast radius much larger: **manual geometry editing on any single bubble now regenerates the ENTIRE page**, so every future geometry-only fix on a page that has an older, already-approved text fix would silently roll that text back to a fresh (and possibly different) raw translation - invisible until the page is reopened. Exactly the "quietly eats already-done work" class of bug.
* **Fix (`kbg_web/app.py`'s `edit_regenerate_manga_page`, `translate_manga.py`'s `apply_pending_manga_edits`):** the "should a regen happen at all" gate is unchanged - still requires at least one genuinely new `status="pending"` edit for the page (so clicking Regenerate on an already-fully-applied page is still a no-op). But once a regen is triggered, the override set fed into `process_page()` now includes **every edit for that page with `status` in `{"pending", "regenerated"}`**, not just the new one(s) that triggered it - so a historically-approved fix rides along on every future regen of the same page, indefinitely, rather than only surviving the one run it was first created in. `"orphaned"`/`"discarded"` edits are correctly excluded (no longer meaningful to reapply). After a successful regen, ALL included edits (old and new alike) get their status re-confirmed via the same IoU IDreconciliation as before - `"regenerated"` (fresh `applied_at`) if their bubble still resolves, `"orphaned"` if not.
* **Deliberately out of scope, disclosed rather than silently expanded:** the separate `"approved"` status (reachable directly from `"pending"` via the Approve button in the Pending Edits tab, which for manga mode does **not** touch the rendered image at all - existing, pre-TASK-36 behavior, confirmed by re-reading `edit_approve`'s own code comment) was deliberately **not** added to the reapply set. An edit that only reached `"approved"` was never actually baked into a render in the first place, so treating it as "should be reapplied on every regen" would be a different, separate fix than the one asked for here.
* **Real, live-tested confirmation (not synthetic):** used the exact bug scenario from TASK-36's own testing - the "Вступ" bubble (`c118 p003`), which had a resolved (`status="regenerated"`) text edit. Deployed the fix, then saved a brand-new, purely-geometric edit (bbox `[900,320,1330,630]`) and triggered Regenerate **without** touching the text edit at all. Confirmed via the live API: the historical text edit's status stayed `"regenerated"` throughout, and the new render's `bubbles_meta` shows `translated_text: "Вступ"` (not reverted to the raw OCR garbage this time) alongside the new geometry - both landed together automatically. Confirmed **visually** too: "Вступ" renders cleanly, unbroken. This is the same page/bubble where the bug was originally caught, now proven fixed against the identical scenario.
* **Type:** `direct` - bug prioritized correctly per explicit user instruction over the next queued task (TASK-33), fixed, and confirmed against the exact real scenario that surfaced it.

## [x] TASK-33: Conservative post-OCR noise cleanup + source_confidence axis + p022_b06 investigation
* **Scope, per the original spec:** (1) strip only edge-anchored OCR noise before translation, logged not silent; (2) new `source_confidence` quality_flags axis, separate from render geometry; (3) investigate `p022_b06`'s "two overlaid texts" report before fixing anything - could be a real merged-detection bug or a UI crop-preview bug, a possible third problem class; (4) test against the 3 real reported bubbles (`c118 p019 _b00`, `c118 p020 _b12`, `c119 p027 _b01`), not synthetic cases.
* **p022_b06 investigation (done first, per instruction, before any fix):** confirmed **real merged/duplicate bubble detection**, not a UI z-index bug. `_b07` ("IT'S NOT THAT I'M THE STRONGEST.") is **100% fully contained** inside `_b06`'s box ("- IT'S NOT — STRONGEST. IT'S THAT ALL THE OTHER WARRIORS OF...") - the intersection exactly equals `_b07`'s entire area. `dedupe_blocks()`'s existing pure-IoU check (threshold 0.3) missed this because IoU comes out low (~0.14) purely from the two boxes' AREA difference, even at 100% containment relative to the smaller box - a known weakness of plain IoU for asymmetric-size duplicates. **User explicitly asked to fold this into TASK-33's scope** once confirmed.
* **Fix 1 - `dedupe_blocks()` containment-ratio check (`translate_manga.py`):** new `_containment_ratio()` (intersection / area of the SMALLER box) checked alongside the existing IoU test - merges if EITHER `iou_threshold` (0.3, unchanged) OR `containment_threshold` (0.85, new) is crossed. Unit-tested: the real p022 case (containment=1.0) now merges; a synthetic pair of genuinely separate bubbles (containment=0) stays unmerged; a synthetic small block touching just a corner of a big one (containment=0.11, below threshold) correctly stays unmerged too - confirms no over-eager merging of genuinely-adjacent-but-distinct bubbles. **Live-tested on the real page**: bubble count dropped from 10 to 9, the duplicate "IT'S NOT THAT I'M THE STRONGEST" fragment is gone entirely, `_b06`'s full text intact. Confirmed visually - one clean text block, no more overlap.
* **Fix 2 - `clean_ocr_edge_noise()` (`translate_manga.py`), applied BEFORE translation:** strips ONLY noise-character runs (`|\/{}@:`, exactly the set specified, nothing added) anchored at the very start/end of raw OCR text - never touches the same characters mid-sentence (e.g. a stray mid-sentence backslash in `"...OUR \ FUTURE."` is deliberately left alone, matching the explicit "don't touch legitimate punctuation" instruction even though it's clearly noise too - a harder, out-of-scope problem). Every actual removal logged (`[OCR-noise] page/block: 'before' -> 'after'`) for audit, never a silent edit; falls back to the original string if the WHOLE text turns out to be noise (never returns/ships an empty string). Wired into `process_page()`'s OCR loop, right after whitespace normalization, before the text ever reaches `page_ocr_texts`/translation.
* **Fix 3 - `source_confidence` quality_flags axis (`translate_manga.py`):** a genuinely separate top-level field per bubble (`bubble["quality_flags"]["source_confidence"]`), never merged into the same sub-structure as `overflow_ratio`/`chosen_size`/`box_overlap` - a bubble can be flagged for bad OCR source AND bad render geometry independently and simultaneously.
  - **English/Tesseract path (the actual active path for frieren):** switched from `pytesseract.image_to_string` (no confidence exposed) to `image_to_data` (per-word confidence), stored as `tesseract_confidence` (0-100 average across recognized words).
  - **Fallback dictionary heuristic:** `real_word_fraction()` - checks the fraction of 2+-letter alphabetic tokens found in a system English word list. No dictionary was installed on the device (`/usr/share/dict/` was empty) - installed `wamerican` (~985KB word list) via `apt`, and added it to `deploy.sh`'s Ubuntu package list too, so a fresh install gets it automatically going forward.
  - **Japanese/MangaOcr path:** honestly left unavailable - MangaOcr's high-level API exposes no confidence score, and an English dictionary check against Japanese OCR output would be meaningless. Disclosed as a real limitation, not silently faked.
  - **Low-confidence threshold, set from REAL comparative data, not a guess:** flags "low" if Tesseract confidence < 68 OR dictionary real-word-fraction < 0.5 (either signal, since a false positive here just costs a human a glance while a false negative ships garbage silently). The threshold was tuned live: across 18 real frieren bubbles tested, legitimately good bubbles scored 71.0-96.0, one confirmed-garbage bubble ("Wie gy oe") scored 24.0/0.333 (correctly flagged even at the original threshold), and the reported-scrambled `p020_b12` scored 61.5/1.0 - sitting BETWEEN the good and bad clusters, initially unflagged at the original 60-point threshold. Raised to 68 (comfortable margin below the good cluster's floor of 71.0, comfortable margin above the scrambled bubble's 61.5) - **redeployed and re-verified live**: `_b12` now correctly flags `low: true`, and all 13 other real bubbles on that same page remain correctly unflagged (zero false positives introduced).
  - **Honest, disclosed limitation:** the dictionary check cannot catch `p020_b12`'s actual problem (word-order scrambling) on its own, since every individual word IS a real English word (`dictionary_real_word_fraction: 1.0` even on the scrambled text) - it's Tesseract's own confidence score, not the dictionary heuristic, that ends up catching this specific case. A pure vocabulary check is fundamentally the wrong tool for a word-*order* problem; flagged explicitly rather than claiming the dictionary heuristic solved something it didn't.
* **Real, live-tested confirmation on all 3 originally-reported bubbles, not synthetic:**
  - `c118 p019 _b00`: `"VIALA- THOR//"` → `"VIALA- THOR"` (noise stripped, hyphen preserved), translated cleanly to `"віала – тор"`, `tesseract_confidence: 71.0` (moderate - a proper noun, correctly not flagged).
  - `c119 p027 _b01`: `"DID I REALLY GO BACK IN |@ TIME? | \ \ \ |"` → `"DID I REALLY GO BACK IN |@ TIME?"` (trailing junk gone, the embedded mid-sentence `|@` correctly left alone). Same page's `_b03` (`"Wie gy oe"`, genuine garbage) correctly flagged `low: true` from the very first test, before any threshold tuning.
  - `c118 p020 _b12`: confirmed flagged `low: true` after the data-driven threshold adjustment, as required by the test spec.
* **Deployment:** all changes diffed against the live phone copies before overwriting (clean each time, no unrelated drift), deployed via `scp`, and every regen test above ran directly through the real `translate_manga.py --regenerate-page` pipeline on the actual device (not a simulation).
* **Type:** `direct` - investigation done first as instructed, three real fixes shipped, all confirmed via live regeneration on the actual originally-reported bubbles plus the p022 case folded in mid-task, with an honest disclosed limitation on the dictionary heuristic rather than an overclaimed fix.

## [ ] TASK-38: [PLANNED, DEFERRED] Migrate edit_store persistence (edits.json) to SQLite + WAL
* **Status:** planning only - **no code changes made**. Deferred deliberately: GitNexus impact analysis (below) shows this touches shared infrastructure used by nearly every write path in the application. Attempting it at the tail of an already-large session (TASK-32 through TASK-33, all real production changes already shipped and deployed this session) was judged not worth the risk - a half-finished migration on the live persistence layer could break the application in ways none of today's targeted fixes could. Scheduled for a dedicated future session with a clean context budget.

### Problem statement and motivation
`kbg_web/edit_store.py` backs every non-destructive edit (manga text/geometry/font-size, regular-book text, stress-marking, audio) with a single flat JSON file per book (`<book_dir>/edits/edits.json`), read via `_load_edits()` (full-file `json.load`) and written via `_save_edits()` (full-file `json.dump`, no atomic rename, no locking) on every `add_edit()`/`mark_status()` call. Two independent processes can legitimately touch this file concurrently on a real device: the Flask web UI (`kbg_web/app.py`, live human edits) and the background `translate_manga.py` process running inside `proot-distro` (`apply_pending_manga_edits()`, invoked between pages while `main()`'s full-book loop is still running - this is TASK-23's own live-editing feature, so this isn't a hypothetical, it's the system's actual designed concurrency model). A read-modify-write cycle with no locking between two writers is a textbook lost-update race: if the background process and a live UI edit both call `add_edit()`/`mark_status()` close enough in time, one write can silently clobber the other, without any error surfacing to either side.

The research report reviewed this session (`Мобільні AI-пайплайни для перекладу.txt`, section 3, "'Живе' редагування та конфлікти ресурсів") names this exact pattern - plain file locks in a Termux/Android I/O-scheduler environment - as unreliable, and recommends SQLite with `PRAGMA journal_mode=WAL` as the standard, proven pattern for a single background Writer + concurrent UI Reader/Writer on a resource-constrained mobile device, plus a trivial `priority` column for a live-edit-jumps-the-queue pattern the current architecture has no equivalent of at all (a live edit currently just waits for whatever `apply_pending_manga_edits()`'s next page-boundary check happens to be, per TASK-23's existing design - no priority signal exists to make it happen sooner).

### Architectural scope and affected components
**Must change:** `kbg_web/edit_store.py` itself (`_load_edits`, `_save_edits`, `add_edit`, `list_edits`, `get_edit`, `mark_status`, `_edits_path`) - the entire module's storage backend.

**Direct call sites (grepped precisely, not the wider GitNexus fan-out through the shared `resolve_book_paths` helper - see Dependency analysis below for why that number is misleading):**
- `translate_manga.py`: `apply_pending_manga_edits()` - 2 call sites (`list_edits`, `mark_status`).
- `run_conversion_batches.py`: `main()` - 2 call sites (`list_edits`, `mark_status`), text-edit mode for regular books.
- `kbg_web/app.py` - ~13 call sites across these route handlers: `edit_text` (regular-book text edits), `edit_stress` (stress-marking), `edit_queue` (GET list, used by every Pending-Edits UI fetch), `edit_regenerate_audio`, `edit_approve` (shared across all edit modes), `edit_discard` (shared across all edit modes), `edit_manga_text`, `edit_manga_bbox` (TASK-36), `edit_regenerate_manga_page`.

**Not required to change:** `common/book_paths.py`'s `resolve_book_paths` (GitNexus's impact query traced through this shared helper to inflate the affected-process count to 20 - those are mostly unrelated routes like `add_book_api`/`delete_book_api`/`upload_file_api` that call `resolve_book_paths` for entirely different reasons and never touch `edit_store` at all; the true dependency surface is the ~9 functions listed above).

### Dependency analysis
`edit_store.py`'s current public API (`add_edit`, `list_edits`, `get_edit`, `mark_status`) is small and already well-factored as a module boundary - **the migration should be designed as a drop-in replacement preserving these exact function signatures**, so none of the ~9 caller functions above need to change at all, only `edit_store.py`'s internals. This is the single most important design constraint for keeping this migration's actual blast radius small despite the large number of indirect callers - if the public API doesn't change shape, the 20-process GitNexus number becomes irrelevant (those callers depend on `edit_store`'s behavior, not its storage format).

### Technical risks and mitigations
1. **Data migration correctness (irreversible if botched)** - existing `edits.json` files for real, in-production books (`frieren`, `vibe-programming`, `three-days-of-happiness`, etc.) must convert to the new schema with zero data loss. *Mitigation*: write a migration script, keep the original `edits.json` as a `.bak` untouched, verify row-count and spot-check content equality before ever deleting/renaming the original.
2. **Concurrency semantics not actually validated in THIS environment** - the research report endorses SQLite+WAL in general, but this project runs inside `proot-distro` (a ptrace-based syscall interception layer, not a true container), and SQLite's locking primitives ultimately rely on `fcntl`/POSIX advisory locks, which is exactly the class of primitive the report separately warns is unreliable under Android's I/O scheduler. **Do not assume the general recommendation transfers correctly to this specific runtime without direct verification.** *Mitigation*: as the very first implementation step, before any schema/design work, run an isolated experiment - two separate Python processes inside the same `proot-distro` container, one looping writes with `WAL` mode enabled, one reading concurrently, verified for corruption/lost-write over a sustained run - before committing to this approach at all.
3. **Multiple-writer assumption may not hold** - WAL mode cleanly supports one writer + many readers; verify the actual concurrency pattern is genuinely single-writer-at-a-time in practice (background pipeline OR live UI edit, not both writing simultaneously in normal operation) rather than assuming it.
4. **Schema design lock-in** - once real data lives in SQLite, changing the schema is a migration itself. *Mitigation*: design the schema to mirror the current JSON record shape closely (`id, slug, mode, target_id, field, original_value, edited_value, status, created_at, applied_at`) rather than over-normalizing, keeping the mapping from old to new format trivial to reason about and re-verify later if needed.

### Recommended implementation order
1. Empirical validation of SQLite+WAL concurrent read/write inside the actual `proot-distro` Ubuntu environment (isolated experiment, zero production impact).
2. Design the schema + rewrite `edit_store.py`'s internals with the *same* public function signatures - a true drop-in replacement.
3. Write and test a migration script (JSON → SQLite) against a **copy** of real book data, not live production data.
4. Migrate one low-stakes/test book first, verify end-to-end through the actual UI (create edit, list, approve/discard, regenerate).
5. Migrate all real books only once step 4 confirms correctness, retaining `.bak` originals.
6. Observe a real background-generation-plus-live-edit session (TASK-23's own scenario) for any residual race-condition symptoms.

### Milestones and acceptance criteria
- **M1:** Concurrent WAL read/write proven corruption-free inside the real `proot-distro` container (empirical, not assumed).
- **M2:** New `edit_store.py` passes identical behavior against the old one for every existing function signature, using a fresh SQLite DB (no callers changed).
- **M3:** Migration script converts a real `edits.json` with 100% row-count/content match, verified against the `.bak`.
- **M4:** A live end-to-end test (create edit via UI → regenerate → status transitions) behaves identically to the current JSON-backed system.
- **M5:** No regression across every edit mode sharing this module (manga text/bbox, regular-book text, stress, audio) - this is shared infrastructure, so all modes need re-verification, not just manga.

## [ ] TASK-39: [PLANNED, DEFERRED] Granular stage invalidation for process_page (avoid full OCR+LLM re-run on geometry/font-only edits)
* **Status:** planning only - **no code changes made**. Deferred deliberately: this is a larger, higher-design-risk undertaking than TASK-38 (see below - it requires changing the persisted artifact schema most of the rest of the system depends on, not just an internal storage swap), and depends on open architectural questions (cross-bubble padding coupling, see Risk 2) that need dedicated thinking time before implementation should start at all.

### Problem statement and motivation
`process_page()` re-runs the entire Stage A-E pipeline (detect → dedupe → OCR → translate → inpaint → typeset → post-check) from scratch on every single regeneration, even when a human edit only changes a bubble's box position or font size and touches neither the source image nor the translated text. This is the root architectural cause behind the bug fixed this session as "TASK-36 follow-up" (a `status="regenerated"` text edit not surviving a *later*, unrelated geometry-only regeneration of the same page) - that fix (widening the override set to reapply already-"regenerated" edits on every future regen) works and is deployed, but it's a symptom-level patch: the page's OCR and LLM translation calls still re-run in full every time, which is wasteful of the two most expensive resources on this device (network/LLM latency and CPU-bound OCR) for a purely cosmetic change. TASK-36 also made this dramatically more likely to matter in practice, since geometry editing on a single bubble now regenerates the whole page routinely, not just on rare full re-translations.

### Architectural scope and affected components
**Core function to restructure:** `translate_manga.py:process_page()` - 13 direct dependents per GitNexus (`wrap_text`, `fit_text`, `draw_text_centered`, `real_word_fraction`, `clean_ocr_edge_noise`, `dedupe_blocks`, `robust_inpaint`, `_get_bubble_core_box`, `_apply_padding_neighbor_safe`, `post_render_check`, `compute_box_overlap_flags`, `assign_bubble_ids`, `translate_batch_llm`), 22 total impacted symbols.

**All three of `process_page`'s own callers need updating**, not just the function itself: `regenerate_single_page()` (CLI/subprocess path), `apply_pending_manga_edits()` (in-process live-regen path, TASK-23), and `main()`'s own full-book per-page loop.

**This is fundamentally an artifact-schema change, not just a code refactor** - "granular invalidation" only works if each expensive stage's output (detected boxes, raw OCR text, translations) is itself persisted somewhere a *later, separate* invocation can read back, rather than living only in the Python call stack of one `process_page()` call as it does today. Currently only the *final* `bubbles_meta.json` is persisted, which conflates every stage's output into one record with no way to tell "this text came from OCR run X" apart from "this box came from detection run Y." This makes the scope meaningfully larger than TASK-38: it isn't just swapping a backend behind a stable API, it's introducing a new intermediate artifact that other things (the frontend overlay, `backfill_bubbles_meta`, quality-flag computation, CBZ export) don't currently know about and must not be broken by.

### Dependency analysis
Recommended design constraint to keep this contained: add a **new, additive, page-scoped cache file** (e.g., `<book_dir>/pipeline_cache/<page_stem>.json`, holding detected boxes + raw OCR text + translations) that sits *alongside* `bubbles_meta.json` rather than replacing or restructuring it - this means the existing schema every other component already depends on (frontend overlay rendering, TASK-25's `backfill_bubbles_meta`, TASK-36's `compute_box_overlap_flags`, CBZ packaging) needs zero changes. Only `process_page()`'s own internals and its three callers need to know the cache exists.

### Technical risks and mitigations
1. **Cross-bubble coupling makes "stage" boundaries less clean than they sound.** TASK-36's neighbor-aware padding (`_apply_padding_neighbor_safe`) means every bubble's box on a page depends on every *other* bubble's box on the same page - "only re-run typesetting for the edited bubble" isn't actually correct in isolation; the padding/box-overlap computation is inherently page-wide, not per-bubble. *Mitigation*: the fast path must still recompute Stage C (box padding) and the overlap/quality-flag pass for the **whole page**, using cached OCR+translation - it's specifically Stage OCR and Stage Translate (the genuinely expensive, per-bubble-content-dependent stages) that get skipped, not the page-wide geometry pass.
2. **Determining safe invalidation triggers.** The cache must be treated as stale (fall back to a full run) whenever: a *text* edit exists for the page (translation changed, cache's translation is wrong), the source image changed, or the cache is simply absent/corrupt/version-mismatched. Conservative-by-default: when in doubt, do a full run rather than trust a possibly-stale cache.
3. **Detection instability across runs.** `dedupe_blocks`'s bubble count/order isn't guaranteed identical between two independent detection runs (this is *why* `match_bubbles_iou` reconciliation exists at all today) - if detection is cached and skipped, that's actually a *feature* here (avoids the reconciliation problem entirely for the fast path), but only as long as the fast path is never taken when the source image genuinely changed.
4. **Bigger payoff, bigger risk, than TASK-38** - the actual benefit (compute/battery savings) should be measured empirically before deciding this is worth the added persistent-format complexity; if TASK-38's SQLite migration alone, or the already-deployed TASK-36-follow-up band-aid, prove sufficient in practice, this task may not be worth the added complexity at all. Recommend re-evaluating necessity after TASK-38 ships and has been observed in real use for a while.

### Recommended implementation order
1. Design the pipeline-cache file format (additive, zero changes to `bubbles_meta.json`'s existing schema) - review and sign off on the design before writing code.
2. Implement cache *writing* only first (persist Stage A/OCR/Translate outputs on every existing regen call, consumed by nothing yet) - a safe, purely observable first step with no behavior change.
3. Implement the "read cache, skip straight to page-wide box/typeset/flags" fast path, gated strictly to geometry/font-only edits, with conservative fallback to full regen for anything else.
4. Test with the same real-device, visual-before/after rigor this session established for TASK-33/36: confirm the fast path produces byte-identical OCR/translation output to what a full regen would have produced, on multiple real multi-bubble pages (not just the single-bubble pages this session happened to test with, given Risk 1's cross-bubble coupling).
5. Only after the fast path is proven reliable, measure real compute/battery savings on-device before deciding whether to keep, extend, or remove the TASK-36-follow-up band-aid (defense-in-depth is fine either way, but the measurement should inform the call).

### Milestones and acceptance criteria
- **M1:** Pipeline-cache format designed and reviewed (not yet consumed by anything).
- **M2:** Cache correctly written on every existing regen call, verified content-correct, zero behavior change to current output.
- **M3:** Fast path implemented for geometry/font-only edits; produces byte-identical OCR/translated text to a full regen on multiple real multi-bubble pages.
- **M4:** Real on-device timing/battery comparison (fast path vs. full regen) demonstrates the savings justify the added complexity - if not, document that finding and reconsider whether to proceed further.
- **M5:** No regression in TASK-36's neighbor-aware padding/overlap-detection logic when the fast path runs on a page with multiple bubbles, only one of which was actually edited.

## [x] TASK-40: [BUG][FIXED] Timed-out single-page regen left the actual worker process orphaned, only killed its proot wrapper
* **Problem, flagged as a loose end during TASK-36 testing, closed out separately** (isolated, low-risk, doesn't touch `edit_store`/`process_page` - explicitly kept apart from the deferred TASK-38/39 refactors): `kbg_web/app.py`'s `edit_regenerate_manga_page()` ran the regen subprocess via `subprocess.run(cmd, timeout=180)`. When the 180s timeout fired, Python's default `TimeoutExpired` handling kills only the *direct* child - the `proot-distro` wrapper process (`cmd[0]`) - not any of its own descendants. Since `cmd` is `proot ... /bin/bash -c "python3 translate_manga.py ..."`, killing just the wrapper left the actual `python3` worker running *inside* the PRoot container as an orphan, unmonitored, for several more minutes past the point Flask (and the client) had already given up and returned an error. Observed live twice during TASK-36 testing; the orphan happened to complete correctly on its own both times, but that was luck, not a guarantee - a genuinely hung run would have run forever, silently burning battery/CPU with nothing watching it.
* **Fix:** replaced the single `subprocess.run(...)` call with `subprocess.Popen(cmd, ..., start_new_session=True)` (puts the whole `proot → bash → python3` chain in its own process group) + explicit `proc.communicate(timeout=180)`. On `TimeoutExpired`, `os.killpg(os.getpgid(proc.pid), signal.SIGKILL)` kills the **entire** process group in one call, not just the wrapper, then re-`communicate()`s once more to reap the killed process and avoid a zombie. Downstream code (which reads `res.returncode`/`res.stdout`/`res.stderr`) needed zero changes - the new code constructs a `subprocess.CompletedProcess` from the `Popen` results, keeping the diff scoped to just the process-spawning block.
* **Verification:** (1) local mechanism proof - a synthetic nested `bash -c "sleep 100 & wait"` process tree, run once the old way (`.kill()` on the direct child only) and once the new way (`start_new_session=True` + `os.killpg`): the old approach measurably left the grandchild `sleep` process running after the "timeout", the new approach's `killpg` left nothing behind. (2) Real-device happy-path confirmation: deployed to production, triggered a real regen through the actual `/api/edit/regenerate-manga-page` Flask route (not a CLI bypass) with a font-size+geometry edit, confirmed the edit correctly reached `status="regenerated"` and the resulting typeset PNG visually shows the requested larger font - proves the refactor didn't break the normal (non-timeout) success path. The true timeout path wasn't forced live against production code (would require an artificial multi-minute hang injected into a live pipeline run, judged not worth the risk for this fix) - covered instead by the local mechanism proof, which isolates and directly demonstrates the actual kill behavior this fix changes.
* **Unrelated observation made during this verification, not investigated further:** the device showed increasingly tight memory (343MB free of 15GB, ~3.7GB swap in use) during this session's later regen tests, plausibly explaining some of the longer-than-expected regen times observed throughout TASK-33/36 testing. Independent of this fix; noted for awareness, not chased.
* **Type:** `direct` - isolated, self-contained, low architectural risk, explicitly separate from the deferred TASK-38/39 work, fixed and verified both locally (mechanism) and live (happy path, real device).

## [x] TASK-41: Auto-resume the last interrupted conversion on Termux/service restart
* **Request:** the device's autostart currently only starts the LLM translation server and the Flask web interface. If a book conversion was actively running when Termux itself crashes/restarts (a real, repeatedly-observed pattern this session - screen-lock kills, memory-pressure crashes), nothing brings it back automatically; a human has to notice and manually re-trigger it.
* **Correction made during investigation, not silently ignored:** the request also asked to add SSH remote-access autostart, on the assumption it wasn't already there - checked the live `.bashrc` directly and found step 1 already starts `sshd` unconditionally on every boot. Reported this rather than adding a redundant duplicate.
* **Design:** the actual per-page resumability already exists at the pipeline level (`translate_manga.py`'s own skip-if-already-done logic per page, relied on and tested repeatedly throughout this session) - so "resume" doesn't need new granular progress tracking, only a durable record of *which command was running* so it can be re-launched identically after a full environment restart.
  - `kbg_web/app.py`: new `.active_conversion.json` state file (`_write_active_conversion_state`/`_clear_active_conversion_state`). Written in `run_conversion_api` right after the subprocess starts (captures the exact `cmd`/`cwd`/`log_path`). Cleared in `handle_process_completion` (fires once Flask observes ANY exit, success or failure - deliberately not just success, so a genuinely-failing pipeline doesn't get auto-retried forever on every restart; only a crash severe enough to take Flask itself down before it ever observes completion leaves the file behind, which is exactly the correct "was interrupted" signal) and in `stop_conversion_api` (an explicit user stop should not trigger auto-resume either).
  - `bin/resume_active_conversion.py`: small standalone script, deliberately not inline shell in `.bashrc` - the saved `cmd` is a real argv list that can contain filenames with spaces/apostrophes (the exact class of bug TASK-34 fixed elsewhere in this project for a different reason), and reconstructing/shell-quoting that correctly from bash is exactly the kind of thing that goes wrong; Python's `subprocess` module handles an argv list natively. No-ops silently if no state file exists (normal boot). Re-launches with `start_new_session=True`, matching TASK-40's same reasoning for process independence.
  - `.bashrc`: new step 4, after the existing 3 autostart steps - checks for the state file and launches the resume script via `nohup` if present.
* **Verification:** deployed to the live device, restarted Flask to pick up the `app.py` changes, then used the state-write/state-clear mechanism as part of this session's own final full-book retranslation (see below) - triggered a real conversion through the actual `/api/run/<slug>` route and confirmed the state file was written with the exact correct `cmd`/`cwd`/`log_path`, matching what the process is genuinely running. The full "survive an actual Termux restart" loop was not forced live (would require deliberately crashing the production device mid-run, judged not worth the risk to test directly) - covered instead by direct code-path review of the write/clear/resume logic, syntax-verified.
* **Type:** `direct` - new small feature, correctly caught and disclosed a factual error in the original request (SSH already present) rather than adding a redundant duplicate, deployed and its write-path verified live as a natural side effect of this session's closing full-book retranslation.

## [x] TASK-42: Full from-scratch retranslation of frieren with all TASK-32–41 changes
* **Request:** re-translate `frieren` completely from scratch (not resuming/skipping already-done pages) so every one of this session's fixes - neighbor-aware padding, `dedupe_blocks` containment-ratio merging, OCR edge-noise cleanup, `source_confidence` flagging, the process-group timeout fix, the new manual geometry/font editing capability going forward - applies uniformly across the whole 194-page book, not just the handful of pages manually spot-tested this session.
* **Pre-flight verification (per explicit request):** diffed all 4 core files (`translate_manga.py`, `kbg_web/app.py`, `kbg_web/templates/stages.html`, `kbg_web/edit_store.py`) between the local repo and the live device - confirmed byte-identical, so the device is genuinely running everything shipped this session before starting.
* **Backup before the destructive wipe**, matching this session's own established precedent: `cp -a` the entire `books/frieren` directory to `books/frieren_full_backup_pretask_final_retranslate` (2.7GB), verified via file-count match (866/866) before touching the original.
* **Wipe:** removed `translated/`, `cleaned/`, `bubbles_meta/`, `output/`, `edits/`, `preview_cache/`, `quality_flags.json`, `manga_progress.json` for `frieren`. Explicitly preserved `source/` (194 original page images, untouched, verified by count) - this is the actual input, never touched.
* **Launch:** triggered via the real `POST /api/run/frieren` Flask route (not a CLI bypass), specifically so it exercises TASK-41's new auto-resume state-tracking for real - confirmed the `.active_conversion.json` state file was written with the exact correct command, and the process is genuinely running (`ps` confirmed the real `proot`/`python3` process tree, not a stale artifact).
* **Health-checked, not just launched-and-forgotten:** watched the conversion log until it confirmed real page-by-page progress ("Processing 194 pages... Page 1/194... Page 2/194... Page 3/194...") - not just that a process started, but that it's genuinely working through the book.
* **Status at session end:** in progress, expected to take multiple hours (matching this session's earlier full-regeneration experience for the same book), now protected by TASK-41's auto-resume if Termux restarts mid-run. Follow-up needed next session: confirm the run completed successfully, spot-check overall quality flag counts (`box_overlap`/`source_confidence`/overflow) across the freshly-generated book as a real before/after comparison against this session's pre-fix baseline, and restore/discard the pre-retranslate backup once satisfied with the result.
* **Follow-up completion (next session, verified live on device, not taken on faith):** `books/frieren/manga_progress.json` confirmed `current_page: 194, total_pages: 194` - full run genuinely completed, not stalled. `books/` listing shows only the one real `frieren` entry (no phantom card). Real before/after quality-flag comparison, read from the pre-retranslate backup's `quality_flags.json` vs. the fresh run's:

  | reason | before (baseline) | after (this retranslation) |
  |---|---|---|
  | box_overlap | 163 | 6 |
  | min_size_floor | 18 | 12 |
  | low_source_confidence | 3 | 33 |
  | **total** | **184** | **51** |

  `box_overlap` dropped ~27x, consistent with TASK-27/28/30/33's geometry/dedupe fixes actually taking effect across the whole book, not just the spot-tested pages. `low_source_confidence` rose 3→33 - not flagged as a regression: TASK-33 is what introduced/expanded the `source_confidence` axis in the first place, so a higher count here plausibly means the check is now applied more thoroughly, not that more pages got worse. Not independently confirmed either way; worth a manual look if the flagged pages ever come up.
* **Backup disposition:** user confirmed the full run's result as correct; `backups/frieren_full_backup_pretask_final_retranslate` (2.7GB) deleted from the device on that basis.
* **Own mistake found and fixed, live, immediately after the initial report:** the backup created above (`books/frieren_full_backup_pretask_final_retranslate`) was placed *inside* `books/`, and the dashboard's book-listing logic (`/api/api/books`, `kbg_web/status_helper.py`) treats every subdirectory under `books/` containing a `config.json` as a real, separate book - the backup (a full `cp -a`, config.json included) was therefore showing up as its own phantom "Frieren" card, complete with `Idle`/`100%`/`Run Conversion` controls, alongside the real, actively-running `frieren` entry correctly showing live progress. User reported this as "100% - is it a bug?", initially suspected a caching issue; the actual live dashboard screenshot they pasted showed the real answer immediately - two distinct book cards, not one stale one. Root-caused instead of guessing (checked `manga_progress.json` on disk, `/api/status/frieren`, and `/api/books` directly - all three consistently showed the REAL ~19-25% progress the whole time, and `/api/books`'s `cache-control: no-store` header ruled out an HTTP/CDN caching explanation before the real cause was found). Fixed by moving the backup to a new `backups/` directory *outside* `books/` (verified 866/866 files preserved after the move) - confirmed via a fresh `/api/books` call that only the 3 real books remain listed, and `frieren`'s own entry continues to show genuine live progress (22.2% → 24.7% between checks).
* **Type:** `direct` - real production operation on live user data, backed up first, launched and health-verified through the actual application (not a workaround), completion confirmed live the following session against the real device state (page count, quality-flag comparison, book listing), not taken on the user's report alone. A self-inflicted backup-placement mistake was found and corrected live in the same session, root-caused with real data rather than assumed to be a caching issue.


## [x] TASK-43: Shared autostart script (bin/start-all-services.sh) — finally committed, was silently missing from the repo
* **Problem:** a previous session refactored deploy.sh STEP 5 to delegate all four autostart steps (sshd, llama-server, Flask, TASK-41 auto-resume) to `bin/start-all-services.sh`, and its comments claimed the script is "part of this repo - a fresh clone already has it via STEP 3". That was false: the script was never `git add`ed (untracked on the OrangePi working tree), and the deploy.sh half of the refactor was never committed either. A fresh-device install would have configured `.bashrc`/Termux:Boot to call a script that does not exist — autostart (including auto-resume after a Termux crash, explicitly requested by the user as a deploy-script requirement) silently broken on every new install.
* **Also fixed in the same commit:** deploy.sh STEP 2's ubuntu-already-installed check was `proot-distro list | grep -q "Installed: yes" | grep -q "ubuntu"` — piping into a grep after `grep -q` (which prints nothing) means the second grep ALWAYS exits 1, so the script ALWAYS attempted `proot-distro install ubuntu`, which `set -e`-kills the run on any device where ubuntu is already installed. Verified the failure mode locally (`echo "ubuntu: Installed: yes" | grep -q "Installed: yes" | grep -q "ubuntu"` → exit 1). Replaced with the canonical `$PREFIX/var/lib/proot-distro/installed-rootfs/ubuntu` directory check.
* **Verification:** `bash -n` both files; commit `6695243` pushed. The re-run of deploy.sh on the real device is tracked under TASK-32's staged hardware test.
* **Type:** `direct` — found during the TASK-32 pre-run static audit (staged approach approved by user).

## [x] TASK-44: Repair git-based deploy on the phone + recover live-only fixes stranded on the device
* **Problem:** both of the phone's git remotes were broken (SSH publickey denied), HEAD stuck at `cec9c11` (weeks behind), deploys happened only via manual scp, and genuinely live-only code changes on the device could never be pushed anywhere.
* **Solution:**
  1. Repo is public → switched the phone's `origin` to plain HTTPS (`https://github.com/maxfraieho/kindle-butch-gen.git`) — anonymous `git pull` now works with no keys at all, from any network.
  2. Untracked `books/` entirely (commit `ee48c18`, `git rm -r --cached` + .gitignore): live, device-mutated book state was tracked, so any future commit touching/deleting those files would have made `git pull` on the phone conflict with — or delete — live user data. Files stayed on disk everywhere; the OrangePi's pile of uncommitted `books/` deletions also became moot.
  3. One-time realignment on the phone via `git fetch` + `git reset --mixed origin/master` (moves HEAD+index only, working tree untouched) instead of any checkout/pull that could clobber files.
  4. **Before restoring anything, diffed every locally-modified tracked file on the device.** Two were REAL live-only fixes that existed nowhere else: `common/text_protect.py` (markdown-table placeholder handling: strip only separator rows + bare `|` chars instead of deleting whole table lines) and `monitor_book.sh` (adds `edit_percent` to progress reporting). Recovered both into master (commit `53e5d81`) BEFORE the final `git checkout -- .`. The other two diffs were just older versions of master files.
  5. Set upstream tracking (`git branch --set-upstream-to=origin/master master`) after discovering plain `git pull` failed without it — caught because the first live test of the new deploy path ran the OLD script version and failed loudly.
* **Verification (live on device):** phone HEAD = master, `git status` clean except deliberate untracked leftovers, `git pull --ff-only` works end-to-end (used for real to deploy TASK-45's fix), Flask still up (401 auth guard) after the whole operation.
* **Type:** `direct` — infrastructure correctness; unblocks TASK-46 (user-facing update button) and makes TASK-32's staged hardware test meaningful.

## [x] TASK-45: [BUG][FIXED] Auto-resumed conversion never cleared .active_conversion.json — every later Termux restart relaunched the pipeline
* **Problem (found live, not hypothetical):** after the completed TASK-42 frieren run, a stale `.active_conversion.json` still sat on the device with no conversion process running. Root cause: `bin/resume_active_conversion.py` deliberately kept the state file so a re-interrupted resume could resume again — but NOTHING ever cleared it when a resumed run *completed*, because Flask's `handle_process_completion` only fires for processes Flask itself started. Consequences: every subsequent Termux restart relaunched the pipeline against a finished book (wake-lock, battery, CBZ rebuild), and a genuinely FAILING resumed run would be retried forever on every boot — the exact infinite-retry hazard app.py's TASK-41 design deliberately avoided on its own path.
* **Fix (commit `e8831ef`):** the resume script now stays alive as a watcher: `proc.wait()`, then clears the state file on ANY exit (success or failure), mirroring `handle_process_completion` semantics. If the environment dies mid-run the watcher dies with it and the file correctly survives for the next boot. Guarded against deleting a NEWER state file Flask wrote for a different run in the meantime (compares saved `cmd`).
* **Verification (live on device, real code path):** deployed via the new TASK-44 `git pull` path; synthetic state file (`sh -c "sleep 2; echo resumed-run-finished"`) → ran `bin/resume_active_conversion.py` → it launched the run, waited, reported `exited (code 0); cleared ...`, state file confirmed gone, log contains the marker line. Stale frieren state file deleted manually after confirming no conversion process was actually running.
* **Type:** `direct` — correctness fix protecting completed user work, found by inspecting real device state during TASK-44 rather than by report.

## [x] TASK-46: One-button self-update from the dashboard
* **Request (user, mid-session):** a non-specialist must be able to install the latest service updates with a single button - no shell, no git knowledge. Depends directly on TASK-44's repaired git-pull deploy path.
* **Implemented (commit `fd22257`, pushed):**
  1. `POST /api/update` (kbg_web/app.py, auth-protected by the global before_request guard): refuses with 409 while any conversion is running (killing Flask mid-run would orphan the conversion from the dashboard and the TASK-41/45 state lifecycle); `git fetch` + `rev-list --count HEAD..origin/master`; 0 behind → `up_to_date` + current version, otherwise spawns the updater DETACHED (start_new_session - it must kill and restart this very Flask process) and returns `updating`.
  2. `bin/self-update.sh`: `git pull --ff-only` (aborts BEFORE touching the running service on any local divergence - old version keeps serving), 2s grace so the triggering HTTP response flushes, restart Flask via the shared `bin/start-all-services.sh`, everything appended to `~/kbg-update.log`.
  3. Dashboard: 🔄 Update button + JS that shows progress, and on `updating` polls `/api/settings` every 3s until the restarted server answers, then reloads the page.
* **Pre-existing bug found and fixed during this feature's audit:** `bin/start-all-services.sh`'s auto-resume step fires on EVERY new Termux shell session and checked only for the state file's existence - opening a shell during a genuinely-active conversion would have launched a SECOND copy of the same pipeline racing the first over the same output files. Now pgrep-guarded (translate_manga.py / run_conversion_batches.py / translate_epub.py).
* **Verification state, honestly:** all four files `bash -n`/`py_compile` clean; commit pushed. Live end-to-end test on the device was IN PROGRESS when Termux itself was killed by Android (ssh: connection refused; device still answers ping; public UI 502 = Flask died with Termux). The phone has NOT yet pulled `fd22257`. Remaining live checklist for when Termux is reopened: (1) `git pull` gets fd22257; (2) restart Flask; (3) `POST /api/update` with no pending commits → `up_to_date`; (4) push a trivial commit → button/API returns `updating`, Flask restarts itself, phone HEAD advances, dashboard reloads; (5) `~/kbg-update.log` contains the run.
* **Live verification progress (after user reopened Termux):** items (1)-(3) PASS on the real device - phone pulled to `521c7a2`, Flask restarted on the new code (200 on /api/settings), and `POST /api/update` with nothing pending returned `{"status":"up_to_date","version":"521c7a2 ..."}`. Incidental lesson recorded: restarting Flask over SSH must use a self-escaping pkill pattern (`pkill -f "[k]bg_web/app.py"`) - the plain pattern matched the SSH command line itself and killed the session mid-restart. This very commit is the payload for item (4)'s full end-to-end update test.
* **Type:** `direct` - feature complete in code, live verification explicitly pending, not claimed done.
* **Final live verification (item 4-5, full end-to-end):** POST /api/update on the device returned `{"behind":1,"status":"updating"}`; the detached updater pulled `521c7a2..89e7fa7` (ff), killed and restarted Flask via start-all-services.sh, phone HEAD advanced to `89e7fa7`, Flask answered 200 afterward, and `~/kbg-update.log` contains the complete run. The dashboard button's JS path (same endpoint + restart-polling) was not separately browser-clicked - the API contract it consumes is what was proven.

## TASK-32 staged hardware test — session log (2026-07-17, in progress)
* **Found and fixed BEFORE execution (pre-run audit + precondition checks):**
  1. Always-false ubuntu-installed grep pipeline (see TASK-43 entry) — first fix used the older proot-distro `installed-rootfs/` layout and STILL died on the real device ("container 'ubuntu' already exists"); the production phone's proot-distro keeps containers in `containers/`. Second fix (`b753542`) checks both layouts + a functional `proot-distro login ubuntu -- /bin/true` probe, and deliberately does not parse `proot-distro list` (old format false-positives on "Alias: ubuntu" for a fresh device). Lesson recorded: the first fix was written from documentation instead of checking the device — reality disagreed.
  2. Destructive model pre-check (`ea9dbfc`): working production GGUF is 4624648896 bytes, script expected 4624650016 (the CURRENT HuggingFace file — verified via HF API) and would have `rm -f`'d the working 4.4GB model BEFORE any consent, then skipped the redownload in a non-interactive run. Now keep-by-default with explicit consent required to delete.
* **Run 1:** STEP 1 completed harmlessly on the live device (python stayed 3.14.6, all pip deps already satisfied, Flask survived) → died at STEP 2 on the layout issue above.
* **Run 2:** relaunched with `b753542` via nohup/SSH → **Termux itself was killed by Android mid-run** (device pings at 4ms, sshd and Flask both gone). Second Termux death in ~1h, both under load — consistent with OnePlus/Android phantom-process-killer behavior toward BACKGROUND apps with heavy child process trees (proot/apt/pip is the exact profile; termux-wake-lock does not protect against it, it only prevents CPU sleep).
* **Hardware-test conclusion so far (a real finding, not a failure of the test):** deploy.sh cannot reliably run with Termux backgrounded via SSH on this device; a full run needs Termux FOREGROUND with screen on (or the Termux battery-optimization exemption / phantom-killer disable via adb). Next attempt: foreground run, user present.

## TASK-32 — hardware test COMPLETED (2026-07-17, run 4)
* **Run 4 (after the three fixes below) completed end-to-end on the production OnePlus 13**: every idempotent path verified live — pkg/pip all "already satisfied" (python stayed 3.14.6), ubuntu-installed check correctly skipped the install, llama.cpp container build correctly skipped (and cleaned 817MB of leftovers from run 3's killed build inside the container), both models detected and kept, autostart block appended to .bashrc, Termux:Boot script written. Services healthy after: Flask 200 (new TASK-7 password), llama-server up. Old inline .bashrc autostart block then removed (pattern-matched, sanity-asserted, backup at ~/.bashrc.pre-task32-backup); fresh shell sources cleanly with only the shared-script block remaining.
* **What this test does NOT cover, stated plainly:** the true from-scratch path (fresh device: proot-distro install, container apt/pip, model downloads, llama provisioning) was not executed — impossible to do honestly on the production device, and the llama compile is exactly the thermal burst that got Termux SIGKILLed twice (user constraint: no heavy sustained load without cooling). Fresh-device llama provisioning is also architecturally wrong today — see TASK-47.
* **Runs 1-3 each paid for themselves:** run 1 died on the always-false ubuntu check (fix v1 from documentation ALSO failed on the real device — its proot-distro keeps containers in containers/, not installed-rootfs/; fix v2 `b753542` checks both + functional probe). Pre-run precondition checks saved the 4.4GB production model from a destructive size-mismatch check (`ea9dbfc`). Run 3 exposed the llama architecture mismatch (`0005b30`) and the phantom-killer/thermal behavior (user confirmed seeing kill -9), now recorded as a standing device constraint.

## [ ] TASK-47: [PLANNED] Fresh-device llama.cpp provisioning path in deploy.sh is architecturally wrong
* **Finding (TASK-32 run 3):** production runs llama-server TERMUX-SIDE (`~/llama.cpp/build/bin/llama-server`, launched by `~/start-translation-server.sh` with Android GPU libs via LD_LIBRARY_PATH, `-ngl 99` Adreno offload) — but deploy.sh STEP 4 builds llama.cpp INSIDE the Ubuntu PRoot container and installs to the container's /usr/local/bin, which nothing in production ever executes. On a fresh device the script would compile the wrong llama and leave the autostart's translation server broken anyway.
* **Needed:** rework STEP 4 to build (or download a prebuilt release of) llama.cpp Termux-side where start-translation-server.sh expects it, with the Adreno OpenCL flags used for the current working binary; keep/remove the container build accordingly; also provision `~/start-translation-server.sh` itself (currently not in the repo — same class of gap as TASK-43's start-all-services.sh). Requires a cooled test window or a spare device for the full-compile verification (thermal constraint, see TASK-32 notes).

## TASK-44 addendum: full two-way git sync phone↔dev↔GitHub (P1.2 completed, 2026-07-17)
* **Step 1 (diff before any commit, as explicitly required):** `audio_stage.py` and `bin/tts_helper.py` on the phone are byte-identical to master HEAD (empty `git diff HEAD` on the device) - their earlier "M" status was the phone LAGGING the repo, not uncommitted local work. The genuinely-local changes had already been recovered in `53e5d81` (text_protect.py, monitor_book.sh). Correct action confirmed: alignment, not commit.
* **Step 2 (real commit from the phone):** found `TASK14_VERIFICATION.md` stranded untracked on the device since 2026-07-14 (the on-phone session that wrote it had no working push remote). Committed FROM the phone (author "KBG Phone (OnePlus 13)") as `6b9cbdc`.
* **Step 3:** ed25519 key generated on the phone, authorized on the dev server (192.168.3.184) - `BatchMode ssh` returns KEY-AUTH-OK.
* **Step 4:** `server-projects` remote re-pointed to `ssh://vokov@192.168.3.184/...`; fetch/pull work with no publickey denial. Found and fixed en route: `bin/*.sh` were committed non-executable, deploy's chmod left a permanent mode-diff on every device, and that mode-diff silently ABORTED the phone's `git pull` (caught because `git log` showed the merge never landed despite an "Updating..." line - trust the log, not the output). Exec bits committed (`acf7ccf`).
* **Step 5 (two-way, proven with a real payload):** phone pushed `6b9cbdc` to the dev server (`receive.denyCurrentBranch updateInstead` set there - working tree auto-updated, file present), dev server forwarded to GitHub, OrangePi pulled - all three nodes show identical HEAD `6b9cbdc`, phone tracked tree clean.

## [ ] TASK-48: Support system Phase 1 — Track A/B banner insertion (CODE DONE + module-tested; full-pipeline check rides the next real book build)
* **Spec:** docs/plans/support-system-plan.md (Q, 2026-07-17). Two strictly separate channels: Track A = official fund's GENERAL donate page (savelife.in.ua/donate/, "офіційний фонд, публічна звітність", developer never touches that money), Track B = developer support, labeled as exactly that, hidden until `track_b_url` is filled in. Phase 2/3 (Telegram bot, profiles, referrals, one-pager) target Appwrite per Q's direction — separate system, NOT phone code.
* **Implemented (`5187984`):** `common/support_banner.py` (config loader with safe no-op defaults, one-step `no_support_banner` opt-out via global_settings.json, seeded 50-70-page SupportInserter — reproducible across TASK-41/45 resumes, conservative heavy-scene keyword heuristic uk+en, HTML block renderer for EPUB, PIL interstitial renderer for manga) + `support_config.json` + wiring: EPUB banners between batches at merge time (run_conversion_batches.py), manga interstitial PNGs named to sort right after a closing chapter's last page, dropped into temp_out before CBZ packaging (translate_manga.py, catch-all guarded — the banner can never break a finished book).
* **Verified so far:** local module test PASS (insertion points 60/130/200/270/330 over 400 synthetic pages, intervals within 50-70(+skip), seeding reproducible, tone heuristic hits/misses correctly, Track B absent while URL empty); all three touched files compile; deployed to the device. **Found live:** the production container's Pillow lacks FreeType (`PIL._imagingft` missing) — truetype() raises with the font file present; fixed with a load_default(size) fallback (`9308083`). Interstitial PNG render re-verify on device pending; full end-to-end (banner actually inside a built EPUB/CBZ) deliberately deferred to the next real book conversion — a full rebuild just for this violates the device's thermal constraint.
* **Type:** `direct` — new feature, additive, guarded to be unable to affect existing builds when disabled/opted-out.

## TASK-32 addendum — P1.1 fresh-container hardware test (2026-07-17, in progress)
* Two more REAL deploy.sh bugs found and fixed by the fresh-path test before it could even start:
  1. `ae1f16f`: all interactive `read`s were unguarded under `set -euo pipefail` — any non-interactive run (nohup, curl|bash without tty) either DIED silently right after the first prompt (stdin at EOF) or HUNG forever (dangling stdin). All five prompts now EOF-safe with their safe default answers.
  2. `9308083`: SIGTERM mid-run (test b) released the wake-lock but ORPHANED the whole launcher→proot→apt tree — container dpkg lock stays held, an immediate resume re-run is guaranteed to fail; deploy-level twin of TASK-40. Trap now resets itself and TERMs its own process group.
* Staging mechanism added (`b0b243e`): `KBG_DEPLOY_DISTRO=ubuntu-test` runs the REAL script against a throwaway container + its own launcher file — production container/launcher untouched.
* Live state: from-scratch `ubuntu-test` container install genuinely running on the device (apt at ~700 packages, calibre chain). The orphan tree from the pre-fix SIGTERM was deliberately left to finish provisioning (SIGKILLing it would just dirty dpkg on a thermally-constrained device); resume re-run + fixed-trap re-TERM follow, then STOP before test (c) per Q's explicit instruction.

## TASK-32 P1.1 addendum 2 — the pip stage vs the fresh container (2026-07-17)
* Fresh-path pip stage failed three ways before the real fix, each a genuine finding:
  1. Pillow source build lacked jpeg/zlib headers → added libjpeg-dev/zlib1g-dev/libpng-dev (`56464df`) — necessary but not sufficient;
  2. Pillow's own setup.py detects the proot-bound Termux prefix (/data/data/com.termux/files/usr) and injects Android headers into Ubuntu's gcc — thousands of compile errors; source-building Pillow inside proot is unwinnable. apt python3-pil added (`52707d9`) — pip recognizes it (12.1.1, freetype verified live in the container) — still not sufficient, because:
  3. **marker-pdf (incl. latest 1.10.2, checked on PyPI) pins `Pillow<11.0.0`** — and the bare 'ubuntu' image now resolves to the 25.10 interim release with python3.14, for which Pillow 10.x has no wheel at all (it predates that Python). Root fix `bd84472`: fresh installs pin `ubuntu:24.04` LTS (python3.12), where every pinned dep ships a binary wheel. proot-distro 5.4.1 installs Docker images with tags, which is what makes this possible.
* Corollary for the PRODUCTION container (feeds TASK-47): it runs the same resolute/python3.14 with a source-built featureless Pillow (no FreeType - `PIL._imagingft` missing, verified). The support-banner interstitial already falls back to the bitmap font (`9308083`), but production's marker/Pillow health deserves its own check in TASK-47's redesign.

## [x] TASK-49: Vydra support system on Appwrite — live, end-to-end verified with a real user
* **Architecture (Q):** Appwrite = registration/referrals/site (always-online cloud); phone = ALL AI generation, self-hosted, reads Appwrite read-only and NEVER blocks on it.
* **DONE, live in Appwrite Cloud (project 6a23420a..., fra):**
  1. Database `kbg-support`, collection `users`: telegram_id (unique idx), referral_code (unique idx), referred_by, banner_disabled (default false), priority_tier (default 0), created_at - all attributes `available`, both unique indexes created.
  2. Function `kbg-tg-support-bot` (python-3.12, execute: any, scopes databases/documents rw): webhook secret check (X-Telegram-Bot-Api-Secret-Token vs TG_WEBHOOK_SECRET), /start [ref_code] with referral crediting (+1 priority_tier to referrer), /referral (code+link+count), /no_support_banner. Deployment `6a59a11d...` built and `ready`. Vars set: TG_WEBHOOK_SECRET (generated, in scratchpad + function), TELEGRAM_BOT_TOKEN=PENDING_FROM_BOTFATHER, TELEGRAM_BOT_USERNAME=PENDING. Code in repo: appwrite/functions/tg-support-bot/.
  3. Site one-pager in repo: site/index.html (bot link placeholder; Appwrite Sites deploy not attempted yet).
  4. Phone-side: common/support_profile.py - read-only profile fetch, 3s hard timeout, EVERY failure path returns safe defaults (banner enabled, tier 0); wired into user_opted_out() (local flag OR remote flag, remote fails toward enabled). support_config.json gained `appwrite` section (endpoint/project/db/collection/telegram_id; key via env KBG_APPWRITE_KEY or ~/.kbg_appwrite_key - NOT in git). priority_tier: helper get_priority_tier() ready; NOT force-wired into TASK-23's audio_priority queue - that queue is per-book chunk priority, tier belongs to the future multi-user queue (documented honestly).
* **BLOCKED on Q:** (a) Telegram bot token from BotFather (+username); (b) Q offered to create a SEPARATE Appwrite project (name proposal: "Bookvar") and give an API key - if so, DB/collection/function/vars migration is scripted and takes ~2 min, then support_config.json project id updates. (c) Live tests (real bot registration, /referral, /no_support_banner, phone reading the flag, fallback simulation with a broken key) all pend the token.
* **Also pending:** set webhook (api.telegram.org/bot<TOKEN>/setWebhook with secret_token + function domain), replace BOT_USERNAME_PLACEHOLDER in site/index.html, Appwrite Sites deployment.

## TASK-49 — completion evidence (2026-07-17, all verified live, not mocked)
* **Brand:** Vydra 🦦 (Q), bot @GetVydraBot, mascot in site/vydra.jpg. Bot short+full descriptions set via Telegram API with Q's copy.
* **Webhook:** function domain `vydra.fra.appwrite.run` (one console click by Q), `setWebhook` with secret_token + allowed_updates=[message] → "Webhook was set", confirmed via getWebhookInfo. **Security verified live:** POST without the secret header → `401 {"error":"unauthorized"}` before any processing.
* **Real registration (Q, not a mock):** /start → row in `kbg-support/users`: telegram_id 6412868393, referral_code d43c6e62, banner_disabled false, tier 0, created_at 2026-07-17T06:54:06Z — read back via REST and shown. /referral answered correctly (code+deep-link+count). /no_support_banner → **banner_disabled flipped to True in the DB, confirmed by direct read.**
* **Phone loop (the whole point):** phone pulled config (owner telegram_id bound in support_config.json; key at ~/.kbg_appwrite_key, chmod 600), `fetch_profile()` returned the REAL remote flag (banner_disabled=True, 0.5s) and `user_opted_out()`=True → banner insertion in TASK-48 will correctly skip. **Fallback verified live:** deliberately broken key → safe defaults {banner_disabled:False, tier:0} in 0.3s — banner counts as enabled, generation never blocked; key restored.
* **Site:** LIVE at https://vydra.appwrite.network (deployment via API, domain via console click) - 200, title/bot-link/mascot all verified serving.
* **Honest TODOs:** (1) replace the phone's key with a console-created scoped databases.read-only key; (3) priority_tier is read (get_priority_tier()) but deliberately NOT wired into TASK-23's per-book audio_priority queue — that tier belongs to the future multi-user generation queue; (4) Q's own banner flag is now disabled (they tested the command) - flip back on request.

## [ ] TASK-50: /install onboarding + fresh-device deploy for outside users (CODE DONE - awaiting the first real fresh device)
* **Shipped (`7edd569`):** deploy.sh STEP 0 pre-flight diagnostics (arch/Termux/RAM/disk/network/Adreno/Termux:Boot as PASS/WARN/FAIL, hard failures abort with Ukrainian explanations before any long work); STEP 3b builds llama.cpp TERMUX-SIDE with Adreno OpenCL flags when detected (closing TASK-47's core gap - production never used the container build) and installs bin/start-translation-server.sh (recovered from the production phone into the repo) to $HOME when absent; bot /install command with full F-Droid Termux + Termux:Boot + one-curl instructions; Telegram command menu registered; README quick-start. One-curl entry verified serving current master (200).
* **Pending live verification:** the first true from-scratch run - planned on Q's son's OnePlus 15 (deferred: he's busy). The Termux-side llama build path has NEVER executed end-to-end; declared honestly, not claimed working. GPU on the newer Adreno may need a CPU fallback in start-translation-server - to be added if the test says so.

## [x] TASK-51: Support-banner control in the local web UI, funneling to the Telegram bot
* **Request (Q):** the local dashboard must let a user manage the banner; the primary path routes them to @GetVydraBot (donation Track A/B options live there, and its /no_support_banner writes the remote flag the phone already reads).
* **Implemented (`dfdbbbb`):** dashboard card "🦦 Примітки підтримки" — live status line (enabled / disabled locally / disabled via bot), primary deep-link button to the bot, and the promised one-step LOCAL toggle (keeps the plan's opt-out-never-hidden guarantee). GET /api/support/profile combines the local flag with the remote Appwrite read (inherits fetch_profile's 3s fail-toward-enabled semantics — the UI can never hang on the network); POST /api/support/local-optout flips global_settings.no_support_banner. Card hides itself entirely when the feature is off.
* **Verified live on the device:** profile endpoint returns the REAL remote flag (banner_disabled=false — the flag flipped back for Q minutes earlier, read via Appwrite), local toggle round-trip true→effective True→false→effective False through the actual Flask API.
* **Also this session (bot side, `12a366f`):** inline main-menu buttons (install / referral / banner-off / Track-A donate URL / developer-donate STUB / premium STUB), callback_query handling with instant ack, left menu button via setChatMenuButton, webhook re-set with callback_query allowed. Payment integrations deliberately stubbed pending a real payment service. Q's banner flag flipped back to enabled per request.

## TASK-32 P1.1 — CLOSED (2026-07-17): from-scratch container test (a) PASSED end-to-end; SIGTERM test (b) fixed and mechanism-proven
* **Test (a) final run (run 5, after the numpy fix `0bf711b`):** the REAL deploy.sh, staged via KBG_DEPLOY_DISTRO=ubuntu-test on the production device, went from `proot-distro install ubuntu:24.04` through apt (~700 pkgs), OpenCL config, Termux-side-llama skip, and the FULL pip line to "Deployment complete". Container verified live: PIL loads a real TrueType font (FreeType working - fresh installs get proper banner interstitials), torch/transformers/manga_ocr/pytesseract all import, marker-pdf 1.10.2 installed. STEP 0 diagnostics ran on the real device (8 PASS). The non-destructive model check took its keep-branch in a real flow. Test container, its launcher and logs removed afterward (~10GB reclaimed); only the production `ubuntu` container remains.
* **Findings chain that this test paid for (all committed):** EOF-killed prompts (`ae1f16f`), trap orphaning + bash's defer-trap-past-foreground-child semantics → bg+wait fix (`892ca01`, mechanism proven locally with setsid+TERM: trap fired immediately, no surviving child), Pillow-in-proot unwinnable source build → LTS pin (`bd84472`) + apt python3-pil (`52707d9`), debian-numpy pip conflict → --ignore-installed (`0bf711b`).
* **Test (b) status, honestly:** the orphan bug is fixed and the fix's mechanism is locally proven; a full second on-device TERM was not repeated after the bg+wait change (post-cache pip window is seconds; forcing an artificial heavy phase contradicts the device's thermal constraint).
* **Test (c) (wipe+reinstall of the PRODUCTION container): NOT executed per Q's standing stop-order.** Recommendation: skip permanently - (a) already proves the fresh path on identical hardware, and (c) risks the working production setup for no new information.
* **Wart noted for later:** the big pip line lets a dependency pull a CUDA torch build over the cpu wheel (--ignore-installed side effect; harmless on-device but wastes ~GBs) - candidate constraint pin in a future pass.

## [x] TASK-52: Vydra UI redesign within existing Flask/Jinja/vanilla-JS (Q numbered it 51; 51 was already taken by the support card)
* **Hard constraint honored, with proof:** redesign commits (`0776c44`, `3618885`) touched ONLY branding/CSS/templates-text and the browse-fs endpoint. `git diff` over the redesign range: stages.html = title+favicon lines only; `kbg_web/edit_store.py` and `common/edit_patch.py` = ZERO changes; drag/resize (TASK-36), overlay math (TASK-27), edit_store API calls, TASK-23 queue - untouched by construction.
* **Branding:** "Kindle Butch Gen" → Vydra in all three templates' titles + headers; round transparent-background mascot (Q's asset, optimized 1024→256px/85KB) in the dashboard header with a teal ring, favicon everywhere. Brand teal→gold gradient applied ONLY to the header h1/logo zone (`3618885`); the functional purple palette, delete/run/re-run button colors and red/yellow/green quality-flag semantics deliberately not repainted.
* **Bug (point 1) - honestly instrumented, not guess-fixed:** "Select Output Directory: Failed to load directories" did NOT reproduce (browse-fs returns 200 with real dirs on both roots, locally and through the public tunnel; storage permission present). Root-cause window is transient (most plausibly Flask autostarted before Android storage mounted, or a Flask-down window). **Resolved 2026-07-17 evening:** Q's error was a STALE BROWSER TAB (page loaded pre-fix at 14:09 among 25 open mobile tabs); after a page refresh the folder picker works — confirmed by Q. The exact modal request was verified 200 through both localhost and the public tunnel before the stale-tab conclusion was reached. Hardening shipped: the endpoint can no longer return non-JSON (any OSError → JSON with the REAL message), missing storage root now falls back to the Termux home with a user-visible Ukrainian hint (termux-setup-storage), and the JS error message now includes the actual failure text - the next occurrence self-explains.
* **Live verification (device, real Flask):** dashboard 200 with new header; `/view/frieren` (stages/QA page) 200 and the bubble-editor JS (startBubbleDrag/resizeHandle/bubble-box/renderManga) present and unchanged; `/api/edit/queue/frieren` 200; browse-fs 200 both roots. Remote screenshotting failed (RPi workspace IPC timeout) - the before/after visual check is on Q's actual phone screen, which beats a remote render anyway; explicitly requested from Q: header look, Save Location dialog, one bubble drag on /view/frieren, flag colors.
* **Deferred by design (point 5):** book-card hierarchy left as-is - no risk added for an optional nicety.

## [x] TASK-53: Donation-gated premium — cast_registry + vision_qa entitlements (verified live, full matrix)
* **Decision (Q):** both TASK-50-cast-registry features (character-gender Cast Registry, vision-QA) become paid/after-donation options. Base generation stays free and unblockable.
* **Implemented (`0e5e9d5`):** Appwrite `users.entitlements` (string) attribute; bot `/premium` shows real entitlement status + unlock instructions (payment service still pending - unlock is manual admin `/grant <tg_id|referral_code> <ents>` restricted to ADMIN_TG_ID, with an automatic 🎉 notification to the user); phone `is_entitled(feature)` in common/support_profile.py - **FAIL-CLOSED** (deliberately opposite to the banner's fail-open: Appwrite down → premium toggle unavailable, generation continues without it) with a **7-day grace cache** so a network blip cannot lock out someone who already donated; `premium_features` list in support_config.json; the gating requirement injected into TASK-50_cast_registry.md's implementation prompt (section 0b) so the features ship gated from day one, showing a locked "🔒 Доступно після підтримки проєкту" toggle linking to @GetVydraBot.
* **Live verification (real device + real webhook):** before grant → False; `/grant` executed through the production webhook with admin identity → after: cast_registry=True, vision_qa=True, nonexistent=False; grace mode (broken key + fresh cache) → True; fail-closed (broken key + no cache) → False; key restored. Q's own profile now carries both entitlements (they are the admin/owner).
* **Numbering note:** the cast-registry spec file is named TASK-50 (collision with the /install onboarding task) - the implementation, when AGY runs it, should register as TASK-54.

## [x] TASK-55: Ukrainian user documentation (docs/uk/)
* **Inventory first (per request):** README.md = English/technical; README_Termux.md = Ukrainian but pre-deploy.sh-era manual steps (now misleading); docs/deployment + plans = mixed working docs; no coherent Ukrainian USER documentation existed.
* **Created (`ec756c4`):** docs/uk/README.md (огляд, принципи, зміст), install.md (вимоги-таблиця, F-Droid Termux, Termux:Boot, one-curl з діагностикою, поведінка при обривах/повторний запуск, типові проблеми), usage.md (Dashboard, картка книги, resumability, Quality Visualizer, редактор бульбашок текст+геометрія, live-editing, де що лежить), bot.md (команди, Track A/B з чесним описом і one-step opt-out, реферали, преміум cast_registry/vision_qa з grace-семантикою, приватність). README.md linked to docs/uk; README_Termux.md marked superseded (kept for history). Every claim in the docs describes verified behavior from this sessions' work - not aspirations.

## [x] TASK-56: In-app Ukrainian user manual with real UI screenshots
* **Made with agent-workspace MCP (RPi .234):** isolated X11 workspace + Chromium driven via DevTools against the live public UI (basic-auth). Captured dashboard, Quality Visualizer (real translated Frieren page 1 - the actual typeset output as the illustration), Downloads Archive; cropped browser chrome, optimized to 90-170KB JPEGs in kbg_web/static/manual/.
* **`/manual` route + manual.html (`44c73b5`):** illustrated Ukrainian guide - dashboard anatomy, book card + safe interruption, visualizer/Pending Edits/Auto-flagged with color legend, manga editing (text + drag/resize geometry, edits survive regens), downloads, bot/support/premium, troubleshooting. Linked from the dashboard header (📖 Посібник). Styled to the Vydra brand (teal-gold gradient title, mascot).
* **Verified live:** /manual 200 on the device, images 200, page screenshot taken through the same workspace browser confirms rendering (gradient title, embedded shots readable, tips blocks).
* **Design assessment (Q asked):** post-rebrand UI is coherent - mascot+gradient header reads well; Run=green / Re-run=orange / Delete=red unambiguous; book-card hierarchy with stage chips and file chips is clear in the full-page view. No risky changes made; candidates for a future light pass: file-chip row density, stage-connector line styling. Workspace stopped after use (RPi resources freed).

## TASK-56 addendum: manual completely rewritten after Q's review (it was missing most of the product)
* Q supplied 10 real phone screenshots; GitNexus reindexed and the code surface re-studied. New manual (`9cf1c52`, /manual) now covers: Add Book anatomy (upload/path, EPUB metadata autofill, Is Manga toggle), per-type stage checkboxes + manga Target Device resolution + TTS Settings, Models Manager (llama status, model swap), the entire TEXT-book editing surface previously omitted - Paragraphs & Audio tab (original | translated+stress-marks columns, per-segment audio player, Edit form with separate Translated text and Stress marks fields feeding priority re-synthesis, Show All/With/Without Audio/Pending Edits filters, 5000+-chunk pagination with jump), Full Page Viewer (per-chapter, Original|Translated) - plus manga visualizer/bubble editor, Downloads Archive with per-device descriptions, troubleshooting incl. Models Manager check. TOC navigation. Illustrations = Q's real mobile screenshots, cropped/optimized 30-155KB. All images verified 200 on device.

## TASK-53 addendum: premium NER/vision model provisioned on Q's device (2026-07-17)
* Gemma 3 4B (stable candidate per the cast-registry spec; the deleted local gemma files were the spec-excluded Gemma-4 family) downloaded to `~/models/gemma3-4b/`: `gemma-3-4b-it-Q4_K_M.gguf` 2489757856 B + `mmproj-model-f16.gguf` 851251104 B (ggml-org/gemma-3-4b-it-GGUF), both size-verified byte-exact, resumable download with wake-lock, released after. Also this round: styletts2 voice published as repo release `models-styletts2-uk-v1` and wired into deploy.sh STEP 6 (fresh-device gap closed); bot /premium + /grant now warn about the first-enable model download; prompt section 0c requires the consent dialog + resumable download + honest missing-model messaging; text NER runs on existing llama-server/cli, vision-QA additionally needs the `llama-mtmd-cli` build target (recorded in the prompt).

## [x] TASK-57: [INCIDENT][FIXED] Production container Pillow broken by the pre-parametrization staging run - ALL new typeset crashed on Cyrillic
* **Symptom (caught by the TASK-54 live test, would have hit ANY manual-edit regen):** fit_text → ImageFont.truetype ALWAYS raised (`PIL._imagingft` missing) → bitmap-font fallback → `UnicodeEncodeError: latin-1` on the first Ukrainian character. TASK-42 (2026-07-16) typeset 194 pages fine, so the breakage window was TODAY.
* **Root cause:** fresh-test run 1 (before `KBG_DEPLOY_DISTRO` existed, `b0b243e`) targeted the DEFAULT alias = the PRODUCTION container; before being killed it got far enough for pip to replace working Pillow with a FreeType-less 10.4.0 source build (the same unwinnable-in-proot build class later diagnosed properly in the ubuntu-test runs).
* **Fix (production container):** `pip uninstall pillow` (removed the broken /usr/local build shadowing everything) + apt `python3-pil` already present underneath → active Pillow now 12.1.1 with working FreeType; verified live: `truetype(LiberationSans-Bold).getbbox("я зробила")` OK.
* **Lesson recorded:** staging isolation arrived one run too late; the alias parametrization + separate launcher (`b0b243e`) is exactly what prevents recurrence. Deep fix for the pip/pin fragility belongs to TASK-47.

## TASK-54 progress (Cast Registry) - implemented core, live verification in flight
* Shipped: common/cast_registry.py (verified-only rules, Latin word-boundary / CJK substring matching, POV, neutral templates), injection into translate_batch_llm (impact: LOW, single call site, optional param - byte-identical prompts when off), gated API (GET/PUT/settings/scan), NER pre-pass via local Gemma 3 4B (bin/cast_ner_prepass.py), Cast & Context tab in the visualizer, llama-mtmd-cli built by default in deploy.sh AND already built on the device.
* Found+fixed en route: entitlement fail-closed inside proot (HOME split hid the key; absolute Termux-home fallback `39e63c5`) - the registry now activates in-container ("Cast Registry active: 1 verified character(s)" in the real pipeline log); TASK-57 above.
* Still open from the spec: invalidation cascade on verified-change (spec §5), text-book-path injection, vision-QA feature itself, neutral-gender regression test.

## TASK-54 — LIVE GENDER TEST PASSED (2026-07-17 evening, real pipeline, real spec example)
* The exact failure class from the spec's own motivation, on real frieren data (page c124 p132), regenerated through the actual pipeline with the registry active ("Cast Registry active: 1 verified character(s)" in the run log), zero tracebacks:
  - "WHAT DID FRIEREN DO?": "що зробив фрірен?" → **"що зробила фрірен?"** (feminine past verb - the headline case, fixed)
  - "I'M ONLY HERE FOR FRIEREN.": "заради фрірена" (masculine-declined) → "через фрірен" (no wrong masculine declension)
* Road to the pass surfaced and fixed two real infrastructure bugs (entitlement HOME-split `39e63c5`, production Pillow incident TASK-57) - both would have bitten future premium users.
* **Cosmetic nit recorded:** the name renders lowercase mid-sentence ("фрірен" not "Фрірен") - the sentence-case normalizer treats it like a common word; candidate: add name_target to the glossary-casing pass. Minor, not blocking.
* **Spec remainder still open:** invalidation cascade on verified-change (§5), text-book path injection, vision-QA feature, neutral-gender regression test.

## [x] TASK-58: [BUG][FIXED] llama-mtmd-cli-by-default broke EVERY fresh install - Termux bionic has no spawn.h

## TASK-58 addendum — CORRECTED: real fix found, not just a workaround
* **Q relayed a Grok analysis of the same log; it was right, my platform-gap conclusion was wrong.** I had NOT checked Termux's own package repo before declaring spawn.h unfixable - I only read llama.cpp's upstream vendored source and Termux's bionic libc directly.
* **Real cause:** Termux's link-time `libc.so` is a curated stub that excludes `posix_spawn` even though the real on-device system bionic exports it (confirmed live: Android API 36, `nm -D /system/lib64/libc.so` shows `posix_spawn` present). `libandroid-spawn` (pkg v0.3) supplies the missing header + a compat `.so`.
* **Verified live on Q's own device, not assumed:** with the package present, `make llama-mtmd-cli` built and linked completely clean - `objdump -p` on the resulting binary shows a normal, fully-resolved NEEDED list, no missing symbols, no extra link flags required.
* **Fix (`93fd670`):** `libandroid-spawn` added to deploy.sh's host `pkg install` line. mtmd now builds for real on fresh devices instead of silently degrading via the `e527348` best-effort fallback - that fallback stays in place as a belt-and-suspenders safety net only.
* **Process note for next time:** "I read the vendored source and found no guard" is not the same claim as "no fix exists" - should have checked the platform's own package repo before asserting a hard platform limit. Recorded as the actual lesson, not just the technical fix.
* **Self-inflicted, caught fast:** earlier tonight `8820a2b` made `llama-mtmd-cli` part of the REQUIRED default build (for future Vision-QA, per Q's "встановлювати по дефолту"). Found live on the OnePlus 15 fresh-install test, run 5: `vendor/sheredom/subprocess.h` (pulled by `mtmd-helper.cpp`) does an unconditional `#include <spawn.h>` - confirmed by reading the actual upstream file, zero `__ANDROID__`/`__BIONIC__` guard exists. Termux's bionic does not ship `spawn.h` at all - a real platform gap, not a missing package deploy.sh could install.
* **Fix (`e527348`):** llama-server/llama-cli (the hard requirement, proven working all session) build and must succeed FIRST; llama-mtmd-cli is attempted in a SEPARATE, best-effort make invocation whose failure only logs a note and never aborts the deploy. Every user's install is unblocked again; Vision-QA prep (not yet a shipped feature) degrading gracefully is the correct tradeoff.
* **Real fix tracked, not attempted live:** build mtmd inside the Ubuntu container instead (glibc has spawn.h; the container already gets the same GPU vendor-lib bind mounts as the Termux host) - follow-up work for TASK-54's Vision-QA half, not risked under a live blocked user tonight.
* **OnePlus 15 fresh-test running tally: 5 real infrastructure bugs found and fixed** (bootstrap libc++/ffmpeg, ipa-uk removed from PyPI, container-only dep in host pip line, ARMv9 -mcpu=native ICE, this mtmd/spawn.h blocker) - every one invisible on Q's years-tuned dev phone, all now fixed for every future installer.

## [ ] IDEA (not planned, not scheduled): native mobile/desktop app instead of Termux on-device compile
* Q's idea 2026-07-17: ship prebuilt llama.cpp binaries per platform (Android arm64+Adreno OpenCL via JNI, Linux/Mac/Windows x86_64/arm64) instead of compiling on-device through Termux - same pattern as LM Studio/GPT4All/Jan.ai. Would eliminate most of tonight's bug class (ARMv9 ICE, missing spawn.h, disappeared PyPI packages, bootstrap libc++) since users get a binary, not a from-source build.
* Tradeoff, why not now: real separate project (per-platform CI build matrix, code signing, likely app-store review for a Play Store path), and the manga pipeline (Calibre/tesseract/marker-pdf, the whole Ubuntu proot container) would need repackaging or stay server/desktop-only, unavailable on a slim mobile APK.
* Status: idea only, not scheduled. Revisit after the current Termux path is stable and TASK-54/support system are further along.

## [x] TASK-59: [PRIVACY][FIXED] deploy.sh leaked developer's private LAN IP + internal GitNexus command to every user
* **Found by Q** reviewing the final output on son's device (Switzerland) - the success banner printed `192.168.3.184` (the maintainer's dev-server LAN IP) and a `docker exec gitnexus-server ...` command, a leftover internal note with zero relevance to any end user, shown to EVERY installer since the script existed.
* **Fix (`f8ec488`):** block removed entirely. Audited all other publicly-shipped files (deploy.sh, bot, site, docs) for similar leaks - none found; the one other LAN-IP mention (superseded README_Termux.md) is a generic illustrative example, not real infrastructure.
* **Lesson:** internal TASK-32-era developer notes had drifted into user-facing script output without review; worth a periodic grep audit of shipped files for `192.168.`/`100.6[0-9].`/internal hostnames before wider public release.

## [x] TASK-61/TASK-62: Add-Book UX (auto-slug, source-lang default) + persistent session login — live-debugged and fixed on son's real device
* **Root-caused via direct SSH+Tailscale access to son's OnePlus 15** (100.119.144.106, key `~/.ssh/id_ed25519_son_op15` on dev-184) instead of relaying more screenshots - confirmed the backend correctly handles real apostrophe-laden paths end-to-end (`.../Frieren - Beyond Journey's End [Manga]/...v02.cbz` added successfully via a direct `/api/add` test); the actual UX gap was the "Source Language" field defaulting to Auto-detect, which `/api/add` rejects for local paths with an opaque 400 - JS now auto-substitutes 'en' with a visible explanation for that specific case (`b100d4b`).
* **TASK-61 (earlier same evening):** slug field removed from the visible Add Book form, auto-derived from Title via Cyrillic transliteration + collision-retry; in-UI password change (immediate, no restart) with a new `KBG_WEB_USER` override so existing devices keep a custom login name across a code update.
* **TASK-62 (this session's closer):** replaced bare HTTP Basic Auth with a proper session-cookie login (`/login` page, 365-day persistent cookie, secret key persisted to survive Flask restarts) - real complaint was "пароль не зберігається, доводиться постійно вводити" (mobile Chrome does not reliably keep Basic Auth across reloads). Found and fixed a real bug in the same pass: 24 routes had redundant per-route `@auth.login_required` decorators that became load-bearing (and would have silently broken session-only access to exactly those routes) once the global hook stopped using flask_httpauth directly - removed. Basic Auth kept as a fallback (auto-upgrades to a session on first use) so scripts/curl keep working unchanged. Verified live end-to-end on BOTH Q's and son's devices: login sets cookie, session-only requests succeed on previously-decorated routes, session survives a full Flask restart, wrong password rejected, cookie-jar test artifacts (curl `/tmp` write failures) correctly identified as tooling noise, not real bugs.

## [x] TASK-63: [BUG][FIXED] three separate production outages on son's live device, same evening - Flask blocking, password ordering, and a script-execution-order crash
* **(a) Flask dev server blocked ALL requests during any slow synchronous handler.** `app.run()` had no `threaded=True`; `/api/update`'s synchronous `git fetch` (up to 90s) froze the entire server for that duration, surfacing as "Loading books..." / "Save Location: Loading..." hanging together with zero error. Fix: `threaded=True` (`53d6d43`). Also hardened `fetchBooks`/`initSettings` to redirect to `/login` on 401 and show a visible error instead of hanging silently on any future unexpected response.
* **(b) `KBG_WEB_PASSWORD` export landed AFTER the autostart trigger line in `~/.bashrc`.** `bash start-all-services.sh` (which launches Flask) ran on an earlier line than the password `export` - the child process never saw it, so every fresh Termux restart silently fell back to a random unprintable temp password, locking the user out. Present since TASK-60's original implementation in `deploy.sh`, plus the same bug in `/api/change-password`'s persistence logic. Fix (`fedc692`): `deploy.sh` now prepends the password export to the top of `.bashrc` instead of appending; the change-password endpoint now inserts before the autostart line (or at the top) instead of at the end. Hand-fixed son's already-broken `.bashrc` live via scp (not heredoc-over-SSH - nested quoting through the double SSH hop corrupted a first attempt, caught immediately by re-reading the file back before restarting anything).
* **(c) The real, 100%-reproducible root cause of "Loading books..." never resolving on ANY device, ever:** the inline `<script>` block sat mid-`<body>`, executing synchronously at HTML parse time, and at line ~1010 unconditionally called `document.getElementById('file_upload').addEventListener(...)` - but that element (inside the "Add New Book" modal) was defined ~1300 lines LATER in the same file, after `</script>`. `getElementById` returned `null`, `.addEventListener` on `null` threw an uncaught synchronous TypeError, and the browser's JS engine aborted the rest of the script on the spot - including the initial `fetchBooks()` and `initSettings()` calls further down. This affected every single page load on every device (confirmed live in both a sandboxed test browser and Q's own real phone browser - neither ever sent a single `/api/books` or `/api/settings` request, while a full page navigation to those same URLs, or plain curl, worked instantly). Fix (`5a4256c`): moved the entire script block to just before `</body>`, after all the modal markup it depends on - pure reorder, zero logic changes.
* **Debugging note:** (c) was very nearly misdiagnosed as a sandboxed-test-browser artifact, since curl and direct-URL navigation both worked fine and only in-page `fetch()` calls silently vanished - a Node.js harness with fully-permissive DOM/fetch stubs was actively misleading (it never returns `null` from `getElementById`, so it couldn't reproduce the crash) until real user traffic in the live Flask log showed the exact same "no `/api/books` ever requested" pattern from a genuine mobile browser, ruling out "just the sandbox" as an explanation.
* All three verified live end-to-end after the fix: correct login (`302`), `/api/books` returns real data, and - critically - an actual browser session (not curl) rendered the full book card with progress bar and controls instead of a stuck spinner. Confirmed independently by the user on the real device afterward ("бачу книжку").

## [x] TASK-64: [BUG][FIXED] manga translation crashes immediately - SIGILL in PyTorch's comic_text_detector on Snapdragon 8 Elite 2 (son's OnePlus 15)
* **FIXED next session (2026-07-18, `c654f84`), verified live.** Two findings closed it:
  1. My env-var attempts failed for a trivial reason: `ATEN_CPU_CAPABILITY` values are **case-sensitive lowercase** (`default`, `sve128`, `sve` - read straight from `DispatchStub.cpp` v2.13.0 source: `envar == "default"`). With lowercase, the override IS accepted (`get_cpu_capability()` reports `DEFAULT`)... **and the crash still happens** - proving the SIGILL was never in ATen dispatch at all.
  2. Real source: **oneDNN/ACL** (the mkldnn conv2d backend), which does its own CPU detection independent of ATen and picks SVE kernels on its own. `ONEDNN_MAX_CPU_ISA=ASIMD` did NOT help either; `torch.backends.mkldnn.enabled = False` **fixes it completely** (raw forward + full detector, tested in isolation on-device).
* **Shipped fix:** `translate_manga.py` disables mkldnn only when `torch.backends.cpu.get_cpu_capability()` starts with `SVE` - ARMv8/NEON devices (Q's own phone) keep the faster oneDNN path untouched. Cost: slower convolutions on affected new SoCs; working-slower beats crashing-instantly.
* **Live verification on son's phone:** real conversion via `POST /api/run` - log shows the fix banner ("SVE CPU detected: disabled mkldnn/oneDNN backend"), then pages 1, 2, 3/198 processed with real OCR output, process alive - previous runs died in seconds on page 1, twice.

## Original investigation notes (2026-07-17 night, before the fix):
* **Symptom:** clicking Translate on `frieren-test-v02` (a real manga book) starts the pipeline, then "hangs" immediately - no visible error in the UI. Two separate real attempts (01:38:37 and 01:41:25) both died identically.
* **Root cause, fully isolated via bisection (not guessed):** `conversion_progress.log` shows `proot info: vpid 1: terminated with signal 4` (SIGILL) right after "Processing 198 pages... Page 1/198" - i.e. as soon as the comic-text-detector's PyTorch model actually runs. Bisected step-by-step inside the proot Ubuntu container: `torch.load()` of the checkpoint - OK; constructing `TextDetector` (model build) - OK; calling `det.net(x)` with a raw zero tensor, bypassing all cv2/numpy preprocessing - **crashes with the exact same SIGILL**. So this is 100% inside PyTorch's own tensor compute kernels during the forward pass, nothing to do with image decoding, OCR, or this app's own code.
* **Why:** `torch.backends.cpu.get_cpu_capability()` reports `SVE128` - this PyTorch 2.13.0 aarch64 wheel auto-detected ARM SVE support from `/proc/cpuinfo` (which lists `sve sve2 svei8mm svebf16 sme ...` - genuine silicon capability of the Snapdragon 8 Elite 2) and is dispatching to SVE-vectorized kernels. Working theory (not yet independently confirmed against this specific device's kernel config): the CPU silicon supports SVE, but Android's kernel likely does not have userspace SVE register-state access enabled (`CPACR_EL1` trap bits) - a very common gap on Android specifically, since almost no mobile app needs SVE and most vendor kernels skip enabling it. Executing a real SVE instruction then traps as SIGILL regardless of proot - proot doesn't affect kernel-level ISA feature gating at all, this would happen identically outside proot too.
* **Fix attempts tonight, ALL FAILED** (each independently verified against the isolated `det.net(x)` repro, not just theorized):
  1. `TORCH_CPU_CAPABILITY=DEFAULT` - wrong env var name entirely for this build (doesn't even change the reported capability).
  2. `ATEN_CPU_CAPABILITY=DEFAULT` / `=GENERIC` / `=ASIMD` - this IS the real env var (confirmed via `strings` on `libtorch_cpu.so`, and `ASIMD`/`SVE128`/`SVE256`/`SVE2`/`DEFAULT` all literally exist as strings in the binary) - but PyTorch's own validation rejects every single value tried with "ignoring invalid value for ATEN_CPU_CAPABILITY", including the ones lifted straight from its own binary. Looks like the aarch64 build's validator only recognizes x86 tokens (AVX2/AVX512) and was never wired up for ARM overrides, or requires some different exact form not yet found.
  3. `OPENBLAS_CORETYPE=ARMV8` (the classic OpenBLAS-runtime-CPU-detection-overshoot workaround) - no effect, confirming this isn't an OpenBLAS/BLAS-level issue, it's ATen's own kernel dispatch.
  4. `OMP_NUM_THREADS=1` / `OPENBLAS_NUM_THREADS=1` - no effect (rules out a threading/concurrency race as the cause).
* **Not yet tried:** downgrading `torch` to an older pip version (2.2.0 through 2.12.1 all available per `pip index versions torch`) on the theory that ARM SVE-specific ATen kernels are a relatively recent addition and an older wheel might never select them, safely falling back to plain NEON/ASIMD. Not attempted live tonight - real cost (~200-300MB download + reinstall on a mobile device late at night) and real risk (might break other torch-dependent features, e.g. TTS) weighed against the hour; paused for Q's explicit go-ahead rather than gambling on a live device without checking in first.
* **Also not yet checked:** whether this is a documented PyTorch GitHub issue with a known fix; whether the same crash reproduces WITHOUT proot at all (would conclusively rule out proot as a factor, though the env-var-driven theory above doesn't implicate proot in the first place); whether a from-source PyTorch build targeting `-march=armv9-a+nosve` would be needed as a last resort (heavy - hours of on-device compile, not attempted).
* **Everything else about this book/pipeline is confirmed healthy** - the earlier TASK-63 fixes (login, book listing, file access) all verified working correctly; this is a new, separate, deeper failure specific to actually RUNNING the manga OCR/translation step, not the UI/backend plumbing around it.

## [x] TASK-65: agentic vision editor - Gemma proposes, human approves (spec doc titled "TASK-53"; number 53 was already taken by Appwrite premium in this file, hence 65)
* **LIVE TEST PASSED (attempt 3, Q's phone, frieren):** `2 proposal(s) submitted as pending (source=gemma_agent), 1 skipped` - the skip was correct dedup (b00 already had a pending edit from the aborted attempt 2). API verification: 3 agent-sourced edits, ALL `status=pending`, field `manual_bbox_override`, **0 approved** - the human gate held exactly as designed. Proposals with model rationale: b01 bbox `[211,451,298,1130]` ("box slightly too high, reduce top by 10px"), b02 bbox `[211,98,615,423]` ("exclude the overlap"). llama-server restarted afterward (200).
* **Honest quality note:** b01/b02 are conservative, plausible single-axis tweaks; b00 (from attempt 2) proposed a bbox IDENTICAL to the current one - a useless no-op proposal the human will just Discard. v1 quality is "sometimes helpful, needs the human gate it has" - exactly why auto-approve stays off.
* **Two real device constraints found the hard way, both now guarded in code:**
  1. RAM: Gemma vision (~3.3GB) on top of resident llama-server (~4.4GB) OOM-killed the ENTIRE Termux session (attempt 2). Guard: agent refuses under ~5GB MemAvailable with an actionable "stop llama-server first" message; never kills llama-server itself.
  2. CPU: full-core llama-mtmd-cli got Termux killed by Android's background process killer even WITH active cooling and 8.7GB free (attempt 3 pre-fix... actually the kill between attempts). Mitigation: vision runs at `-t 4` - slower per page, survives. Possible permanent fix (not done): adb `max_phantom_processes` override.
* **What shipped (`d579c3f`, `d5e9839`):** extension of the Cast Registry premium line per Q's spec. Gemma 3 4B vision (llama-mtmd-cli + mmproj, both already on Q's phone from the premium setup) examines ONLY algorithmically flagged QA cases (`box_overlap`/`overflow` from quality_flags.json - never the whole book) on the rendered page and proposes fixes.
* **Same-API principle (spec §2):** the agent acts through the exact endpoints the human UI uses - `PUT /api/edit/manga-bbox` (TASK-36 geometry/font-size) and `PUT /api/edit/manga-text` - authenticated with the same Basic-Auth fallback creds from the environment. No new action endpoints; the only new route is the launch trigger `POST /api/agent-editor/scan/<slug>` (mirrors the TASK-54 NER-scan pattern: entitlement-gated, detached Popen, log in edits/agent_editor.log).
* **Human gate (spec §3, the TASK-17 lesson):** every proposal lands in edit_store as `status=pending` with the new mandatory audit field `source="gemma_agent"` (default `"human"` everywhere else); Pending Edits UI shows a "запропоновано агентом" badge; `agent_auto_approve` is read but DELIBERATELY not honored in v1 - no agentic bypass of human Approve/Discard, no exceptions.
* **Memory (spec §5):** MemPalace precedent lookup before text proposals + approved-only translation-memory writes (`edits/approved_tm.jsonl` + optional MemPalace POST) - both best-effort behind `KBG_MEMPALACE_URL`, fail-open, never blocking; the agent itself never writes memory, only the human-approve hook in app.py does, so unapproved agent guesses can never contaminate cross-book memory.
* **Opt-in (spec §0):** `enable_agent_editor` in the book's config.json, default absent/false; scan endpoint 400s without it.
* **First live run findings (Q's phone, frieren, 8 real box_overlap flags):**
  1. Crash: no PIL on the Termux host (TASK-57 déjà vu) - fixed by dropping the dependency entirely; `bubbles_meta.bbox_ref_size` [1500,2250] is the canonical coordinate space and keeps every number in the vision prompt consistent with the stored bubble bboxes (`d5e9839`).
  2. **OOM incident: the second run killed the ENTIRE Termux session on Q's phone** (Flask + sshd + everything) - Android's low-memory killer, triggered by loading Gemma vision (~3.3GB) on top of the resident llama-server translation model (~4.4GB). Added a hard RAM guard: agent refuses to start under ~5GB MemAvailable with an actionable message (stop llama-server via Models Manager first), and never silently kills llama-server itself (it may be mid-translation).
* **Status: implementation + guards deployed; the end-to-end frieren live test (proposals actually landing in Pending Edits, quality comparison vs human fixes per spec §Тест) still pending** - blocked on the phone's Termux being reopened after the OOM kill, then re-run with llama-server stopped.

## [ ] TASK-66 (PLANNED, Q's idea 2026-07-18): teachable agent - DRAKON-authored knowledge base of behavioral rules
* **Q's ask:** be able to WRITE a knowledge base for the premium agent - "how to behave in specific situations" - authored as DRAKON flowcharts in [ai-drakon-scaffolder](https://github.com/maxfraieho/ai-drakon-scaffolder) and exported as pseudocode (the scaffolder already has `handleExportPseudocode` -> `pseudocodeToMarkdown`, confirmed via GitNexus).
* **Design (grounded in the v2 geometry+veto architecture):** rules plug into three existing hook points, and can only NARROW agent behavior - never bypass the human gate, never enable auto-approve:
  1. **pre-filter** - which flagged cases to touch at all ("не чіпай звуконаслідування/декоративний текст", "пропусти обкладинки");
  2. **geometry constraints** - preferred/forbidden move directions, no-go zones, min/max font ("рухай бульбашки вниз, не вгору", "не розширюй рамку в бік облич");
  3. **vision-veto additions** - free-text behavioral instructions appended to VERIFY_PROMPT.
* **Format:** `agent_rules.yaml` (global + per-book override). Two rule tiers: structured `when:`/`then:` over a DOCUMENTED fact vocabulary (reason, aspect ratio, text length, looks-like-SFX, page position, overlap direction) with a WHITELISTED action set (skip / prefer_move / forbid_expand_into / min_font / veto_note) - interpreted, never executed as code; plus free-text `advise:` lines for the vision judge.
* **DRAKON path:** draw diagram -> Export Pseudocode -> new small converter `drakon2rules` (DRAKON question-icons -> when-conditions, action-icons -> then-verbs) -> agent_rules.yaml. This is knowledge engineering, not ML training - the correct fit for an on-device 4B model where fine-tuning is impossible.
* **The "learning" feedback loop:** every proposal records which rule(s) fired (rule_id in the edit); approve/discard outcomes (already captured via approved_tm.jsonl + edit statuses) aggregate into per-rule acceptance stats - Q sees which rules produce approvable proposals and edits the diagram accordingly. Draw -> export -> agent applies -> stats -> refine.
* Status: idea worked through, not yet scheduled for implementation.

## TASK-66 addendum (Q, 2026-07-18): private admin training studio + future audio-QA direction
* **Separation decision:** the teaching environment must be admin-only and NOT in the public repo. A branch of the public repo does NOT hide anything (all branches of a public repo are public) - the correct split is:
  - **Public repo (this one):** runtime only - the whitelisted rule interpreter, rule-hit logging, per-rule approve/discard stats, and one admin-gated upload endpoint. Rules and stats are per-device DATA files (books/userdata), never repo content.
  - **Private studio (separate private repo):** the actual teaching environment - rule authoring (DRAKON pipeline from ai-drakon-scaffolder, which already ships Agent Studio scaffolding: `agent-studio-data.ts`/ToolDefinition), per-rule acceptance dashboards, rule A/B trials, and later the audio-review tooling. Talks to the device's Vydra API with admin credentials; deploys rules as data (scp/API upload), not via git.
* **Future audio direction (Q's sketch):** the same model-assisted loop for audiobooks - "listen" to generated audio chunks with previous-chunk context and fix stress placement (наголоси). Grounded implementation path with EXISTING pieces: TTS chunk -> whisper.cpp transcription on-device/dev-server -> align vs source text -> mismatch/homograph flags -> LLM proposal WITH rolling previous-chunk context + Q-authored pronunciation rules -> lands in the EXISTING /api/edit/stress pending queue -> human approve -> existing regenerate-audio. Adds a per-book "stress decisions journal" (pronunciation memory, sibling of Cast Registry). Same teaching pattern: DRAKON rules for homograph disambiguation; approved stress edits feed the dictionary. Needs no new edit infrastructure - the whole QA loop rides the pending->approve rails that already exist.
* **Roadmap refinement (Q, same day):** the end state is the OPPOSITE of locked-down - eventually open rule-authoring to users as a public feature, doubling as a popularization channel for the DRAKON editor project (users draw behavior diagrams for their own agents in ai-drakon-scaffolder). The private-studio phase is deliberate maturation: keep it admin-only until the rule vocabulary/interpreter/feedback loop are proven on Q's own use, then ship user-facing rule editing as a separate feature. The public-runtime/private-studio split above stays correct for BOTH phases - opening later means shipping the studio UI, not moving the runtime.
* Status: architecture decided (public-runtime / private-studio split, closed-then-open roadmap); studio itself not scheduled yet.

## TASK-65/66 session wrap (2026-07-18 afternoon) - shipped today, all live-verified on Q's phone
* Agent v2 (geometry computes / vision vetoes) - 8/8 real proposals on frieren, visual "Як є / Як буде" canvases, editable before accept, human page labels, "👁 Результат" jump (with the approved-edits-never-baked bug fixed en route).
* Nonsense-text detection + horizontal reformat + agent notes; vetoed nonsense findings survive as flag-only proposals.
* 🤖 Агент tab: explicit owner start/stop, live log, llama-server auto-pause/restore, friendly 💎 state for non-premium.
* Premium onboarding dialog (model download resumable, cooling warning, manual link) + /manual premium section.
* Heavy-model conflict guards on all 5 launch points with Ukrainian dialogs (deployed, `003e197`).
* UX round 1: dashboard fully Ukrainian.
* Private studio repo https://github.com/maxfraieho/vydra-studio: rule contract, drakon2rules (tested), deploy/stats tools, web UI LIVE at dev-184:8090 (convert 200-verified), multi-diagram composition rules documented.
* **NEXT STEP (not started): runtime rules interpreter in agent_editor.py** - read agent_rules.yaml (global + per-book), compute facts (vocabulary in studio docs/fact-vocabulary.md), apply whitelisted actions (skip/prefer_move/forbid_move/max_shift_px/min_font/max_font/veto_note/require_note), stamp rule_id into proposals (edit_store + PUT endpoints ready to carry it), first-in-file-wins precedence, skip strongest. Then remove the "runtime TODO" banner in studio_web.py.

## [ ] TASK-67 (PLANNED; spec doc "TASK-54_bubble_classification_premium.md" - number 54 already taken): bubble classification free tier + vision-fallback premium
* **Two reality-check findings from code analysis (change the spec's assumptions):**
  1. **Item 0 (RTL content.opf) is a no-op in THIS codebase:** manga never generates content.opf at all - output is CBZ (plain zip of images, no OPF) + AZW3 via mapaki, and mapaki packs manga RTL BY DEFAULT (the `--left-to-right` override flag is already wired in translate_manga.py:1999). No RTL defect exists to fix.
  2. **Item 1's "extend get_bubble_box over mask_refined" is built on a wrong premise:** `mask_refined` is the TEXT-GLYPH mask (for inpainting), not the balloon outline. Rigaud et al. classify by the BALLOON CONTOUR shape (smooth/wavy/zigzag) - applying sigma analysis to the glyph mask would classify letter shapes, not bubble types. A real implementation needs a balloon-segmentation step first (flood-fill from the text region across the white balloon interior to its dark border -> contour -> sigma), which the research got for free from its dataset's annotations.
* **Implementation order for next session:** (1) balloon-contour extraction helper (flood-fill based, additive, stores `bubble_class` + `class_confidence` into bubbles_meta - no typography changes yet, safe to ship); (2) typography per class (Bold Italic Shout etc. + Cyrillic ascender check vs TASK-28 stroke-width work); (3) SFX Track A (no inpaint, subtitle in safety_margin, ALWAYS needs_human_review); (4) premium vision-fallback for low-confidence cases (is_entitled("manga_bubble_classification"), reuse agent's llama-mtmd-cli invocation + heavy-model guards from TASK-65); (5) keep_honorifics in Cast Registry + honorific-change review flag; (6) frieren live test with real % needing fallback (research benchmark ~15%).
* **Hard boundary (spec section 6, keep verbatim):** geometry+typography autonomous and free; SFX / vision-fallback / honorific changes ALWAYS pending in Pending Edits regardless of premium status.

## TASK-67 step 1 checkpoint (live calibration on frieren, 60 pages / 348 bubbles)
* Core shipped: `common/bubble_shape.py` (flood-fill balloon contour from CLEANED pages + Rigaud sigma analysis) + `bin/classify_bubbles.py` calibration runner (additive, pipeline untouched). Runs in the container, pure cv2 - RAM-safe alongside llama-server.
* **Calibration iteration 1 worked as designed:** first run showed 75% false-open contours; root cause was the too-tight analysis window (balloon outline sits 1.5-2x beyond the text box, flood hit the window border before the outline). Window escalation (0.4 -> 0.8 -> 1.4) dropped it to 43%. Real dialogue classifies correctly (sigma 0.003-0.017, closed, conf 0.9); front matter/chapter lists correctly land outside balloons; thought/shout classes appearing (7%/2%).
* Current fallback rate 44% vs research benchmark ~15%. Honest assessment: frieren is caption-heavy seinen (lots of borderless narration boxes -> genuinely balloon-less), so the true rate should exceed 15%, but not 3x. Next calibration steps before wiring typography/prompts: (1) eyeball a sample of sfx_candidate cases to split "genuine caption/SFX" from "flood escaped through the balloon tail"; (2) add a caption class via rectangularity test on the escaped region; (3) morphological closing before flood to seal tail openings.
* **Q's direction (recorded):** classification must also feed the TRANSLATION prompt - per-class tone patterns (Shout expressive, Thought inner-reflective, Caption literary, SFX domestication) injected into translate_batch_llm the same optional-param way Cast Registry does. Planned after classes are trustworthy.

## [~] TASK-68: interface for non-specialists (Q's directive; reference user = his son)
* Round 2 shipped+deployed (`0f9e95d`): simple-mode book card - ONE primary action per state (Почати/Зупинити/⬇️ Завантажити готову книгу з автовибором формату), human phase names, cockpit (checkboxes/resolutions/TTS sliders/re-run/tech log/delete) collapsed into "⚙️ Додаткові налаштування" with preserved open-state. Round 1 was labels+premium states.
* Still open for next rounds: Add-Book simplification (file-first, auto title from filename, langs collapsed, "почати переклад одразу?" after add), viewer/stages jargon pass, first-run hello screen. Live feedback source: son gets premium today (/grant in bot) and tests together with Q.

## HANDOFF для наступних моделей (Fable 5 недоступний з завтра; написано механічно-виконуваними кроками)

### Стан на передачу (все задеплоєно на телефон Q, все запушено)
Агент v2 + вкладка Агент + guard-и моделей + правила DRAKON (повний runtime: skip/notes/advise/напрямки) + студія (dev-184:8090) + спрощені картки книг і форма додавання + класифікація бульбашок у пайплайні (дані завжди, тон у промпті за прапорцем).

### Крок 1 (перевірка, безпечно): cast-init у повних прогонах
`grep -n "init_cast_registry" translate_manga.py` → викликається ТІЛЬКИ в regenerate-шляхах (1393, 1458). Повний прогін, схоже, ніколи не ініціалізує Cast Registry. ПЕРЕВІРИТИ: запустити повний переклад 2 сторінок тестової книги, шукати "Cast Registry active" в conversion_progress.log. Якщо нема - додати `init_cast_registry(book_dir)` поруч з `init_bubble_tone(book_dir)` (~рядок 1859) і повторити перевірку.

### Крок 2 (A/B тесту тону): enable_bubble_tone
1. У books/frieren/config.json додати "enable_bubble_tone": true.
2. Regenerate 2-3 сторінки, де bubbles_meta має bubble_class=shout/thought (шукати: `grep -l '"bubble_class": "shout"' books/frieren/bubbles_meta/*.json`).
3. Порівняти переклад до/після (bubbles_meta зберігає translated_text). Якщо реєстр тону доречний і нічого не зламалось - зробити прапорець default=true у kbg_web/app.py /api/add (створення config.json нової книги).
4. Лог пайплайна при увімкненому: "Bubble-tone prompt injection: ENABLED".

### Крок 3 (типографіка за класом, спец TASK-67 §3): NE ЧІПАТИ без A/B
Точка входу: fit_text/draw у translate_manga.py (шрифт вибирається там). Мапа: shout→Bold Italic +15-20% кегль; thought→Italic -5%; caption→вирівнювання вліво. Кирилічні виносні (Ї/Щ/Ц) вже враховані в TASK-28 stroke-width - НЕ переносити латинську "crossbar I" конвенцію.

### Крок 4 (калібрування далі, за бажанням): частка sfx_candidate 31%
Найкращий наступний хід - НЕ підкручувати пороги наосліп, а очима переглянути 15-20 sfx_candidate-кейсів (класифікатор друкує σ/spikes/closed для кожного) і розділити "справжні SFX/титули" від помилок. Пороги - константи в common/bubble_shape.py.

### Тестування з сином (коли повернеться)
Грант: /grant <його tg_id|referral_code> cast_registry у @GetVydraBot (TASK-56: vision_qa прибрано як застарілий дублікат - одне ім'я entitlement на обидві фічі). Оновлення на його телефоні: кнопка "🔄 Оновити Vydra". Спостерігати 3 точки: додавання книги / що робити далі / де результат.

## [x] TASK-56: сторінка налаштувань (app-level + per-book) + заміна термінології "преміум"

**Крок 0 (реконсиляція, зроблено ПЕРЕД будь-якими фіксами):** `is_entitled("vision_qa")` викликався 0 разів у всьому коді (grep-підтверджено) - усі 6 реально premium-gated точок перевіряють `is_entitled("cast_registry")`, незалежно від того, чи фіча - Cast Registry, чи агент-редактор. "Vision-QA" і "Агент-редактор" виявились однією й тією ж реалізованою фічею (vision-модель перевіряє позначені сторінки, пропонує виправлення) під двома різними маркетинговими назвами в різних місцях UI. Звели до однієї назви ("Агент-редактор", бо `enable_agent_editor` - реально підключений конфіг-прапорець): `vision_qa` прибрано з `support_config.json`'s `premium_features`, з usage-тексту бота, з manual.html, з dashboard.html'ного мапи назв entitlement. Мертве поле `editor_model` (залишок видаленої TASK-17-фічі, ніде не читається - GitNexus impact query підтвердив "target not found") прибрано з `global_settings.json` на телефоні.

**Крок 1:** усі 56 видимих згадок "преміум"/"premium" замінено на "розширені можливості" в manual.html, dashboard.html, stages.html, app.py, боті - grep підтверджує 0 залишків (перевірено і на реально задеплоєному коді телефону). Команда `/premium` і внутрішні ідентифікатори (function/element names, API routes) лишились без змін - технічні маркери, не видима користувачу мова.

**Крок 2-3:** дві нові модалки (стиль існуючих folderModal/premiumOnboardModal): app-level "⚙️ Налаштування" (статус Track A/B + посилання на бота, shortcut у Models Manager - адитивно, не замінює наявний supportCard) і per-book "⚙️ Налаштування книги" (нова іконка на кожній картці): Cast Registry - лінк на вкладку Cast & Context (не дублюється), агент-редактор - НОВИЙ перемикач, якого раніше не існувало ЖОДНОГО (мануал буквально казав редагувати config.json вручну), роздільність манги (перенесено з картки, тепер персистентна), нова фіча keep_honorifics.

**Нова фіча keep_honorifics:** опційна інструкція в промпті перекладу - зберігати японські/корейські гоноративні суфікси (-сан/-чан/-кун) замість локалізації. Той самий gate-патерн, що й enable_bubble_tone (порожній рядок = байт-в-байт ідентичний промпт, коли вимкнено).

**Виправлено Q під час імплементації:** аудіокнижковий дефолт спершу зробив ДРУГИМ чекбоксом у новій модалці - неправильно, дублює наявний "Аудіокнига" на картці. Q явно вказав зробити ІСНУЮЧИЙ чекбокс persistent, не додавати другий - перероблено.

**Крок 5 (жива перевірка, не на віру):** увімкнення/вимкнення `enable_agent_editor` на реальній книзі frieren через новий `/api/book-settings/<slug>` (той самий шлях, що й новий UI-перемикач) підтвердило зміну в `config.json` на самому телефоні, обидва напрямки (true→false→true), плюс round-trip для `keep_honorifics`/`manga_resolution` - усі підтверджені, потім повернуті до попередніх значень книги.

### Пастки (щоб не наступати повторно)
- pkill -f "<ім'я скрипта>" вбиває ВЛАСНИЙ ssh-шелл → завжди bracket-regex: `pkill -f "app[.]py"`.
- Flask рестарт на телефоні: kill і launch РІЗНИМИ ssh-сесіями, launch з setsid nohup + явні KBG_WEB_USER/KBG_WEB_PASSWORD (значення — у приватному ~/.kbg_appwrite_key / config, НЕ тут — цей файл публічний).
- Шаблони Flask кешуються - після зміни templates/ потрібен рестарт.
- Термукс /tmp нередоступний для запису; heredoc з кирилицею через подвійний ssh ламається → Write локально + scp.
- Vision (llama-mtmd-cli) на OnePlus 13: ТІЛЬКИ -t 4 і ТІЛЬКИ при зупиненому llama-server (RAM-guard в агенті це контролює).
- Після кожного push: git pull клону ~/projects/kindle-butch-gen на dev-184 + docker exec gitnexus-server ... analyze (інакше індекс бреше).
- GitNexus HTTP MCP: initialize→mcp-session-id→notifications/initialized→tools/call; готовий скрипт-патерн /tmp/gq3.sh на dev-184.

## [x] TASK-69: [BUG][FIXED] heartbeat-watchdog хибно нуджив уже 100%-завершені книги (номер 57 зайнятий іншим закритим інцидентом - Pillow/Cyrillic - див. вище; 69 наступний вільний)

**Симптом:** книга 194/194 (повністю дороблена) отримала від @GetVydraBot "⏸️ Схоже, переклад... зупинився" двічі поспіль.

**Корінна причина (підтверджено читанням реального коду, не здогад):** `common/heartbeat.py`'s `send_heartbeat()` пише `active_book_slug` в Appwrite (`kbg-support/users`) на кожній сторінці/кейсі, але НІЧОГО не скидало це поле на успішному завершенні. `appwrite/functions/heartbeat-watchdog/src/main.py` перевіряє лише `active_book_slug IS NOT NULL` + вік `last_heartbeat_ts` (`STALE_SECONDS=300`) - явної completion-перевірки не існувало взагалі, тому щойно доробена книга рано чи пізно виглядала "зависла". Найгостріший repro: `bin/resume_active_conversion.py` перезапускає `translate_manga.py` після Termux-kill; якщо всі сторінки вже перекладені, skip-цикл жодного разу не викликає `send_heartbeat()`, тож старий (ще до-крашевий) `last_heartbeat_ts` взагалі ніколи не оновлюється - watchdog далі б'є по ньому, повторно на кожен 5-хв cron-тік (`REALERT_SECONDS=300`), звідси "двічі поспіль".

**Фікс (`edff4c0`):** новий `common/heartbeat.clear_heartbeat()` (порожній `book_slug` → `tg-support-bot`'s `_heartbeat()` вже вмів перетворювати це на `None`) викликається РІВНО один раз - у справжній success-точці `translate_manga.py` (одразу після "Manga translation completed successfully!", всередині `try`, до `finally`) і `bin/agent_editor.py` (поруч із наявним коректним очищенням `.active_conversion.json`). Обидві точки недосяжні з жодного exception-шляху (жодного `except`, що ковтав би помилку, між циклом і цим викликом немає) - реальний крах і далі не викликає clear, тому watchdog і далі його побачить.

**Тести (реально виконано, не лише прочитано):** написав `/tmp/test_watchdog_task57.py`, який завантажує РЕАЛЬНИЙ `heartbeat-watchdog/src/main.py` + `common/heartbeat.py` через `importlib`, підміняє лише Appwrite SDK (немає на dev-184) і `requests.post` (щоб не спамити реальний Telegram/Appwrite), і виконує справжню логіку функцій без мережі:
- (a) `active_book_slug=None` (стан після фіксу) → 0 сповіщень.
- (b) реальний завислий стан (slug ще стоїть, heartbeat старий) → рівно 1 сповіщення + `last_stall_alert_ts` оновлено.
- (c1) негайний повторний прогін того самого стану → 0 (дедуп працює в межах вікна).
- (c2) той самий незмінний завислий стан ПІСЛЯ `REALERT_SECONDS` → знову 1 (монітор НЕ став сліпим, реальний крах і далі детектується).
- POST-тіла `send_heartbeat`/`clear_heartbeat` перевірені напряму: `clear_heartbeat()` шле `book_slug=""`, `send_heartbeat()` - реальний slug.
Усі 6 перевірок - PASS. `translate_manga.py`/`bin/agent_editor.py` самі не запущені живцем (потребують cv2/torch/vision-модель, доступні лише в Termux) - безпека їхньої нової точки виклику перевірена структурно: жодного `except` між success-логом і `clear_heartbeat()`, Python гарантує недосяжність цього рядка при винятку.

**Не зроблено (свідомо, поза скоупом цього фіксу):** дедуп і далі суто часовий (`REALERT_SECONDS`), не "по інциденту" - справжній незникаючий збій ре-нуджитиме кожні 5 хв, а не одноразово. Q запропонував окрему фічу - ручний toggle "працюю/не працюю" в боті, щоб watchdog не турбував поза активною сесією - реалізовано нижче як TASK-70.

## [x] TASK-70: ручний `/pause` `/resume` toggle для heartbeat-watchdog (Q's ask, продовження TASK-69; номер 58 - природний аналог - теж зайнятий закритим llama-mtmd-cli багом, тому 70)

**Мета:** watchdog не вміє відрізнити "Q свідомо зараз не працює з Vydra" від "Termux мовчки здох" - обидва виглядають як однаковий stale `last_heartbeat_ts`. Явний toggle замість спроби вгадувати.

**Реалізація:**
- Новий boolean-атрибут `watchdog_paused` на `kbg-support/users` - створено вживу через Appwrite REST API (`POST .../attributes/boolean`), підтверджено `status: available` перед використанням.
- `/pause` і `/resume` в `tg-support-bot/src/main.py` (той самий патерн, що й `/no_support_banner`) - ставлять/знімають прапорець, шлють підтвердження. НЕ впливають на сам `send_heartbeat()` з телефону - лише на рішення watchdog'а нуджити чи ні.
- `heartbeat-watchdog/src/main.py`: перший чек у циклі по користувачах - `if user.get("watchdog_paused"): continue`, до будь-якої перевірки staleness.

**Тести (реально виконано):** `/tmp/task70/test_pause_task70.py`, той самий підхід (реальний код, підмінені лише Appwrite SDK + Telegram HTTP): `/pause` виставляє прапорець + шле підтвердження; `/resume` знімає + підтверджує; paused-юзер із реально старим heartbeat → 0 сповіщень (навіть попри genuine staleness); той самий stale-стан на unpaused юзері → рівно 1 (регресія: реальні краші й далі детектуються). Усі 6 перевірок - PASS.

**Деплой (повний, не лише git push - ці два файли живі Appwrite Functions):** `git push` (`ba4806b`) + окремий деплой обох функцій через Appwrite Deployments API (`tar` з `src/`+`requirements.txt`, `POST .../deployments`, `activate=true`).

**Інцидент під час деплою, спричинений мною, знайдений і виправлений у ту ж сесію (не Q):**
1. `heartbeat-watchdog`'s перший новий деплой (`6a5ce2f7...`) зібрався (`status: ready`), але build-крок нічого не встановив - `Module not found: requests` на першому ж живому крон-тику (`14:50:18`, `status: failed`). Причина: у функції рівня `commands` (build-команда) був порожнім (на відміну від `kbg-tg-support-bot`, де стоїть `pip install -r requirements.txt`) - моя POST на створення деплою не передавала `commands` явно, і імпліцитного pip install для Python-рантайму НЕМА.
2. Виправляючи це через `PUT /v1/functions/heartbeat-watchdog` (додав `commands`), ненавмисно **обнулив `scopes`** - Appwrite PUT замінює конфіг функції повністю, а не патчить; я не включив у тіло запиту наявний `scopes: [databases.read, databases.write, ...]`. Наслідок: watchdog падав на КОЖНОМУ тику з `missing scopes ["documents.read"]` - функція взагалі не могла читати БД (fail-safe у бік мовчання, не спаму, але моніторинг був сліпий).
3. Знайдено самостійно (не через скаргу Q) читанням execution-логів після кожного кроку, а не повірою у "status: ready" build'а. Виправлено: (a) новий деплой з явним `commands=pip install -r requirements.txt`; (b) окремий `PUT` що відновив повний `scopes`-список.
4. **Реальне вікно даунтайму watchdog'а: ~`14:52`-`15:00` (13 хв)**, підтверджено логами виконань (`15:00:35` ще `failed`, `15:05:07` вже чистий `completed`) - жоден ручний тригер не використовувався навмисно (щоб не зчинити хибну нотифікацію Q); чекав саме на живий `*/5 * * * *` крон-тик обидва рази.
5. `kbg-tg-support-bot` цим самим способом НЕ зламано - його я лише передеплоїв кодом (POST deployment), налаштування функції (PUT) не чіпав; його execution-лог за весь період - 0 `failed`.

**Урок:** Appwrite `PUT /v1/functions/{id}` - full replace, не partial patch. Перед будь-яким PUT на існуючу функцію - спершу `GET` повний поточний стан і скопіювати ВСІ поля (`scopes` особливо) у нове тіло, не лише ті, що міняєш.

**Підтверджено вживу:** живий `*/5 * * * *` schedule-тик на фінальному деплої (`6a5ce491cd3ef14a6c9d`) відпрацював чисто (`15:05:07`, `status: completed`), без ручного тригера.

**Follow-up того ж дня (Q's ask - потрібна видима кнопка, не лише текстова команда):** додано ряд `⏸️ Призупинити сповіщення` / `▶️ Відновити сповіщення` в `_main_keyboard()` (той самий inline-keyboard, що й `📲 Встановити Vydra`/`🔕 Вимкнути банер`), `callback_data: "pause"/"resume"` заведено через наявний command-twin dispatch (`{"pause": "/pause", "resume": "/resume"}`). Задеплоєно окремо (`4ea17b5` → новий деплой `6a5ceaa0...`, `scopes`/`commands` перевірені цілими - урок з інциденту вище застосований, більше ніякого PUT на функцію). Реального webhook-тесту зробити не міг (TG_WEBHOOK_SECRET - секретна змінна, недоступна на читання) - перевірено лише через прямий виклик `bot.main()` з підробленим `callback_query`-update (4/4 нових перевірок: кнопка ставить/знімає прапорець, шле і ack, і підтвердження) - потрібна ручна перевірка Q в самому Telegram.

## [x] TASK-71: [SECURITY][FIXED] fresh deploy.sh clones inherited the original developer's own telegram_id - фонові heartbeat/entitlement перевірки мовчки цілились би у ЧУЖИЙ Appwrite-акаунт

**Виявлено під час діагностики "нема сповіщення про преміум" на телефоні сина** - `support_config.json` git-tracked, і його `appwrite.telegram_id` завжди був реальним ID Q (оскільки саме з РОБОЧОЇ копії Q цей файл колись закомітили). `deploy.sh` не має ЖОДНОЇ логіки генерації цього файлу - кожен `git clone` просто успадковує його як є. Наслідок для БУДЬ-ЯКОГО майбутнього нового клієнта: `common/support_profile.py`'s heartbeat/entitlement-перевірки мовчки писали б/читали б профіль Q, а не свій власний - без жодної видимої помилки (fail-closed дизайн ковтає це тихо).

**Фікс:**
1. `support_config.json`: `telegram_id` тепер `""` (порожній) у трекованому шаблоні - `common/support_profile.py` вже коректно обробляє порожній `telegram_id` як безпечний fail-closed дефолт, нового коду не знадобилось. Додано явний `_comment_telegram_id`, що попереджає ніколи не комітити сюди реальний ID.
2. Новий `/api/support/link-telegram` (POST, `kbg_web/app.py`) - одноразова локальна прив'язка: валідує (лише цифри), пише в `appwrite.telegram_id`, не чіпає сусідні ключі (`heartbeat_secret` тощо).
3. `/api/support/profile` тепер повертає `telegram_id` (порожній чи заповнений).
4. `dashboard.html`: новий ряд у supportCard - показується, лише якщо `telegram_id` не заповнено; поле вводу + кнопка "Прив'язати".
5. Бот `/start` (обидва випадки - вже зареєстрований і нова реєстрація) тепер показує користувачу його власний числовий Telegram ID для копіювання.

**Тести (реально виконано на живому телефоні Q, не лише прочитано):** `/api/support/profile` → `telegram_id: "6412868393"` (його реальний, кориговано вручну під час merge-конфлікту з git pull, див. нижче); `/api/support/link-telegram` з `"abc"` → 400 з правильним повідомленням; той самий ендпоінт з реальним ID (no-op) → 200, `heartbeat_secret`/`heartbeat_function_id` лишились цілими після запису.

**Побічний інцидент під час деплою на телефон Q (моя провина, виправлено тут-таки):** `git pull --ff-only` відмовився через реальний конфлікт (Q локально мав `heartbeat_secret`+`heartbeat_function_id`, incoming коміт міняв сусідній `telegram_id`) - вирішено через `git stash` → `pull` → `stash pop` → ручне злиття (лишив Q's реальні значення + новий `_comment`). Потім при рестарті Flask **об'єднав kill+launch в одній SSH-команді замість окремих сесій із `setsid`** (порушив власне задокументоване правило) - Flask впав і не піднявся (~2 хв простою, реальний). Виправлено за задокументованою процедурою (окрема сесія, `KBG_WEB_USER`/`KBG_WEB_PASSWORD` явно, `setsid nohup`), підтверджено `HTTP 302`.

**Не зроблено (свідомо, поза скоупом):** окремий вузько-скопований Appwrite-ключ (`documents.read` тільки) замість повноправного адмін-ключа на кожному телефоні клієнта - реальний ризик безпеки, задокументований, але не виправлений цієї сесії. `self-update.sh`/`git-remote-https died of signal 11` resilience - те саме, задокументовано, не виправлено.

## [x] TASK-72: підтримка кількох пристроїв на один Telegram-акаунт (Q's ask - у сина з'явився планшет Samsung)

**Реальний баг, знайдений ще до першого фактичного multi-device користувача:** heartbeat-стан (`active_book_slug`/`progress`/`stage`/`last_heartbeat_ts`) лежав ЄДИНИМ набором полів на документі `users`, ключ пошуку - лише `telegram_id`. Якщо два пристрої під одним акаунтом (телефон + планшет) конвертують РІЗНІ книги одночасно, кожен heartbeat одного пристрою перезаписував стан іншого - watchdog гарантовано плутав/пропускав пристрої.

**Дослідження:** промпт для Gemini (`docs/promt/multidevice-gemini-prompt.md`) + повна відповідь у `Архітектура підтримки кількох пристроїв.txt` - рекомендувала нормалізовану модель (окрема колекція `device_sessions`, one-to-many до `users`), UUIDv4 device_id (без root/ADB/MAC-залежностей), watchdog у форматі "digest" (один диджест на акаунт, не спам по кожному пристрою) - патерн Syncthing/Resilio.

**Реалізація (спрощена відносно чистих Appwrite Relationships - виправдано, деталі в коміті `31b34c0`):**
- Нова колекція `device_sessions` (Appwrite): `device_id` (унікальний індекс), `telegram_id` (денормалізовано, індекс - обійшовся без формальних Relationships, вони не потрібні для наших запитів), `device_alias`, `last_heartbeat_ts` (індекс), `active_book_slug/stage/progress`, `last_stall_alert_ts`.
- `common/device_identity.py` (новий) - `get_or_create_device_id()` (UUIDv4, atomic write, зберігається в Termux $HOME абсолютним шляхом - той самий bind-mount що й `.kbg_appwrite_key`, тому доступний і з-під proot); `get_device_alias()` через `getprop ro.product.model` (без root).
- `common/heartbeat.py` - кожен heartbeat тепер несе `device_id`+`device_alias`.
- `tg-support-bot/_heartbeat()` - upsert у `device_sessions` по `device_id`, більше НЕ пише в `users`.
- `heartbeat-watchdog/main()` - групує stale-сесії по `telegram_id`, будує ОДИН digest на акаунт (усі пристрої в одному повідомленні, ⏸️/▶️ по кожному), account-level `watchdog_paused` глушить усі пристрої разом.

**Тести (реально виконано, 14/14 PASS):** два різні `device_id` під одним `telegram_id` → два незалежні записи, стан не б'ється; повторний heartbeat того самого пристрою → update, не дублікат; один stale+один fresh → рівно один digest, що згадує обидва (перевірено текст); дедуп/re-alert/account-pause - усі регресії тримаються і з кількома пристроями; heartbeat без `device_id` (застарілий виклик) → тихий no-op, не створює запис-колізію на унікальному індексі.

**Деплой (повністю завершено):** Appwrite-схема створена вживу (`device_sessions` + індекси, підтверджено `available`). Код запушено (`31b34c0`), обидві функції передеплоєні (`POST /deployments` тільки, `scopes`/`commands` перевірені цілими), живий крон-тик watchdog'а на новому деплої підтверджено (`20:25:15`, `status: completed`, без ручного тригера). Обидва наявні телефони (Q, син) оновлені й перевірені наживо: `git pull` (обидва мали реальний merge-конфлікт на `support_config.json` з TASK-71 - той самий патерн, вирішено ідентично: stash/pull/pop/ручне злиття), `python3 -m py_compile` чисто, `get_or_create_device_id()`/`get_device_alias()` реально виконані на кожному пристрої (Q: `CPH2653`, син: `CPH2747` - внутрішні модельні коди через `getprop`), Flask НЕ рестартовано на жодному з двох - не потрібно, ці файли не імпортуються в `kbg_web/app.py`.

**Планшет Samsung сина:** ще не підключений (Q лише згадав, що має з'явитись у Tailscale) - жодних спеціальних кроків для нього не треба: щойно deploy.sh/git pull і перший heartbeat відбудуться, `device_identity.py` згенерує йому власний унікальний `device_id` автоматично, і він з'явиться в `device_sessions` як окремий рядок паралельно з телефоном, без жодної ручної реєстрації.

**Побічна дрібниця:** зворотні лапки з `getprop ro.product.model` у тексті commit-message виконались як підстановка команди в shell (bash, без heredoc) - повідомлення коміту трохи "з'їло" ту фразу. Косметично, код не постраждав, не вартий переписування історії.

## [x] TASK-73: безкоштовний ліміт 3 пристрої на акаунт (Q's ідея, продовження TASK-72) + документація multi-device

**Мета:** щойно з'явилась реальна multi-device підтримка (TASK-72) - Q запропонував монетизувати понад 3 пристрої тим самим `cast_registry` ентайтлментом, за встановленим патерном TASK-56 (одна фіча розблоковує все).

**Реалізація:** новий атрибут `over_limit` (boolean) на `device_sessions`. У `tg-support-bot/_heartbeat()` - при створенні СПРАВДІ нового `device_id` (не при update наявного) рахуються існуючі пристрої акаунта; 4-й і далі без `cast_registry` в `entitlements` позначаються `over_limit: true`. Heartbeat НІКОЛИ не відмовляє в записі - генерація локальна й офлайн, ніколи не залежала від досяжності Appwrite за архітектурою Q, тому ліміт може реально обмежити лише crash-моніторинг, не саме використання. `heartbeat-watchdog` виключає `over_limit`-сесії з перевірки stale повністю (жодного Telegram-спаму про пейвол). Натомість `common/support_profile.py`'s нова `get_device_status()` + `/api/support/profile` показують кількість пристроїв прямо в дашборді (де користувач реально дивиться), з попередженням "⚠️ понад безкоштовний ліміт" якщо перевищено.

**Документація:** `docs/uk/usage.md` і `manual.html` отримали розділ "Кілька пристроїв на один акаунт" (як прив'язати через /start + telegram_id у дашборді, ліміт 3). `docs/uk/bot.md`'s список преміум-фіч оновлено + попутно виправлено застарілу згадку "Vision-QA" (мала бути "Агент-редактор" ще з TASK-56 - пропущено при тодішньому переписуванні).

**Тести (реально виконано, 4/4 PASS на новому + 14/14 на регресії TASK-72 без змін):** перші 3 пристрої не позначені; 4-й новий - позначений `over_limit`; повторний heartbeat уже позначеного пристрою - стабільний (не крашить, не дублює рядок); акаунт з `cast_registry` - жоден новий пристрій не позначається незалежно від кількості.

**Деплой (повний):** нова Appwrite-атрибут `over_limit` створена вживу. Код+доки запушено (`972efef`), GitNexus переіндексовано (950 nodes). Обидві Appwrite Functions передеплоєні (`POST /deployments`, `scopes`/`commands` перевірені цілими). **Обидва телефони (Q, син) оновлені, Flask ПЕРЕЗАПУЩЕНО на обох** (цього разу дійсно потрібно - `app.py`/`dashboard.html` змінились) - за задокументованою процедурою (окрема сесія на kill, окрема на launch, `setsid` + явні `KBG_WEB_USER`/`KBG_WEB_PASSWORD` з кожного `.bashrc`). `/api/support/profile` реально перевірено на обох - `device_count: 0` (очікувано, жоден ще не запускав конверсію після оновлення), `device_limit: 3`, обидва акаунти вже мають `cast_registry`.

## [x] TASK-74: фікси за результатами незалежного код-ревʼю (AGY rpi3b, `--mode=plan` read-only)

**Контекст:** Q попросив делегувати рев'ю всього проєкту окремому AGY-інстансу на RPi (rpi4b, помилково задокументований як "rpi3b" - колізія назв). Це виявилось окремим, повноцінним Antigravity CLI (`~/.local/bin/agy`, НЕ голий `gemini`), з тисячами generic-скілів і власними MCP (gitnexus, ai-memory тощо). Значний час пішов на реверс-інжиніринг того, як його правильно запустити headless (`agy -p "ПРОМПТ" --mode=plan` - `--mode=plan` дає read-only на рівні інструменту, не лише промптом) - все задокументовано в скілі `agy-termux` (секція 7) для наступного разу.

**Кожна знахідка перевірена на реальному коді перед застосуванням** (одна - stale-коментар про `vision_qa` в dashboard.html - виявилась хибним спрацюванням: це коректний історичний коментар "чому видалено", не баг - залишено як є).

**Виправлено:**
1. **[Критичний] Пароль злітає після справжнього перезавантаження телефону** - `change_password_api()` писав новий пароль лише в `KBG_WEB_PASSWORD` (`~/.bashrc`). Підтверджено читанням коду `deploy.sh`: `~/.termux/boot/start-services.sh` (справжній Termux:Boot тригер, на відміну від "просто відкрити Termux") виконується з власним shebang НАПРЯМУ, ніколи не читає `.bashrc` - Flask тоді тихо відкочувався на застарілий `web_credentials.json` з першого встановлення. Тепер пише в обидва місця.
2. **[Безпека процесів] Наївний substring-пошук слага книги** в `is_book_process_running` і, серйозніше, у force-kill шляху (`os.kill(SIGKILL)`) - слаг типу "home" збігався з будь-яким запущеним процесом конверсії через сам шлях `/files/home/kindle-butch-gen/...`. Виніс спільну `_find_book_process_pids()` з regex, прив'язаним до `--book <slug>` або `/books/<slug>` конкретно. **Перший варіант фіксу сам мав ту саму діру** (загальна `/`-межа все ще збігалась із "/home/" в стандартному шляху Termux) - спіймано реальним тестом (7 кейсів), не здогадом.
3. **Чотири неатомарні `open(path, "w")` записи JSON** (`global_settings.json`, per-book config двічі, і `support_config.json` з моєї ж власної TASK-71) - винесено в спільний `_atomic_write_json()` (tmp+fsync+os.replace), той самий патерн, що вже був у `common/device_identity.py`.
4. **`/delete_my_data` не чистив `device_sessions`** (TASK-72 з'явився пізніше цієї команди) - реальна прогалина GDPR для команди, чий сенс - видалити все.
5. **"Телефон ще не прив'язаний"** → "Цей пристрій ще не прив'язаний" (dashboard, бот, docs, manual) - написано до TASK-72, дивно звучить для планшета.

**Свідомо не зроблено цієї ночі (позначено, не забуто):** дублювання парсингу `config.json` у `translate_manga.py` (дрібний I/O-нюанс, але цей файл - критичний генераційний пайплайн, dev-184 не має cv2/torch щоб перевірити повний прогін); адмін-ключ на клієнтських пристроях (той самий ризик, що я вже сам зафіксував раніше цієї сесії - потребує архітектурного рішення, не швидкого патчу); override-прапорець для RAM-діагностики в `deploy.sh` і батчинг N+1-запиту у watchdog (реальні, але низька терміновість при поточних 2 акаунтах, ризиковано чіпати непротестований шлях вночі без нагляду).

**Деплой:** запушено (`14194b0`), GitNexus переіндексовано (954 nodes), `kbg-tg-support-bot` передеплоєно. Обидва телефони оновлено, Flask перезапущено (той самий безпечний протокол). **Явно НЕ тестував `change_password_api` наживо** (ризик заблокувати Q вночі без можливості підтвердити новий пароль) - лише синтаксис + базовий health-check після рестарту.

**Безпека телефонів під час деплою:** підтверджено `llama-server` на обох телефонах уже працював ДО будь-яких моїх дій сьогодні (той самий PID на телефоні Q, що й на самому початку сесії) - мій рестарт торкався лише Flask, ніколи не запускав важкі моделі. На телефоні сина в момент перевірки виявлено реальну активну конверсію (frieren-beyond-journey-s-end-v01, 55% CPU, 5+ хв) - явно вже йшла до мого рестарту (Flask-рестарт не чіпає відв'язані conversion-підпроцеси), не втручався.


## [x] TASK-75: критичний баг - зникала кнопка "Додати книгу" через незакритий `<details>` (знайдено AGY rpi3b, статичне UI/UX-ревʼю v3)

**Контекст:** Q попросив прогнати AGY rpi3b по UI/UX з іменованими скілами (ui-review, ux-audit, design-ux, frontend-code-review, uxui-principles, accessibility), не лише по коду. Дві перші спроби (живий headless-браузер через agent-workspace/chrome-win) провалились через нестабільність цього тулінгу (access-denied на скріншотах, timeout навігації, агент витратив бюджет часу на самодіагностику власного Chromium) - третя спроба (v3) використала статичний аналіз розмітки/CSS (read-only git clone) із застосуванням критеріїв скілів до HTML напряму, чесно позначено в звіті як "судячи з розмітки" - і спрацювала (звіт 14КБ, `docs/promt/2026-07-20-agy-ui-review-v3.md`).

**Знахідка перевірена на РЕАЛЬНОМУ живому `dashboard.html` на dev-184 перед застосуванням** (не довіряв звіту сліпо): `<details>` відкривався в формі "Додати книгу" перед полем "Автор(и)" (для секції "Детальніше, не обов'язково"), але НІКОЛИ не закривався тегом `</details>` до кінця форми. Через це весь HTML-парсер браузера вважав checkbox "Це манґа / комікс" і саму кнопку `➕ Додати книгу` частиною згорнутого блоку "Детальніше" - вони не рендерились взагалі, доки користувач не здогадається розкрити необов'язковий блок. Перевірено: жодного CSS (`details[open]`) чи JS, що примусово розкривало б блок за замовчуванням - реальний, непом'якшений баг, що блокував головний сценарій "додати книгу" для будь-кого, хто не клікнув на опціональний "Детальніше".

**Фікс:** одна закриваюча `</details>` вставлена одразу після select "Мова перекладу" (`#lang`), перед чекбоксом манги - тепер "Детальніше" згортає лише Автор(и)/Мова оригіналу/Мова перекладу, а чекбокс манги і кнопка сабміту завжди видимі (ймовірно, оригінальний задум структури).

**Верифікація перед деплоєм:** підрахунок тегів (`<details>`/`</details>`/`<form>`/`</form>`) на всьому файлі збалансований (3/3, 2/2 - два інші `<details>` у файлі, у налаштуваннях книги, вже мали коректні закриваючі теги); `diff` між живим файлом dev-184 і локальною виправленою копією показав РІВНО один доданий рядок.

**Деплой:** файл оновлено на dev-184 (`~/projects/kindle-butch-gen/kbg_web/templates/dashboard.html`).

**Свідомо не зроблено цієї ночі (позначено, не забуто):** решта ~10 знахідок з того самого v3-звіту (конкуруючі CTA, `lang="en"` замість `lang="uk"`, відсутні aria-label на іконках-кнопках, тач-таргети менші за 44px, контраст плейсхолдерів, хаотичне перенесення кнопок хедера на мобільному, ризик випадкового натискання "видалити" поруч із "завантажити", змішування англійської/української в модалках, різна висота кнопок, хардкод hex-кольорів замість CSS-змінних) - жодна не є severity-blocking як ця, потребують окремого проходу при денному світлі з живим переглядом, не вночі наосліп по одній лише розмітці.

## [x] TASK-76: сканування персонажів (NER) - жива полоска прогресу % + лог знизу + мініатюра сторінки на кожного персонажа

**ЗАВЕРШЕНО (2026-07-20, живий тест на телефоні Q):**
Реалізовано AGY rpi3b (`--mode=accept-edits`, перший не-review запуск, гілка `task-76-cast-scan-progress`), пере­глянуто і зміцнено Claude перед мержем (коміт `c331772`) - знайдено й виправлено реальний баг: `run_llm()`'s стрімінг-цикл перевіряв 1800с дедлайн лише ПЕРЕД кожним `proc.stdout.read(64)`, а сам read блокується без обмеження - зависла модель ніколи не впиралась у таймаут. Виправлено через `select.select()`-обмежені read'и, підтверджено окремим regression-тестом (реальний subprocess, що ніколи нічого не пише в stdout, скорочений дедлайн 2с) - спрацьовує рівно за ~2.00с, не висне. Також відновлено два коментарі, які AGY's edit-tool загубив під час своєї боротьби з правками (не функціонально, але Chesterton's-fence контекст), додано валідацію `char_id` в thumbnail-ендпоінті (defense-in-depth).

**Живий баг, знайдений ПІСЛЯ мержу через реальний тест на телефоні Q:** `character_thumbnail_api` падав з `ModuleNotFoundError: No module named 'PIL'` - Flask працює в голому Termux Python, а Pillow був лише в proot Ubuntu-контейнері (для важкого пайплайну). Встановлено `python-pillow` через `pkg` (Termux prebuilt, 41с, без компіляції - НЕ через pip, той самий урок що вже задокументований у deploy.sh для контейнерної версії). Додано в `deploy.sh` (коміт `acfcd2c`) для всіх майбутніх клієнтів.

**Повна жива верифікація на телефоні Q (реальні дані, не фікстури):** `/scan-progress` → 200; `/scan/stop` → 200; валідація `char_id` (сміттєвий ввід → 400, traversal → 404 від самого Flask-роутингу); мініатюра з РЕАЛЬНОЇ сторінки книги "frieren" (`source/*.png`, вже наявні на диску) → реальний 108KB JPEG, 480×720, RGB; повторний запит → mtime не змінився (кеш працює, не перегенеровує). Тестові артефакти (characters.json, thumbnail) прибрані після перевірки.

**Не перевірено наживо:** RAR/CB7/PDF-гілки `get_manga_page_image()` (AGY додав їх поза початковим скоупом - деградують безпечно через try/except у 404, якщо `7z`/`pdftoppm` відсутні, але жодна реальна книга в цій сесії не була в цих форматах, щоб перевірити).

**Деплой:** master `acfcd2c`, GitNexus переіндексовано. Телефон Q - повністю оновлено й перевірено наживо. **Планшет сина - ЗАПЛАНОВАНО на сьогодні (Q's явний запит), ще не виконано.**

**Контекст:** Q запустив "Сканувати персонажів (NER)" (кнопка `#cast-scan` на вкладці "🧬 Cast & Context", `kbg_web/templates/stages.html`) і побачив тільки статичний toast "Сканування персонажів запущено (кілька хвилин)" - жодного індикатора активності, поки телефон реально гріється й модель рахує. Три конкретні вимоги від Q (одним повідомленням, послідовно):
1. Полоска прогресу з відсотками.
2. Живий лог процесу знизу (те, що зараз пише LLM/скрипт).
3. Мініатюра-зразок зображення біля кожного знайденого персонажа - "щоб агент та людина мали розуміння хто мається на увазі" (тільки для манги, звісно - текстові книги зображень не мають).

**ВАЖЛИВО - режим виконання:** це ПЕРШИЙ раз, коли AGY rpi3b працює НЕ в `--mode=plan` (read-only рев'ю), а в `--mode=accept-edits` (реальні правки). Явно дозволено Q цього разу ("делегуй завдання agy raspberry"). ОБОВ'ЯЗКОВО працюй в ІЗОЛЬОВАНОМУ клоні/гілці, НЕ в канонічному робочому дереві на dev-184 (`~/projects/kindle-butch-gen`), і НЕ пуш в `master` напряму - запуш у нову гілку `task-76-cast-scan-progress`. Claude (я) переглядає diff і мержить/деплоїть сам, як з усіма попередніми задачами цієї сесії.

**Файли, з яких почати (вже досліджено, не вигадуй заново):**
- `bin/cast_ner_prepass.py` - сам скрипт сканування. `main()` (рядки ~99-142): один блокуючий `run_llm()` виклик (рядки 61-67, `subprocess.run(..., capture_output=True, timeout=1800)`) до `llama-cli -m <gemma3-4b> -c 8192 -n 1024 ... -no-cnv -p PROMPT+text`. Дані про персонажів приходять з `collect_text()` (рядки 39-58) - читає `book_dir/bubbles_meta/*.json` (по сторінках), кожен bubble має `original_text` І `page`-контекст у самому імені файлу (`bubbles_meta/<page_stem>.json`, див. `write_bubbles_meta()` у `translate_manga.py:713`, поле `"page": page_name` записується в bubble-entry на рядку ~652 того ж файлу).
- `kbg_web/app.py`: `characters_scan_api` (рядки 1435-1478) - POST-ендпоінт, що детаче запускає `cast_ner_prepass.py` через `subprocess.Popen(..., start_new_session=True)`, пише лог у `book_dir/edits/ner_scan.log`.
- `kbg_web/templates/stages.html`: `renderCastTab()` (рядки 2466-2614) - таблиця персонажів (`rows`, рядки 2506-2519) і кнопка сканування (`#cast-scan`, обробник рядки 2572-2606, вже має 10-секундний polling `/api/agent-editor/status/${slug}` для `ner_running` - можна перевикористати цей самий loop, зменшивши інтервал під час активного сканування, а не писати паралельний окремий).
- `common/cast_registry.py`: `_characters_path()`, `load_characters()`/`save_characters()` (атомарний tmp+`os.replace()` вже є - той самий патерн треба повторити для нового progress-файлу, БЕЗ спільного helper - це другий випадок такого патерну в проєкті, третій буде привід для абстракції, не раніше).
- `common/book_paths.py`: `resolve_book_paths()` - НЕМАЄ явного ключа для директорії "сирі/typeset PNG сторінки манги". Перше, що треба зробити - знайти, де реально лежать PNG-сторінки манги на диску під час/після конвертації (перевір `translate_manga.py` навколо `process_page()` (рядок ~1045) і будь-який `output_dir`/`batches_dir`-подібний шлях для PNG per-page файлів, а не лише фінальний `.cbz`). Якщо сторінки існують лише всередині CBZ-архіву (не як окремі файли на диску) - треба буде розпакувати потрібну сторінку "на льоту" в мініатюру (PIL `Image.open` з ZipFile), а не покладатись на директорію файлів.

**Що зробити:**

1. **Progress-файл** (`book_dir/edits/ner_scan_progress.json`, атомарний запис tmp+`os.replace`) - `cast_ner_prepass.py` пише його на кожному етапі:
   - `{"stage": "читання тексту", "percent": 5, "log_tail": []}` - одразу на старті `main()`.
   - Під час LLM-виклику: переписати `run_llm()` з блокуючого `subprocess.run(capture_output=True)` на `subprocess.Popen` зі стрімінгом stdout рядок-за-рядком (або чанками); оновлювати progress-файл періодично (напр. раз на ~1-2 сек або кожні N символів, НЕ на кожен символ - це I/O на телефоні) з `percent` - евристична оцінка на основі кількості згенерованих символів проти очікуваного `-n 1024` токенів (напр. `min(90, 5 + 85 * chars_so_far / (1024*4))`), і `log_tail` - останні ~40 рядків сирого виводу моделі (для UI-логу знизу).
   - Після LLM: `{"stage": "обробка результатів", "percent": 92}`.
   - Якщо манга - крок пошуку сторінки-зразка для кожного персонажа (див. п.3 нижче): `{"stage": "пошук зображень", "percent": 96}`.
   - Фінал: `{"stage": "завершено", "percent": 100, "done": true}` - залишити файл (не видаляти), щоб UI встиг показати "100% готово" перед тим як зупинити polling. Наступний запуск сканування перезапише його заново.
   - При помилці/винятку - `{"stage": "помилка", "percent": <останній відомий>, "error": "<короткий опис>", "done": true}` (обгорни `main()` тіло в try/except, щоб UI бачив помилку, а не вічний "зависла" індикатор).

2. **Новий read-only ендпоінт** `GET /api/characters/<slug>/scan-progress` у `kbg_web/app.py` (той самий entitlement-гейт `is_entitled('cast_registry')`, той самий `validate_slug`, поруч із `characters_scan_api`) - читає й повертає `ner_scan_progress.json` як JSON; `{"stage": null, "percent": 0}` якщо файл не існує (сканування ще не запускалось) - НЕ 404, щоб фронтенду не треба було спеціально обробляти помилку.

3. **Мініатюра-зразок персонажа (тільки манга):**
   - У `cast_ner_prepass.py`: під час `collect_text()`/парсингу персонажів - треба ЗНАТИ, на якій сторінці вперше згадується кожне ім'я (зараз `collect_text()` просто конкатенує весь текст, втрачаючи прив'язку до сторінки - це треба виправити, зберігаючи `(page_stem, original_text)` пар замість голого рядка).
   - Для кожного `drafted`-персонажа (у `parse_characters()`, рядки 70-96) - знайти першу сторінку, де зустрічається будь-яке з `name_source`, зберегти як нове поле `"sample_page": "<page_stem>"` у записі персонажа (розширення схеми `characters.json`, зворотно сумісне - старі записи просто не матимуть цього поля).
   - Новий ендпоінт `GET /api/characters/<slug>/thumbnail/<char_id>` у `kbg_web/app.py` - той самий entitlement-гейт; знаходить персонажа за `id` у `characters.json`, бере його `sample_page`, генерує ЗМЕНШЕНУ мініатюру (макс. ширина ~480px, PIL `Image.thumbnail()`) з відповідної сторінки манги (звідки саме - залежить від п.4 дослідження вище: або окремий PNG-файл, або витягнути сторінку з CBZ-архіву), КЕШУЄ результат у `book_dir/edits/thumbnails/<char_id>.jpg` (генерувати ОДИН РАЗ, не на кожен запит - важливо для теплового бюджету телефону, який Q явно згадав). Якщо `sample_page` відсутній або файл не знайдено - 404 (фронтенд просто не рендерить `<img>`, без плейсхолдера-помилки).

4. **Фронтенд (`stages.html`, `renderCastTab()`):**
   - Новий стовпець зліва в таблиці персонажів (`rows`, рядок 2506) - `<img src="/api/characters/${slug}/thumbnail/${c.id}" style="width:48px;height:48px;object-fit:cover;border-radius:6px;" onerror="this.style.display='none'">` (тихий fallback через `onerror`, не JS try/catch - простіше і надійніше для відсутньої мініатюри).
   - Полоска прогресу + % + прокручуваний `<pre>` лог-бокс - додати ПІД кнопкою `#cast-scan`, показувати лише поки триває сканування. Перевикористати вже наявний `setInterval` polling-цикл в обробнику `#cast-scan` (рядки 2594-2604, зараз кожні 10 сек питає `ner_running`) - додати туди ж (або паралельний швидший, якщо 10 сек занадто рідко для плавної полоски - вирішуй сам, орієнтир ~2 сек) виклик нового `/api/characters/${slug}/scan-progress`, оновлювати ширину/текст полоски й вміст лог-боксу. Зупинити polling коли `done: true` або `ner_running` стає false.

**Верифікація (ОБОВ'ЯЗКОВО реальна, не лише читання коду - той самий стандарт що й у решти сесії):**
- Запусти сканування на РЕАЛЬНІЙ тестовій книзі-манзі (можна взяти будь-яку вже наявну з `bubbles_meta` на dev-184, якщо є, або створи мінімальний фікстур-набір `bubbles_meta/*.json` вручну для тесту без реального LLM-виклику - головне перевірити прогрес-файл пишеться коректно на кожному етапі, а не лише "має сенс на вигляд").
- Перевір `ner_scan_progress.json` реально оновлюється під час виконання (не тільки на старті й в кінці) - прочитай файл 2-3 рази з паузою під час роботи скрипта, підтверди зростання `percent`.
- Перевір `GET .../scan-progress` повертає коректний JSON на кожному етапі, і `{"percent": 0}` коли файлу ще нема.
- Перевір мініатюра реально генерується (файл з'являється в `edits/thumbnails/`), і повторний запит НЕ перегенеровує (кеш працює - перевір mtime файлу не змінюється між двома запитами).
- Якщо не вдалось знайти прив'язку сторінки в реальних даних (напр. на тестовій книзі bubbles_meta порожній) - чесно напиши в коміт-повідомленні що саме не протестовано наживо і чому, НЕ вигадуй "працює" без доказу.

**Коміт:** окремий, з чітким описом всіх трьох частин + що реально перевірено. НЕ до `master` - у гілку `task-76-cast-scan-progress`, `git push origin task-76-cast-scan-progress`.

**Diary:** запиши в AGY diary щось на кшталт `"SESSION:2026-07-20|TASK-76:branch-pushed|commit:<hash>|branch:task-76-cast-scan-progress"` після завершення.

**ОНОВЛЕННЯ TASK-76 (до старту AGY, виправлення+доповнення):**
1. Номери рядків вище були зняті зі СТАРОГО локального клону (`/home/vokov/workspace/kbg`, застарілий на ~20 задач) - виявлено й виправлено. `characters_scan_api` реально на рядку ~1478 в `kbg_web/app.py` (не 1435), `renderCastTab`/`cast-scan` в `stages.html` - номери рядків збіглись, ці коректні. **Шукай функції за іменем (grep), не покладайся на точні номери рядків з цього файлу.**
2. **Нова вимога від Q (4-та, надійшла після початкового опису):** зараз НЕМАЄ способу зупинити сканування з UI взагалі - Q довелось просити мене зупинити процес вручну через SSH. Потрібна кнопка старт/стоп, за зразком вже наявного патерну для агента-редактора (`bin/agent_editor.py`):
   - Backend: новий `@app.route("/api/characters/<slug>/scan/stop", methods=["POST"])` в `kbg_web/app.py`, скопіюй патерн `agent_editor_stop_api` (рядок ~1656): `subprocess.run(["pkill", "-f", "cast_ner_prepass.py"], capture_output=True)` + `subprocess.run(["pkill", "-f", "llama-cli"], capture_output=True)` (УВАГА: саме `llama-cli`, НЕ `llama-mtmd-cli` - те інший бінарник, той для agent_editor) + `_clear_active_conversion_state(slug)` (та сама вже наявна функція, що й у `agent_editor_stop_api`).
   - Frontend (`stages.html`, `renderCastTab`): заміни одну кнопку `#cast-scan` на пару кнопок за зразком `#agent-start-btn`/`#agent-stop-btn` (рядки ~755-756 у тому ж файлі, шукай `startAgentScan`/`stopAgentScan` для повного патерну toggle-видимості) - "▶️ Сканувати персонажів" видима завжди, "⏹ Зупинити" прихована (`display:none`) поки не активне сканування, показується коли `ner_running:true` (з існуючого polling `/api/agent-editor/status/${slug}` - ЦЕЙ ендпоінт вже повертає `ner_running`, перевір чи він рахує саме `cast_ner_prepass.py`-процес, не плутай з агентом).
3. Ці pkill-виклики виконуються ВСЕРЕДИНІ Flask-процесу (subprocess.run напряму, НЕ через SSH) - самозбіг command-line тут НЕ загрожує (на відміну від виконання `pkill -f` через SSH-сесію, де воно може вбити саму SSH-сесію - те, з чим Claude зіткнувся сьогодні на іншій задачі). Тут це безпечно, як і в уже наявному `agent_editor_stop_api`.

## [ ] TASK-77: часткове відновлення персонажів при зупинці сканування (Q's ask, follow-up до TASK-76)

**Контекст:** У TASK-76 з'ясувалось, що `cast_ner_prepass.py` зараз НІЧОГО не зберігає до самого кінця - `save_characters()` викликається РІВНО ОДИН РАЗ, після повного завершення одного блокуючого виклику LLM. Це означає: зупинка сканування (вручну через SSH, як зробив Claude 2026-07-20, або майбутньою кнопкою "⏹ Зупинити" з TASK-76) ЗАВЖДИ втрачає весь прогрес - навіть 10+ хвилин роботи моделі пропадають повністю, доводиться сканувати заново з нуля. Q підтвердив, що хоче це покращити.

**ЗАЛЕЖНІСТЬ - НЕ виконувати паралельно з TASK-76:** ця задача будує на новому `/api/characters/<slug>/scan/stop` ендпоінті, якого ще немає (TASK-76 його щойно додає). Виконувати ТІЛЬКИ після того, як TASK-76 змержено в master і задеплоєно - інакше конфлікт правок у тому самому файлі/гілці.

**Ідея реалізації (не остаточна - AGY/Claude мають право скоригувати після дослідження):**
- Повний сирий вивід моделі вже й так пишеться в `book_dir/edits/ner_scan.log` (через `subprocess.Popen(cmd, stdout=lf, ...)` в `characters_scan_api`, `kbg_web/app.py`) - незалежно від стрімінгу, що додає TASK-76 для прогрес-бару. Це вже готове джерело для відновлення.
- У новому `/api/characters/<slug>/scan/stop` (з TASK-76) - ПІСЛЯ `pkill`, ПЕРЕД поверненням відповіді: прочитати `ner_scan.log`, спробувати ПОБЛАЖЛИВО витягнути окремі валідні JSON-об'єкти персонажів навіть якщо зовнішній масив `[...]` не закрився (regex на рівні окремих `{...}`-блоків, а не `json.loads()` всього виводу - той факт, що `-n 1024` обірвав генерацію на середині, майже гарантовано ламає JSON-масив, але ОКРЕМІ вже згенеровані об'єкти всередині часто повні).
- Успішно розпарсені об'єкти зберегти в `characters.json` як `auto_drafted`, з додатковим полем `"ner_partial": true` (чесно позначити, що це з перерваного скану - QA-gate і так вимагає ручної верифікації для будь-якого `auto_drafted`, тож ризику для перекладу немає, це суто економія часу користувача при повторному скануванні).
- Повідомлення у відповіді стоп-ендпоінту: якщо щось врятовано - "Зупинено. Знайдено N персонажів у незавершеному скані (потребують перевірки)."; якщо нічого - просто "Зупинено." (без вигаданих чисел).

**Верифікація:** реальний тест з ОБРІЗАНИМ/неповним JSON-виводом (сконструюй фікстуру - валідний масив, обірваний посередині третього об'єкта) - підтвердь, що перші 2 повних об'єкти рятуються, третій (обрізаний) коректно ігнорується без падіння процесу.

**Коміт:** окремий, після TASK-76.

## [x] TASK-78: ручне завантаження зображення персонажа (Q's follow-up до TASK-76)

**Контекст:** одразу після живого тесту TASK-76 Q помітив, що мануально доданий персонаж (кнопка "➕ Додати вручну") взагалі не має шляху отримати картинку - `sample_page` заповнюється тільки NER-сканом.

**Реалізація:** `POST`/`DELETE /api/characters/<slug>/thumbnail/<char_id>` в `kbg_web/app.py` - пишуть/чистять той самий кеш-файл (`edits/thumbnails/<char_id>.jpg`), який GET-хендлер з TASK-76 вже перевіряє першим. Завдяки цьому upload автоматично "перекриває" авто-визначену мініатюру без жодного окремого прапорця "manual vs auto" - GET просто віддає найновіше. DELETE повертає до авто-визначення (або до "нічого", якщо `sample_page` немає). Ліміт розміру файлу 15MB (це джерело мініатюри, не документ - окремо від значно більшого ліміту `parse-metadata`), `img.load()` всередині try/except примусово декодує зображення одразу, щоб зіпсований/не-зображення файл падав одразу з чіткою помилкою, а не десь пізніше.

Фронтенд (`stages.html`): клік на мініатюру (в БУДЬ-ЯКОГО персонажа, не лише мануального) відкриває file picker, маленьке посилання "↺" під нею скидає до авто-версії.

**Живий тест на телефоні Q (реальні дані):** створив реальний JPEG (800×600) → `POST .../thumbnail/char_manual_test123` → `{"status":"success"}` → `GET` того ж ендпоінту одразу після → HTTP 200 (файл реально збережений і роздається) → `DELETE` → `{"status":"success"}`, файл реально видалений з диску (перевірено `ls`) → `GET` після видалення → HTTP 404 (коректно, для тестового id немає `sample_page`, тож нема куди відкочуватись). Тестові артефакти прибрані.

**Деплой:** master `a8a67f1`, GitNexus переіндексовано, телефон Q оновлено й перевірено наживо. **Планшет сина - разом з TASK-76, заплановано на сьогодні.**

## [x] TASK-79: прибрати зайве тертя/неточні формулювання в діалогах про стан памʼяті моделей (Q's ask, живий аудит)

**Контекст:** Q наштовхнувся на `confirm()` перед скануванням персонажів, що звучало як "сервер перекладу зараз щось перекладає" - хоча насправді модель просто лишається завантаженою в памʼяті після використання (для швидкості), незалежно від активної роботи. Q запитав: чому взагалі турбувати клієнта таким службовим повідомленням, якщо відповідь завжди "так, звісно"? Далі попросив перевірити через GitNexus, чи є ще такі місця.

**Знайдено й виправлено:**
1. `characters_scan_api` (409-повідомлення) + фронтенд-`confirm()` при скануванні персонажів - повністю прибрано модальне вікно (у ньому не було реального "ні", тільки "почекати ще один клік"), скан тепер сам зупиняє/запускає модель мовчки, з інформативним текстом у статус-рядку замість блокуючого діалогу. Заразом виправлено формулювання ("Сервер перекладу...зараз займає" → "Модель перекладу...завантажена в памʼяті", щоб не звучало як активна робота) - і синхронізовано regex на фронтенді, що парсить це повідомлення (інакше автоматичне зупинення/перезапуск тихо зламалось би).
2. `startAgentScan()` (агент-редактор) - інший `confirm()`, залишено (несе реальну корисну інформацію - попередження про нагрів телефону + пояснення механізму підтвердження пропозицій), але виправлено ОДИН неточний пункт: текст стверджував "сервер перекладу БУДЕ зупинений" безумовно, хоча код одразу під ним перевіряє `st.llama_running` і зупиняє лише за потреби.

**Аудит:** переглянуто кожен `confirm()` в обох шаблонах (`stages.html`, `dashboard.html` - там жодного). Решта (незбережені зміни, backfill-regenerate, зупинка сканування) - справжні вибори з реальними наслідками, не чіпав.

**Деплой:** master `48814f8`, GitNexus переіндексовано, телефон Q оновлено.

## [x] TASK-80: обмежити сканування персонажів і агента-редактора діапазоном сторінок + кнопка агента з переглядача сторінки (Q's ask)

**ЗАВЕРШЕНО (2026-07-20, живий тест на телефоні Q):**
Реалізовано AGY rpi3b (`--mode=accept-edits`, гілка `task-80-page-range-scoping`, значно чистіший прогін ніж TASK-76 - жодних "restore"-циклів в дії AGY цього разу). Переглянуто і зміцнено Claude перед мержем (коміт `66d56c9`).

**Реальний баг, знайдений при рев'ю:** `preview_manga()` (Viewer-вкладка) сортує список сторінок через `natsorted()` (природний порядок - page2 перед page10), а нові `--page-start`/`--page-end` в `cast_ner_prepass.py`/`agent_editor.py` використовували звичайний `sorted()` (лексикографічний) на тому самому `bubbles_meta`-гліб. Для тестової книги (zero-padded `p000`/`p001`...) обидва сортування випадково збігаються, але на книзі з не-доповненими нулями іменами файлів кнопка "Агент на цю сторінку" мовчки цілила б у НЕПРАВИЛЬНУ фізичну сторінку. Виправлено на `natsorted()` в обох місцях (вже наявна залежність - `app.py` вже імпортує її для тієї ж причини). Доведено окремим regression-тестом на фікстурі `page1/page2/page10/page11` (реально запущено на телефоні Q, не лише прочитано) - стара версія клала `page10.json` туди де мала бути `page2.json`, нова - коректно.

**Живий тест на реальних даних телефону Q (не фікстури):** `collect_text(..., page_start=2, page_end=3)` на реальній книзі "frieren" → рівно 2 сторінки (коректно); `get_page_number()`-резолвер на РЕАЛЬНОМУ `quality_flags.json` (51 кейс) → реалістичні номери сторінок (16, 165-180...), фільтр за діапазоном 5-10 коректно повернув порожній список (у книзі немає кейсів у цьому діапазоні - очікувано).

**Деплой:** master `db6231c`, GitNexus переіндексовано, телефон Q оновлено й перезапущено, `llama-server` не зачеплено.

**Не перевірено наживо:** реальний LLM-виклик зі звуженим діапазоном (немає моделі в тестовому оточенні під час рев'ю - як завжди для цієї категорії задач), клік по кнопці "🤖 Агент на цю сторінку" в реальному браузері.

**Контекст:** обидві важкі LLM-операції (сканування персонажів NER, агент-редактор QA-виправлень) завжди проходять по ВСІЙ книзі/перших N сторінках з початку - Q хоче мати змогу обмежити діапазоном сторінок (наприклад, якщо знає, що проблема лише в главі 5), і додати кнопку запуску агента прямо з перегляду конкретної сторінки (Viewer-вкладка), а не лише з глобальної кнопки.

**ВАЖЛИВО - режим виконання:** `--mode=accept-edits`, ІЗОЛЬОВАНИЙ клон+гілка `task-80-page-range-scoping`, НЕ пуш у master. Claude перегляне diff і зробить мерж+живий деплой сам (той самий процес, що й TASK-76).

**Дослідження вже зроблено (реальні дані з канонічного репо dev-184, НЕ зі старого локального клону - раніше цієї ночі вже була помилка зі застарілим клоном, перевіряй за іменами функцій, не сліпо за номерами рядків):**

1. **`bin/cast_ner_prepass.py`**: `collect_text(book_dir, pages)` (біля рядка 74) зараз бере `meta[:pages]` (перші N файлів `bubbles_meta/*.json`, відсортованих за іменем - завжди з початку). `main()`'s `argparse` має `--pages` (default 50), АЛЕ `characters_scan_api` в `kbg_web/app.py` НІКОЛИ його не передає - завжди дефолт.
   - Додай `--page-start`/`--page-end` (1-indexed, як бачить користувач, необов'язкові - якщо відсутні, поведінка як зараз, повна зворотна сумісність) до `argparse`. `collect_text()` - замість `meta[:pages]` роби `meta[start_idx:end_idx]` (переведи 1-indexed в 0-indexed зріз списку `meta`, який вже відсортований за іменем файлу - природний порядок сторінок).
   - `characters_scan_api` (POST `/api/characters/<slug>/scan`) - прийми `page_start`/`page_end` з `request.get_json()` (необов'язкові), прокинь у `cmd` як `--page-start`/`--page-end`, якщо задані.
   - Фронтенд (`stages.html`, `renderCastTab`, кнопка `#cast-scan`) - два необов'язкові `<input type="number">` поля (напр. "з сторінки" / "по сторінку") поруч із кнопкою сканування, порожні за замовчуванням (= вся книга, як зараз). Значення читай і передавай у POST-body при кліку.

2. **`bin/agent_editor.py`**: `main()` (рядок ~332) фільтрує `cases = [f for f in flags if f.get("reason") in FLAG_REASONS]` - усі позначені кейси книги, кожен має `case.get("page")` (рядок з файлу, напр. `"p007"` чи схоже - подивись реальний формат у `quality_flags.json` якщо є тестова книга, або в тому ж файлі де `page` вперше присвоюється кейсу). Є `--limit` (кількість), НЕМАЄ фільтра за сторінкою взагалі.
   - Додай `--page-start`/`--page-end` (той самий 1-indexed підхід, необов'язкові) - фільтруй `cases` за `case.get("page")` ПІСЛЯ побудови списку, ПЕРЕД лічильником `--limit` (щоб ліміт рахувався від відфільтрованих, не загальних).
   - `agent_editor_scan_api` (`POST /api/agent-editor/scan/<slug>`, рядок ~1940) - прийми ті ж поля з `request.get_json()`, прокинь у `cmd`.
   - Фронтенд (`stages.html`, `startAgentScan()` ~рядок 806, і елементи в тому ж блоці керування агентом) - той самий стиль двох необов'язкових number-полів поруч із кнопкою "▶️ Запустити пошук".

3. **Нова кнопка з Viewer-вкладки (окремий сценарій - "з перегляду конкретної сторінки"):** у Viewer-вкладці (де рендериться поточна сторінка, `currentPageBubbles`/`selectedBubbleId`, шукай навколо рендеру самої сторінки - там має бути поточний номер/ідентифікатор відкритої сторінки в JS-змінній) - додай кнопку "🤖 Агент на цю сторінку" (чи схожу), яка викликає ТОЙ САМИЙ `agent_editor_scan_api`, але з `page_start=page_end=<поточна сторінка>` (діапазон з однієї сторінки). Кнопка активна лише коли `enable_agent_editor` увімкнено для книги (той самий гейт, що вже є для головної кнопки агента) і має пройти той самий `confirm()` (з TASK-79 виправленим текстом), що й головна кнопка.

**Верифікація (реально, не на слово):**
- Реальний тест `collect_text()` з фікстурними `bubbles_meta/*.json` (кілька файлів) - підтвердь, що `--page-start 2 --page-end 3` бере РІВНО файли 2 і 3 (1-indexed), не 1 і не 4.
- Реальний тест фільтра `agent_editor.py`'s cases за page-range на фікстурному `quality_flags.json` з кейсами на різних сторінках - підтвердь, що поза діапазоном кейси не потрапляють у список для обробки.
- Синтаксис (`python3 -m py_compile`) обох python-файлів і повного `app.py`.
- Чесно вкажи в коміт-месседжі, що НЕ протестовано наживо (як завжди - немає моделі/телефону в цьому середовищі).

**Коміт:** окремий, у гілку `task-80-page-range-scoping`, НЕ в master.

## [x] TASK-81: видима піктограма для порожньої мініатюри персонажа (Q's скріншот)

**Контекст:** Q надіслав скріншот - обвів зону де мала бути мініатюра персонажа (TASK-78), там повністю порожньо (img.onerror лише ховає сам себе, нічого не показує замість), видно тільки крихітну "↺" поруч, схожу на випадкову стрілку без пояснення.

**Виправлено:** пунктирна рамка з іконкою 📷-плейсхолдером заповнює слот, поки не завантажиться реальне зображення (перемикання через `img` `load`/`error`-події, не inline-атрибути - треба координувати ще й з посиланням скидання). "↺" перейменовано на "↺ скинути" і приховано за замовчуванням - з'являється тільки коли є що скидати.

**Деплой:** master `17555f0`, GitNexus переіндексовано, телефон Q оновлено й перезапущено. Живого браузерного тесту не було (немає доступу до реального рендеру) - логіка перевірена лише читанням коду й diff.

**Побічний інцидент:** перша спроба закомітити впала через unmatched-quote в багаторядковому commit-message всередині SSH-команди - ЦІЛИЙ блок (включно зі scp, який стояв РАНІШЕ в тому ж виклику) не виконався взагалі через помилку парсингу bash (не часткова невдача - парсинг падає на всьому скрипті одразу). Витратив зайвий раунд, доки не зрозумів що файл реально не задеплоївся. Виправлено через `git commit -F <файл>` окремим кроком - тепер стандартна практика, задокументована в [[feedback_git_commit_via_file]].

## [x] TASK-82: стадії прогресу для манги на дашборді (за чекбоксами) + фікс 100%-багу і зайвого пульсування (Q's ask, скріншот)

**ЗАВЕРШЕНО (2026-07-20, живий тест на телефоні Q):**
Реалізовано AGY rpi3b - вперше делегував ЙОМУ і аналіз, і кодування (не лише реалізацію за готовою специфікацією), з явним переліком скілів у промпті (`python-development`, `clean-code`, `backend-development-feature-development`, `test-driven-development`, `code-review-and-quality`). AGY самостійно дослідив `translate_manga.py`'s pipeline, визначив 3 обов'язкові стадії манги (Детекція/Переклад/Типографіка) + 3 опціональні залежні від чекбоксів (Cast Registry, Тон, Агент-редактор), написав реальний TDD-набір тестів (`test_calculate_progress.py`, 3 сценарії).

**Реальний, серйозний баг знайдений при рев'ю:** `list_books()` (`/api/books` - ендпоінт, що живить увесь дашборд, часто опитується) викликав `is_entitled("cast_registry")` ВСЕРЕДИНІ циклу по книгах - ця функція робить справжній мережевий HTTPS-запит до Appwrite щоразу. Бібліотека з N книг = N послідовних мережевих викликів, перш ніж дашборд взагалі міг щось показати - пряме порушення власного архітектурного принципу проєкту "генерація не залежить від доступності хмари" (тут йшлось не про генерацію, а про сам ДАШБОРД, що ще гірше - зависання UI при недоступному/повільному Appwrite). Виправлено - виклик один раз ПЕРЕД циклом, той самий результат для всіх книг (це акаунт-рівневий ентайтлмент, не book-специфічний).

**Живі тести на реальних даних телефону Q:**
- `python3 -m unittest test_calculate_progress` → 3/3 PASS (на dev-184 І на самому телефоні).
- Реальний виклик `calculate_progress()` на РЕАЛЬНИХ книгах: "frieren" (манга, `generate_audiobook: false`) → `overall_percent: 100.0` (раніше застрягало б нижче); "vibe-programming" (текстова, `generate_audiobook: true`, справді ще генерує озвучення) → `overall_percent: 98.9` (коректно, НЕ баг - аудіо справді ще не готове).
- `GET /api/books` (реальний HTTP-запит, `time` показав 0.886с - швидко, продуктивний фікс підтверджено) → усі нові поля коректні: `overall_percent` вкладено в `progress`, `enable_cast_registry`/`enable_bubble_tone`/`enable_agent_editor` правильно відображають реальний конфіг книги "frieren" (усі `true`, бо Q реально їх увімкнув раніше цієї сесії).

**Відоме обмеження (не виправлено, задокументовано):** стадії манги (Cast/Тон/Агент/Детекція/Переклад/Типографіка) усі мають ОДИН спільний active/completed стан, похідний від єдиного `manga_percent` - на відміну від текстових книг, де є конкретний `activeStage`. Причина: cast-scan і агент-редактор - незалежно запущені процеси, поточна модель даних не відстежує "яка саме стадія манги йде зараз" окремо. `_heavy_state()` (вже в коді) технічно має потрібну гранулярність (`ner`/`agent`/`conversion` окремо) - можливе покращення на майбутнє, не сьогодні.

**Деплой:** master `cb65a61`, GitNexus переіндексовано, телефон Q оновлено й перезапущено, `llama-server` не зачеплено.

**Не перевірено наживо:** візуальний рендер стадій-лейблів і пульсації в реальному браузері (немає доступу до GUI в цьому середовищі).

**ВАЖЛИВО - режим виконання:** цього разу Q явно попросив делегувати AGY і АНАЛІЗ, і КОДУВАННЯ (не лише реалізацію за готовою специфікацією, як у TASK-76/80) - тому нижче НАВМИСНЕ немає точних номерів рядків чи готового дизайну рішення, лише опис симптомів + де шукати. `--mode=accept-edits`, ІЗОЛЬОВАНИЙ клон+гілка `task-82-manga-stages-100pct`, НЕ пуш у master - Claude сам перегляне diff і задеплоїть, як завжди.

**СКІЛИ (застосуй їхню методологію під час аналізу й реалізації - Q підтвердив, що це варто робити завжди, не лише для UI-ревʼю):** `python-development`, `clean-code`, `backend-development-feature-development`, `test-driven-development`, `code-review-and-quality` (само-рев'ю власного diff перед комітом).

**Контекст:** на дашборді (`kbg_web/templates/dashboard.html`) картка ЗВИЧАЙНОЇ (текстової) книги показує ряд лейблів стадій над прогрес-баром ("Текст", "Переклад", "Наголоси", "Звук") - користувач бачить, через які фази проходить конвертація. Картка МАНГА-книги показує лише голий прогрес-бар з відсотком і "Перекладано сторінки: X із Y", без жодних стадій-лейблів. Q хоче, щоб манга теж мала стадії.

**Знахідки з попереднього дослідження (стартові точки, не остаточна специфікація):**
- `kbg_web/status_helper.py:calculate_progress()` (рядки ~127-346) - вже рахує `marker_percent`/`translation_percent`/`stress_percent`/`tts_percent` для звичайних книг, і окремо `manga_percent` (той самий один % на всі 4 поля) для манги - манга зараз НЕ має окремих стадій у самому розрахунку прогресу, лише в переклад-циклі сторінок.
- `kbg_web/templates/dashboard.html` - рендерить лейбли стадій для звичайних книг (шукай "Текст"/"Переклад"/"Наголоси"/"Звук" у розмітці картки книги).
- Config-прапорці книги (`config.json`) вже існують по-стадійно: `generate_audiobook`, `enable_cast_registry` (з `common/cast_registry.py`, premium-gated), `enable_agent_editor` (premium-gated), `enable_bubble_tone` (з `common/support_banner.py`'s `is_heavy_scene` чи схоже - манго-специфічний, безкоштовний для всіх). Ці прапорці й мають визначати, які стадії показує UI - "всі стадії мають мати чекбокс" (Q).

**Вимога 1 (косметика, стадії для манги):**
- Дослідити, які реальні "фази" проходить манга-конвертація (детекція бульбашок/OCR, переклад, типографіка/рендер сторінки - подивись `translate_manga.py`'s основний pipeline) і додати відповідні лейбли стадій на манга-картку дашборду, за тим самим візуальним патерном що вже є для звичайних книг.
- Список показаних стадій має залежати від чекбоксів книги (`config.json`) - якщо `enable_cast_registry`/`enable_agent_editor`/`enable_bubble_tone` увімкнені для ЦІЄЇ книги, відповідна стадія теж зʼявляється в списку; якщо вимкнено - немає. Преміум-only стадії (Cast Registry, Агент-редактор) не повинні зʼявлятись у списку для непреміум користувача навіть якщо якимось чином увімкнені в config.json (fail-closed, як і решта преміум-гейтів у проєкті - подивись `is_entitled('cast_registry')` патерн).
- "Всі стадії мають мати чекбокс" - переконайся, що для КОЖНОЇ стадії, яку показує UI (текст/manga-специфічні/аудіо/наголоси/cast/agent), існує реальний чекбокс десь у налаштуваннях книги, яким користувач може її увімкнути/вимкнути. Якщо для якоїсь стадії такого чекбокса ще нема (напр. типографіка манги завжди обовʼязкова, не опціональна) - це нормально, не кожна стадія повинна бути опціональною, але тоді вона просто завжди показана, без чекбокса. Задокументуй яке рішення прийняв і чому.

**Вимога 2 (реальний баг, не косметика):**
Q's опис: "у звичайній книзі, новелі, аудіо не генерується з самого початку (`generate_audiobook: false` для цієї книги від початку) - не треба рахувати озвучення/наголоси в прогрес взагалі, це правильно НЕ генерувати - але тоді загальний відсоток застрягає нижче 100%, хоча всі УВІМКНЕНІ стадії насправді завершені." Тобто: `calculate_progress()` має рахувати % тільки серед УВІМКНЕНИХ для цієї книги стадій, а не завжди ділити на всі 4 (marker/translation/stress/tts) незалежно від того, чи стадія взагалі мала відбутись.
- Знайди, де підсумковий % картки на дашборді агрегується з `marker_percent`/`translation_percent`/`stress_percent`/`tts_percent` (ймовірно десь у `dashboard.html`'s JS, бере ці 4 поля й якось усереднює/показує) - виправ так, щоб вимкнена стадія (`generate_audiobook: false` → stress/tts не рахуються) не тягнула загальний % вниз.
- Другий пункт того самого багу: "іконка процесу не має пульсувати, пульсують лише активні стадії" - зараз (за словами Q, реально бачив у скріншоті) іконка/стадія продовжує пульсувати навіть коли книга вже на 100% (чи коли ця конкретна стадія взагалі вимкнена й ніколи не працювала). Пульсація має позначати "зараз реально йде робота над цією стадією", а не бути статичною декорацією. Знайди CSS/JS, що керує пульсацією (ймовірно клас типу `.active`/`.pulse` в dashboard.html), і прив'яжи її до реального активного стану (стадія в процесі виконання ЗАРАЗ), не просто "стадія в принципі існує для цієї книги".

**Верифікація (реально, наскільки дозволяє середовище):**
- Реальний тест `calculate_progress()`-логіки (чи де саме буде виправлення) з фікстурою книги де `generate_audiobook: false` - підтверди що фінальний % = 100, коли marker/translation готові, незалежно від відсутності stress/tts файлів.
- Синтаксис/структура всіх змінених файлів.
- Чесно вкажи, що НЕ перевірено наживо (як завжди - немає браузера/телефону в цьому середовищі для мангa-специфічної частини).

**Коміт:** окремий, у гілку `task-82-manga-stages-100pct`, НЕ в master.

## [x] TASK-83: виправити "мертвий конфіг" TTS (Tier 1, безкоштовний bugfix)

**ЗАВЕРШЕНО (2026-07-20, живий тест на телефоні Q):**
Реалізовано AGY rpi3b (гілка `task-83-tts-dead-config-fix`, з явними скілами й GitNexus-нагадуванням у промпті).

**Supertonic 3:** `noise_scale`/`noise_w` більше НЕ надсилаються в payload взагалі (реально нема куди - `OfflineTtsConfig` цього рушія не має таких полів), відповідні UI-слайдери деактивовані для цього рушія.

**StyleTTS2:** захисний шар трансляції в `bin/tts_helper.py` - `sess.get_inputs()` перевіряється ПЕРЕД тим, як намагатись передати `diffusion_steps`/`t`/`embedding_scale`/`alpha`/`beta`, safe no-op якщо модель їх не підтримує.

**Реальна перевірка Claude ПЕРЕД мержем, яка виправила план:** пряма інтроспекція живої StyleTTS2 ONNX-моделі на телефоні Q (`sess.get_inputs()`) показала - модель має ЛИШЕ `tokens`/`speed`/`s_prev`, жодного з quality-контролів, які припускало дослідження Gemini. Backend-код лишається (безпечний, автоматично запрацює, якщо колись з'явиться інша збірка моделі), але фронтенд ("Voice Quality" dropdown для StyleTTS2) прибрано повністю - Q підтвердив: не показувати контроль, який нічого не робить. Другий коміт AGY (той самий процес делегування - "делегуй наступні кроки AGY") прибрав dropdown, повернув хардкод `'medium'`, прибрав мертвий JS - чистий diff, нуль залишкових посилань (перевірено grep'ом).

**Живі тести на телефоні Q (реальні дані):**
- Реальний виклик `bin/tts_helper.py` (StyleTTS2, `voice_quality=high, noise_w=0.9` через stdin) → реальний валідний WAV (1.87с, 24kHz mono) - новий захисний код не ламає генерацію.
- `GET /` (реальна сторінка) → 0 входжень "Voice Quality" в HTML.
- `GET /api/books` → `vibe-programming` (styletts2), `frieren` (supertonic3) - реальні книги з різними рушіями підтверджують коректну гейт-логіку.

**Деплой:** master `0a5b287`, GitNexus переіндексовано, телефон Q оновлено й перезапущено, `llama-server` не зачеплено.

**Джерело:** Gemini deep-research (`/storage/emulated/0/Documents/kindle-butch-gen/promt/`, звіт "Дослідження Покращень Проєкту Vydra") - той самий баг, який Claude вже знайшов самостійно раніше цієї ночі (TASK-82 review), тепер з точним технічним поясненням і рішенням.

**Діагноз (з дослідження, перевір сам перед правкою):** `tts_voice_quality`/`tts_noise_scale`/`tts_noise_w` зчитуються з `config.json`, передаються в payload з `audio_stage.py` в `bin/tts_helper.py`, але:
- **Supertonic 3 (sherpa-onnx)** використовує архітектуру Self-Purifying Flow Matching, а НЕ VITS - `OfflineTtsConfig` для цієї моделі взагалі не має полів для noise-параметрів. Це ДЕТЕРМІНОВАНИЙ рушій, керується лише `speaker_id`/`speed`. Передача noise-параметрів - синтаксично проігнорована операція на найнижчому рівні бібліотеки, не баг Python-коду, а фізична відсутність підтримки в рушії.
- **StyleTTS2** (onnxruntime, style diffusion) РЕАЛЬНО підтримує контроль якості, але через ІНШУ номенклатуру: `diffusion_steps` (5-30, компроміс якість/швидкість), `alpha`/`beta` (вага акустичного стилю/просодії), `embedding_scale`, `t` (рівень шуму).

**Що зробити:**
1. Для Supertonic: прибрати передачу `noise_scale`/`noise_w` з payload зовсім (нема куди), в UI деактивувати відповідні повзунки коли обрано Supertonic (10 жорстко визначених голосів, єдиний реальний контроль - вибір квантованої моделі int8/fp16 при інсталяції, не runtime-параметр).
2. Для StyleTTS2: в `bin/tts_helper.py` додати шар трансляції - `tts_voice_quality` лінійно на `diffusion_steps` (напр. "Low"→5, "High"→20), `tts_noise_w` на `t` (діапазон 0.6-1.0) або `embedding_scale`.

**Верифікація:** реальний тест генерації короткого WAV-чанку з різними значеннями `tts_voice_quality`/`tts_noise_w` для StyleTTS2 - підтвердь, що вихідний файл РЕАЛЬНО відрізняється (різний diffusion_steps параметр видно у виклику), не просто "код компілюється".

**СКІЛИ:** `python-development`, `clean-code`, `debugging-toolkit`, `code-review-and-quality`.

**Режим:** `--mode=accept-edits`, ізольована гілка `task-83-tts-dead-config-fix`, НЕ пуш у master.

---

## [x] TASK-84: Cast Registry для звичайних текстових книг (Tier 1, преміум)

**ЗАВЕРШЕНО (2026-07-20, живий тест на телефоні Q, найчистіший diff AGY цієї сесії):**

**Головне архітектурне питання розв'язано правильно:** `collect_text()` раніше для текстових книг читала ВЖЕ ПЕРЕКЛАДЕНИЙ текст (курка-яйце проблема, знайдена й задокументована Claude перед делегуванням). Тепер 5-рівнева пріоритетна система: `merged_source_<lang>.md` (EPUB) → сирі PDF-батчі без `_translated_` (з підтримкою `page_start`/`page_end`) → загальний `merged_source_*.md` → переклад як останній fallback.

**Реалізовано:** `common/utils.py`'s `translate_text`/`translate_text_hy_mt2`/`translate_segment_with_retry` отримали опціональні `cast_rules`/`book_dir` (за замовчуванням `None` - повна зворотна сумісність), переюзали вже наявну `cast_registry.py`-машинерію (той самий fail-closed premium+opt-in гейт, що й манга) замість дублювання логіки. Обидва реальні виклик-сайти (`translate_epub.py`, `translate_stage.py` - підтверджено, це ВСІ виклики в репо) оновлені. Нова вкладка "🧬 Cast & Context" для текстових книг у `stages.html`, переюзає ту саму `renderCastTab()`, що й манга. Bubble-tone UI коректно захований для не-манга книг (`isManga`-перевірка, підтверджено - shared module-scope змінна, без бага скоупу).

**Незалежна перевірка Claude (не просто повірив звіту AGY):**
- 3 власні фікстурні тести (пріоритет джерела, виключення перекладених батчів, фільтр діапазону сторінок) - запущені на РЕАЛЬНОМУ коді на телефоні Q (окремий тестовий клон, не живий репо) - усі 3 PASS.
- Живий тест на РЕАЛЬНІЙ книзі Q ("vibe-programming", `merged_source_ru.md` реально існує) - `collect_text()` коректно повернула ОРИГІНАЛЬНИЙ російський текст ("Ван Вэньцзе... Вайб-программирование...") замість українського перекладу - точний доказ, що курка-яйце проблема реально вирішена, не лише в теорії.
- `GET /view/vibe-programming` (жива сторінка) → 6 входжень `tab-cast`/"Cast & Context" - нова вкладка реально присутня на сторінці справжньої текстової книги.

**Деплой:** master `be2868b`, GitNexus переіндексовано (з двома self-heal відновленнями через WAL/worker-нестабільність сервісу - не блокуюче, варто пізніше перевірити стан GitNexus-контейнера), телефон Q оновлено й перезапущено, `llama-server` не зачеплено.

**Не перевірено наживо:** реальний LLM-виклик (NER-скан + переклад з інжектованими правилами) - немає моделі/`llama-server` в тестовому оточенні під час рев'ю.

**Джерело:** той самий Gemini-звіт, розділ "Інтеграція Cast Registry для морфологічної узгодженості".

**Ідея:** перенести вже наявний NER-патерн (`bin/cast_ner_prepass.py`, зараз тільки для манги) на EPUB/PDF-книги. Auto-draft на перших 3-5 розділах тексту (Gemma 3 4B текстова, без mmproj), той самий JSON-реєстр персонажів (рід/стилістика), той самий QA-gate (auto_drafted → verified вручну). Динамічна ін'єкція правил через regex зі словесними межами (`\bName\b` для латиниці) в промпт `Hy-MT2-7B` - той самий Lightweight RAG підхід, що вже працює для манги, суто CPU-side пошук, нуль додаткового навантаження на модель під час масового перекладу.

**Не починати без**: перевірки, чи `cast_ner_prepass.py`'s `collect_text()` вже має гілку для текстових книг (звіт каже "має бути", але перевір реальний код - можливо вже частково готово, судячи з `common/heartbeat.py`'s коментаря "text book -> first chunk of merged markdown").

**КРИТИЧНЕ УТОЧНЕННЯ до TASK-84 (Claude, перед делегуванням AGY):** перевірено `collect_text()` в `bin/cast_ner_prepass.py` - гілка для текстових книг ДІЙСНО вже існує:
```python
for cand in glob.glob(os.path.join(book_dir, "translated", "merged*_*.md")) + \
            glob.glob(os.path.join(book_dir, "translated", "*.md")):
    try:
        return open(cand, encoding="utf-8").read()[:15000], []
    except Exception:
        continue
```
АЛЕ ця гілка читає `merged*_*.md` з `translated/` - тобто **ВЖЕ ПЕРЕКЛАДЕНИЙ** текст (українською), не оригінал! Це створює архітектурну проблему "курка-яйце": щоб інжектити правила роду персонажів У ПЕРШИЙ прохід перекладу, скан має відбутись НА ОРИГІНАЛІ (ще недоступному в перекладеному вигляді до завершення перекладу). Поточна гілка придатна лише для РЕТРО-сканування вже перекладеної книги (виправити майбутні перевидання/перечитування), не для типового сценарію "новий текстовий проєкт, скан ПЕРЕД масовим перекладом".

**Дослідницьке питання для AGY (частина завдання, не готова відповідь):** чи існує проміжний файл з ОРИГІНАЛЬНИМ (не перекладеним) текстом до старту перекладу - подивись `common/book_paths.py`'s `resolve_book_paths()`, `run_conversion_batches.py`'s marker-конвертацію (PDF/EPUB → markdown, mavelib output ДО translate-етапу), можливо `merged_source_<lang>.md` (бачив натяк на цю назву в `status_helper.py`). Якщо такий файл існує на етапі ПІСЛЯ marker-конвертації, АЛЕ ДО translate-етапу - саме туди треба додати нову гілку `collect_text()`, а не покладатись на вже наявну (яка читає ПЕРЕКЛАД, не оригінал). Задокументуй знайдене архітектурне рішення в комміт-месседжі чітко - це найважливіша частина цього завдання, помилка тут зробить всю фічу марною для типового нового користувача.

---

## [x] TASK-85: Емоційний тон через Chain-of-Thought тег (Tier 1, безкоштовно)

**ЗАВЕРШЕНО (2026-07-20, живий тест на телефоні Q):**

**Побічна подія:** AGY rpi3b вперше за сесію вперся у реальну квоту ("Individual quota reached... Resets in 1h52m26s") - ПІСЛЯ того, як реалізував і сам протестував рішення, але ПЕРЕД комітом/пушем. Claude знайшов реальну незакомічену роботу в робочій директорії (не втрачено), переглянув diff, самостійно прогнав повний тест-набір на окремому одноразовому клоні на телефоні Q (не повірив на слово), і сам довів справу до кінця (коміт → push через SSH-remote → merge → деплой), оскільки AGY був недоступний ще ~2 години.

**Реалізовано:** промпт `Hy-MT2-7B` (обидва формати, 7B і не-7B) переформульовано так, щоб дозволити ОДИН структурований `<tone_analysis>` тег ПЕРЕД перекладом (модель коротко визначає емоційний регістр - neutral/aggressive/melancholic/suspense), а заборону "без пояснень" застосовано лише ПІСЛЯ цього блоку - вирішує пряму суперечність зі старим промптом, знайдену Claude ще до делегування. Нова `clean_translation_text()` вирізає тег з відповіді ПЕРЕД валідацією/збереженням - обробляє закриті теги, незакриті, кілька тегів, відсутність тегу (якщо модель проігнорувала інструкцію), порожній ввід - без падінь і без сміття в фінальному перекладі.

**Свідомо поза скоупом:** generic (не-Hy-MT2) fallback-гілка `translate_text()` не займана - цей шлях в проєкті ніколи не використовується (Hy-MT2 - єдина реально задеплоєна модель перекладу).

**Живі тести (реально, не на слово):** 7/7 тестів (`test_tone_analysis_cutting.py`) пройдено ДВІЧІ - на окремому одноразовому клоні (незалежна перевірка Claude ще до мержу) і на живому репо телефону Q (після деплою). Покриття: нормальний тег, відсутність тегу, незакритий тег, кілька тегів, порожній/None ввід, тег без вмісту, і мокований end-to-end тест, що підтверджує і зміст промпту, надісланого серверу, і очищений вихід.

**Деплой:** master `f80684e`, GitNexus переіндексовано, `common/utils.py` оновлено на телефоні Q. Рестарт Flask НЕ потрібен - цей модуль використовується лише окремими скриптами конвертації (`translate_epub.py`/`translate_stage.py`), що запускаються заново при кожній конвертації, не імпортується Flask-процесом напряму.

**Не перевірено наживо:** реальний виклик `llama-server` з новим промптом (жодна конвертація не запускалась цієї ночі для перевірки).

**Знайдено додатково (не задача цієї ночі, але варте уваги):** на телефоні Q (він же OnePlus 13) вже давно встановлений і авторизований ОКРЕМИЙ інстанс AGY rpi3b-подібного Antigravity CLI (акаунт `arsen111999@gmail.com`, `agy` в `$PREFIX/bin`, версія 1.0.16, з жовтня-червня активним використанням) з уже готовим GitNexus MCP (той самий dev-184 інстанс) - можна використовувати як запасний варіант, коли rpi3b на квоті. RAM на телефоні тісний (14GB загалом, лише ~2.2GB available через резидентний `llama-server`), варто бути обережним з важкими задачами там.

**Ідея:** додати `<tone_analysis>` XML-тег у системний промпт `Hy-MT2-7B` - модель спочатку коротко визначає емоційний регістр абзацу (neutral/aggressive/melancholic/suspense) в ТОМУ Ж виклику інференсу, перед власне перекладом. Нуль додаткових апаратних ресурсів (лише трохи більше вихідних токенів). Аналог bubble-tone для манги, без окремої моделі.

**КРИТИЧНЕ УТОЧНЕННЯ до TASK-85 (Claude, перед делегуванням AGY, з живого рев'ю TASK-84):** `translate_text_hy_mt2()` в `common/utils.py` зараз для ОБОХ форматів моделі (7B і не-7B) явно каже моделі "Output ONLY the translation, no explanations, no commentary" / "Output only the translation, no explanations". Це ПРЯМО суперечить ідеї додати `<tone_analysis>`-крок ПЕРЕД перекладом - модель отримає суперечливі інструкції.

**Що треба зробити правильно:**
1. Переформулювати промпт так, щоб ЯВНО дозволити один структурований `<tone_analysis>...</tone_analysis>` блок ПЕРЕД перекладом, а заборону "без пояснень/коментарів" застосувати ТІЛЬКИ до частини ПІСЛЯ цього блоку (напр.: "First, in a single <tone_analysis> tag, briefly state the emotional register of this passage (neutral/aggressive/melancholic/suspense) in a few words. Then, after closing the tag, output ONLY the translation with no further explanation or commentary.").
2. **ОБОВ'ЯЗКОВО:** відповідь моделі тепер міститиме І тег `<tone_analysis>...</tone_analysis>`, І сам переклад. Перед тим, як `translate_text_hy_mt2()` поверне результат виклику (чи в `translate_text()`, де б не був фінальний return) - тег та його вміст МАЮТЬ БУТИ ВИРІЗАНІ (regex, напр. `re.sub(r'<tone_analysis>.*?</tone_analysis>\s*', '', raw, flags=re.DOTALL)`) з відповіді ПЕРЕД тим, як вона піде далі в `validate_translation_segment()` (перевірка заголовків/плейсхолдерів) чи збережеться як переклад. Якщо цього не зробити - текст тону потрапить У ФІНАЛЬНИЙ ПЕРЕКЛАД як сміття, зламавши і валідацію, і сам текст книги.
3. Подумай, чи `stop_tokens` (`["<|eos|>", ...]` для 7B, `["<|hy_User|>", ...]` для не-7B) потребують коригування - модель має ЗАВЕРШИТИ tone_analysis тег і продовжити генерацію перекладу в ТОМУ Ж виклику, стоп-токени не повинні обірвати генерацію РАНІШЕ, ніж модель дійде до кінця блоку тону.
4. Реально протестуй (мок LLM-відповідь із tone-тегом + текстом перекладу) - regex-вирізання коректно прибирає тег і залишає ЛИШЕ переклад, включно з edge-кейсами (тег відсутній у відповіді - модель проігнорувала інструкцію; тег не закритий; кілька тегів).

---

## [x] TASK-86: ASR-петля верифікації наголосів (дослідження + standalone-модуль)

**ЗАВЕРШЕНО (2026-07-20, живий тест на телефоні Q) - з реальним інцидентом посеред виконання:**

**Побічна подія:** делегування на телефон Q вперше зіткнулось з тим, що ЦЯ конкретна збірка `agy` (телефон, версія 1.0.16) не підтримує `--mode` взагалі - лише бінарний вибір: інтерактивне підтвердження (зависло б у headless-режимі) чи `--dangerously-skip-permissions`. Q підтвердив використання останнього. Одразу після завершення роботи Android вбив Termux (той самий термальний/OOM ризик, задокументований для цього пристрою) - але коміт УЖЕ був на диску до вбивства, нічого не втрачено. `git push` окремо не вдався (SSH-ключ з passphrase, ssh-agent не запущено) - Claude переніс коміти через `git bundle` (телефон → релей через dev-184), зберігши оригінальне авторство, а не переписав історію.

**Знахідка кращу за заплановану:** дослідження (без компіляції - за вимогою безпеки) показало, що `whisper.cpp` пакета в Termux немає, компіляцію свідомо НЕ запускали. Але виявили: `sherpa_onnx` 1.13.4 вже встановлений на телефоні Q і підтримує Whisper ASR напряму через `OfflineRecognizer.from_whisper()` (та сама бібліотека, що вже використовується для Supertonic TTS) - усуває всю проблему "компілювати whisper.cpp на термально крихкому телефоні" повністю. Не активовано - потребує окремого підтвердження Q щодо завантаження ONNX-моделі Whisper Small (~244MB).

**Реалізовано:** `common/asr_verify.py` (standalone, НЕ підключений до `audio_stage.py` - свідомо, чекає підтвердження) - Левенштейн, char_error_rate, `transcribe()` (subprocess-обгортка, параметризований шлях до бінарника, не потребує його існування при імпорті), `build_mismatch_flag()` (формат як `quality_flags.json`), fail-safe `verify_chunk()` (будь-яка помилка ASR → позначається для ручної перевірки, ніколи не тихо ковтається).

**Виправлено Claude перед мержем:** `append_to_stress_queue()` мала звичайний `open(path, "w")` - замінено на атомарний запис (tmp+`os.replace`), той самий патерн що вже є в проєкті (`_atomic_write_json`, `save_progress`) - саме той тип збою (kill посеред запису), що реально стався цієї ж задачі трохи раніше.

**Живі тести (реально, тричі):** 30/30 тестів пройдено на телефоні (прогін AGY), знову на телефоні після мого хардненгу, і знову на dev-184 після перенесення через bundle. Плюс реальний (не мокований) функціональний тест `append_to_stress_queue` - 2 реальні виклики → коректна черга з 2 елементів, жодного залишкового `.tmp`-файлу.

**Деплой:** master `774ce5b`, GitNexus переіндексовано, телефон Q оновлено (рестарт Flask не потрібен - модуль ще нікуди не підключений).

**Не зроблено (свідомо, потребує окремого підтвердження Q):** завантаження реальної ONNX Whisper-моделі, підключення до `audio_stage.py` pipeline.

**Джерело:** вже була ідея в TASKS.md, тепер деталізована. Модель **Whisper Small** (244M, ~852MB RAM, квантована) - оптимальний компроміс (Tiny/Base занадто неточні для омографів наголосу, Medium/Large виходять за RAM-бюджет). Строго послідовна фаза: LLM перекладу вивантажується → TTS синтезує чанк (Supertonic, ~66M) → `whisper-cli` транскрибує → порівняння з оригіналом (Левенштейн) → `mismatch_flag` в чергу `/api/edit/stress` для ручного підтвердження. Сумарний пік пам'яті на цій фазі ~1.2GB - безпечно.

**Залежність:** потребує встановлення `whisper.cpp` (компіляція чи pkg, як llama.cpp зараз) - дослідити спершу, чи є прекомпільований шлях для Termux/ARM, перш ніж писати task-специфікацію для AGY.

**ОБМЕЖЕННЯ БЕЗПЕКИ до TASK-86 (виконується на телефоні Q - OnePlus 13, той самий пристрій, що вже має документовану термальну проблему при довгих компіляціях/важких процесах без охолодження):**

1. **НЕ намагайся реально скомпілювати whisper.cpp** на цьому пристрої в рамках цього завдання. Лише ДОСЛІДИ: чи є прекомпільований пакет у Termux pkg-репозиторії (`pkg search whisper` чи схоже), чи є готовий ARM64-бінарник для завантаження (не компіляції). Якщо ні готового пакету, ні готового бінарника немає - чесно задокументуй це, НЕ запускай `make`/`cmake --build`/будь-яку компіляцію з нуля.
2. Реалізуй python-логіку ASR-петлі (порівняння тексту через Левенштейна, побудова `mismatch_flag`, структура черги) як окремий, тестований модуль - тести MОЖУТЬ мокати виклик `whisper-cli` subprocess (не потребують реального бінарника для юніт-тестів).
3. Якщо резидентно вже працює `llama-server` (перевір `ps aux | grep llama-server` ПЕРЕД стартом) - не намагайся зупиняти чи перезапускати його, просто врахуй цей факт у документації (реальний RAM-бюджет для послідовної фази).
4. Якщо ЩОСЬ у цьому завданні виглядає так, ніби вимагатиме реального важкого локального процесу (компіляція, завантаження великої моделі, тестовий inference-прогін) - ЗУПИНИСЬ і задокументуй це як "потребує окремого підтвердження Q" в комміт-месседжі, НЕ виконуй.

---

## [BLOCKED] TASK-87: Voice Cloning з ASR-валідацією якості запису (Tier 2, преміум)

**ЗАБЛОКОВАНО (2026-07-20, перевірено Claude перед делегуванням, реальний код, не гіпотеза):**

`bin/tts_helper.py:383`: `s_prev = np.load(style_path).astype(np.float32)` - реально задеплоєний StyleTTS2 ONNX-експорт завантажує ОДИН статичний вектор стилю з `models/styletts2/style.npy` ПРИ ЗАВАНТАЖЕННІ МОДЕЛІ, той самий для всіх книг/користувачів. Немає жодного коду для видобування вектора стилю з ДОВІЛЬНОГО референс-аудіо - модель просто не приймає такий вхід (той самий клас обмеження, що вже знайдено для diffusion/noise-контролів у TASK-83: ONNX-експорт спрощений/зафіксований, а не той самий, що в оригінальному дослідницькому проєкті StyleTTS2).

**Отже:** "zero-shot voice cloning" з ідеї Gemini-дослідження НЕ реалізовуване простим Python-кодом на цій моделі - потрібен або інший ONNX-експорт з реальним style-encoder входом (дослідницьке ML/export завдання, не "додати обгортку"), або інший TTS-рушій із вбудованою підтримкою клонування голосу.

**Рішення Q (2026-07-20):** пропустити TASK-87 зараз, перейти до TASK-88. Не видалено з TASKS.md - лишається задокументованим блокером на майбутнє, якщо colись з'явиться придатна модель.

**Ідея:** користувач записує 10-20с свого голосу через Web UI, `whisper.cpp Small` перевіряє якість дикції/шуму (порівняння з еталонним текстом, WER-поріг), якщо ОК - `speaker_embedding` кешується ОДИН РАЗ на всю книгу (не перераховується на кожен чанк - усуває дрейф тембру І економить CPU) для StyleTTS2's zero-shot voice cloning.

---

## [x] TASK-88: MQM семантична валідація перекладу через reflection

**ЗАВЕРШЕНО (2026-07-20, живий тест) - перший AGY-прогін цієї ночі БЕЗ жодного знайденого бага при рев'ю.**

Чисте виконання на телефоні Q (`--dangerously-skip-permissions`) - без Termux-kill цього разу, без несподіванок. AGY сам, без нагадування, застосував атомарний запис (tmp+`os.replace`) для `translation_quality_flags.json` - урок з рев'ю TASK-86 прижився. Push через SSH не вдався (той самий passphrase-ключ) - AGY ПРОАКТИВНО підготував `git bundle` сам, точно за інструкцією - перенесено через dev-184 релей.

**Реалізовано:** `common/mqm_review.py` (standalone, не підключений до `translate_segment_with_retry` - той самий патерн, що TASK-86) - `mqm_review()` робить НЕЗАЛЕЖНИЙ stateless `/completion`-виклик до тієї ж `Hy-MT2-7B` з промптом-критиком (не перекладачем), багаторівневий парсинг JSON-відповіді (прямий → regex → markdown-fence → fail-safe), score clamp [1,10], поріг запису 7/10, `review_and_record()` - готовий high-level entry-point для майбутньої інтеграції.

**Підтверджено уточнення Claude перед делегуванням:** "очищення контексту" не знадобилось як окрема технічна дія - `mqm_review()` просто ще один незалежний `/completion`-виклик, точно як і передбачалось.

**Живі тести (реально, двічі):** 47/47 тестів пройдено на телефоні (AGY) і знову на dev-184 (Claude, незалежно). Плюс реальний нефункціональний тест `should_write_flag`/`append_quality_flag` - поріг спрацьовує коректно (score=4/accept=False → пишеться, score=9/accept=True → пропускається), атомарний запис підтверджено (жодного залишкового `.tmp`).

**Деплой:** master `3b2d79f`, GitNexus переіндексовано, телефон Q оновлено (рестарт не потрібен - модуль ще не підключений нікуди).

**ІНТЕГРАЦІЮ ТА НАЛАШТУВАННЯ ЗАВЕРШЕНО (2026-07-21, AGY Architect audit + integration verification):**
- **Підключення**: `_maybe_mqm_review()` підключено у `translate_segment_with_retry` (common/utils.py) як opt-in шар. Прапорець `"enable_mqm_review"` додано до `config.json` книги та веб-інтерфейсу налаштувань (`dashboard.html`). Дефолт для нових книг — `False`.
- **Fail-safe**: Підтверджено наживо та інтеграційними тестами (`test_mqm_pipeline_integration.py`, 4/4 PASS): при таймауті, обриві мережі або невалідному JSON MQM-виклик ловиться як non-blocking застереження, пишеться запис `mqm_parse_failure` (`score=None`, `accept=True`), переклад НЕ блокується і НЕ втрачається.
- **Замір продуктивності (5 сегментів)**:
  - MQM Disabled: 0.0092с загалом (~0.0018с оверхед на сегмент).
  - MQM Enabled (з урахуванням рефлексійного виклику): +0.5310с на 5 сегментів (+0.1062с на сегмент при 100мс latency LLM). В реальних умовах на пристрої підключення MQM додає ~1.2s–2.5s на сегмент (часовий множник ~1.8x–2.0x відносно чистого перекладу), що повністю виправдовує дефолтний вимкнений стан (opt-in).

**Джерело:** розділ звіту "Семантична валідація шляхом рефлексії" - рекомендований підхід ЗАМІСТЬ back-translation (який Gemini explicitly відхилив через "Paradox of Poetic Intent" + подвоєння часу генерації).

**Ідея:** ПІСЛЯ генерації пакету перекладених абзаців - примусово очистити контекст `llama-server`, новим системним промптом перетворити ТУ Ж модель `Hy-MT2-7B` на MQM-аналізатор (Multidimensional Quality Metrics): подати оригінал+переклад, попросити JSON з оцінкою 1-10 і бінарним accept/reject, шукаючи пропуски інформації/смислові викривлення/неперекладені сутності. Одна модель в RAM, розділені контексти усувають "consistent hallucination blind spot" (модель не може підтвердити власну галюцинацію, бо оцінює її як "чужий" текст в новому контексті).

**УТОЧНЕННЯ до TASK-88 (Claude, перед делегуванням, перевірено реальний код):**

"Примусово очистити контекст" з ідеї дослідження - НЕ потребує окремого технічного кроку. `translate_text_hy_mt2()` (`common/utils.py`) вже робить СТАТЕЛESS виклики до `/completion`-ендпоінту `llama-server` (запущений з `--parallel 1`) - кожен виклик надсилає ПОВНИЙ промпт заново, без переносу історії розмови між викликами (це НЕ chat-completion API з накопиченням стану). Новий MQM-виклик - це просто ЩЕ ОДИН незалежний виклик `/completion` з іншим системним промптом, так само як зараз. НЕ треба шукати/реалізовувати спеціальний "context clear" API - його вже й так немає що чистити.

**Реалізуй:**
1. Нову функцію `mqm_review(original, translated, api_url, source_lang, target_lang)` в `common/utils.py` (поруч із `translate_text_hy_mt2`) - системний промпт перетворює модель на MQM-аналізатора: подає оригінал+переклад, просить структурований JSON `{"score": 1-10, "accept": true/false, "issues": ["опис проблеми", ...]}`, шукаючи пропуски інформації, смислові викривлення, неперекладені сутності.
2. Функція має бути fail-safe: якщо LLM повернула не-JSON чи щось незрозуміле - НЕ падати, повертати `{"score": null, "accept": true, "issues": ["MQM review failed to parse"]}` (accept=true за замовчуванням - НЕ блокувати переклад через збій самої перевірки, це best-effort якісний шар, не gate).
3. Де зберігати результати - подивись на патерн `quality_flags.json` (манга) - створи аналогічний файл для тексту, напр. `books/<slug>/translation_quality_flags.json`, ТІЛЬКИ для сегментів з `accept: false` чи низьким score (поріг - на твій розсуд, задокументуй чому).
4. НЕ інтегруй ще в `translate_segment_with_retry`/основний pipeline перекладу (як окремий, тестований модуль спочатку - Q підтвердить підключення окремо, той самий патерн, що TASK-86's `asr_verify.py`).
5. Реальні тести з мокованим `requests.post` - перевір: (а) нормальна відповідь з валідним JSON парситься коректно, (б) LLM повертає щось, що НЕ парситься як JSON - fail-safe спрацьовує (accept=true, не падає), (в) LLM повертає score поза діапазоном 1-10 - обробляється розумно (clamp чи ignore, на твій розсуд), (г) реальний виклик `build/append` до `translation_quality_flags.json` (як TASK-86's `append_to_stress_queue` - атомарний запис tmp+os.replace, той самий патерн проєкту).


## [x] TASK-89: Підключення ASR-петлі верифікації наголосів (`common/asr_verify.py`) до audio-пайплайну
* **Problem:** ASR-петля верифікації наголосів (`common/asr_verify.py`), яка порівнює синтезоване аудіо з оригінальним текстом для виявлення неправильних наголосів (mismatch-ів), готова та протестована у stateless-режимі (30/30 тестів), але все ще не підключена до основного `audio_stage.py` конвеєра.
* **Solution:**
  1. Замінити в `common/asr_verify.py` subprocess-виклики `whisper-cli` на Python API виклики через `sherpa_onnx.OfflineRecognizer.from_whisper()` з ледачим імпортом (`numpy`/`sherpa-onnx`) та синглтон-кешуванням інстансу моделі для уникнення OOM та повторного завантаження ваг у RAM.
  2. Додати прапорець `enable_asr_verify` (opt-in, дефолт `False`) в налаштування книги (`config.json`), Flask API та веб-інтерфейс (Settings modal).
  3. Інтегрувати верифікацію чанків у `audio_stage.py` строго после завершення синтезу TTS (`tts_helper.py`), запускаючи ASR-декодування лише для нових синтезованих чанків (`unique_missing_chunks`).
  4. При виявленні невідповідності (CER > 0.15) зберігати результати в `asr_stress_queue.json` книги.
  5. Створити роути Flask `GET /api/preview/asr-quality-flags/<slug>` та `POST /api/edit/stress/discard/<slug>/<chunk_hash>`. Автоматично видаляти флаги з черги при внесенні ручної правки наголосу через `PUT /api/edit/stress`.
  6. Відобразити список авто-виявлених помилок ASR у вкладці "Pending Edits" (секція "Auto-flagged ASR") в UI `stages.html` разом з аудіоплеєром та кнопками швидкого редагування/відхилення.
  7. Забезпечити повний fail-safe контракт: будь-яка помилка ASR ловиться, повертає статус помилки і позначає чанк для ручного рев'ю, ніколи не ламаючи генерацію книги.
* **Status:** **100% ВИКОНАНО.** Всі кроки реалізовано:
  - `common/asr_verify.py` переписано на чистий Python API `sherpa_onnx` з пріоритезацією `int8` квантованих моделей для Whisper.
  - Всі 29 юніт-тестів переписані з використанням моків та успішно проходять локально.
  - Успішно розгорнуто на OnePlus 13 (u0_a438), Whisper Small ONNX (int8) завантажено та розпаковано в `models/sherpa-onnx-whisper-small-int8`.
  - Проведено live E2E-тест на книзі `vibe-programming`: при видаленні чанка та його регенерації Whisper ASR виявив розбіжність вимови (CER 24.56%), яка успішно з'явилась у черзі `asr_stress_queue.json` та відобразилась у Web UI.
  - Перевірено автоматичне розв'язання флага (видалення з черги) при збереженні ручного виправлення наголосу через API/UI та ручне відхилення (Discard).
  - Навантаження RAM залишається в межах 1.2GB завдяки послідовному вивантаженню/завантаженню моделей TTS та ASR.

---

## Відхилено дослідженням (НЕ робити, задокументовано для довідки):
- **Vision-аналіз обкладинок/ілюстрацій EPUB** - низька рентабельність, ризик OOM без співмірної користі (текст перших сторінок для Cast Registry вже дає точніші дані для тону).
- **Back-translation для семантичної перевірки** - подвоює час генерації, "Paradox of Poetic Intent" (буквальний збіг ігнорує стилістику). MQM-рефлексія (TASK-88) кращий баланс.
