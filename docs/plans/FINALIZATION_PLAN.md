# План фіналізації kindle-butch-gen — 2026-07-16

Складено після повного рев'ю (GitNexus-індекс master@d85679d + живий код + живий пристрій),
а НЕ з переказу стану в промті. Кожне твердження нижче верифіковане, спосіб верифікації вказано.

## Верифіковані факти (стан на 2026-07-16 вечір)

| Твердження з промта | Реальний стан | Як перевірено |
|---|---|---|
| "OCR-артефакти проходять у переклад нефільтрованими" | **ЗАСТАРІЛЕ — вже виправлено (TASK-33)** | `clean_ocr_edge_noise` (translate_manga.py:288), `source_confidence` axis (:903-:1064) присутні в коді; md5 файлу збігається з пристроєм |
| "approved-правки манги можуть губитись при регенерації" | **ЗАСТАРІЛЕ — вже виправлено (TASK-34 client-side + TASK-36 follow-up)** | translate_manga.py:781, :864 — verbatim-override людської правки в regeneration path; TASK-42 повний прогін exercised цей код |
| "deploy-скрипт не пройшов hardware-тест" | **АКТУАЛЬНЕ — TASK-32 досі PENDING_HARDWARE_TEST** | нового `deploy.sh` (23K) на пристрої немає, лише старий `deploy_oneplus13.sh` (5.9K); блокер (недоступність телефона) ЗНЯТО — телефон досяжний зараз |
| TASK-7 security | **Код виправлено, ручна дія на пристрої НЕ виконана** | На пристрої `web_credentials.json` від 8 липня (хеш "0523") активний; Flask (pid 20951) працює з ним; `kindle.exodus.pp.ua` публічний |
| Синхронізація код↔пристрій | **Повна** | md5 5 core-файлів identical (translate_manga.py, app.py, stages.html, edit_store.py, TASKS.md) |

## Знайдений технічний борг (Крок 1)

1. **Нуль регресійних тестів** при високому churn: `kbg_web/app.py` — 41 зміна за 30 днів,
   `audio_stage.py` — 20, `translate_manga.py` — 18. High-blast-radius функції без покриття:
   `process_page` (264 рядки, translate_manga.py:852), `edit_regenerate_manga_page`
   (kbg_web/app.py:1934), `main` (translate_manga.py:1645).
2. **Зламаний git-деплой на телефон**: обидва remotes телефона неробочі (publickey denied),
   HEAD застряг на `cec9c11`, деплой іде тільки через scp вручну. Крихко: файли на пристрої
   розсинхронізуються з git-історією, локальні live-only зміни (audio_stage.py, tts_helper.py)
   ніколи не потраплять у git.
3. **Брудне робоче дерево на OrangePi**: купа видалених book-файлів і untracked-документів
   не закомічені (не мої зміни — потребують рішення власника).
4. TASK-38 (edit_store → SQLite+WAL) і TASK-39 (granular stage invalidation) — відкладені
   свідомо, залишаються відкладеними.

## Пріоритезований план (коректність > інфра-стабільність > нові фічі)

### P0 — Коректність/безпека (сьогодні, малі й живо-верифіковані)
- [ ] **P0.1 TASK-7 finalize**: на пристрої прибрати активний пароль "0523"
      (env `KBG_WEB_PASSWORD` в autostart АБО видалити старий json → рестарт Flask →
      перевірити, що старий пароль більше не приймається). Рішення про метод — за Q.

### P1 — Інфраструктурна стабільність
- [ ] **P1.1 TASK-32 hardware test**: телефон доступний. РИЗИК: запуск from-scratch
      інсталера на продакшн-пристрої. Потрібен sign-off Q на підхід
      (повний запуск покладаючись на idempotency-guards / поетапний / відкласти).
- [ ] **P1.2 Полагодити git-деплой на телефон**: згенерувати SSH-ключ на телефоні,
      авторизувати його на dev-сервері (192.168.3.184) → `git pull server-projects master`
      запрацює. Прибирає scp-only крихкість назавжди. Перед цим — закомітити live-only
      зміни телефона (audio_stage.py, bin/tts_helper.py) або звірити їх з git-версіями.

### P2 — Закриття верифікацій
- [ ] **P2.1 TASK-14 audio**: код задеплоєно; лишилась людська перевірка на слух.
      Підготувати конкретний листинг: які wav/сторінки слухати і що саме перевіряти.

### P3 — Нова функціональність: система підтримки (Документ 2)
- [ ] **ЗАБЛОКОВАНО: специфікація не надана.** У промті замість артефакту — плейсхолдер.
      Відомо лише: Track A/B (верифікований фонд + підтримка автора), вставки кожні
      50-70 сторінок на природних паузах, реєстрація через Telegram-бот, односторінковий
      сайт, реферали на базі пріоритетної черги TASK-23. Без повного тексту специфікації
      реалізацію не починати — запитано у Q.

### P4 — Технічний борг (задокументовано, свідомо відкладено)
- [ ] P4.1 Мінімальний smoke-test harness для process_page / edit_regenerate_manga_page
      (потребує окремої сесії; фікстури — реальні сторінки frieren).
- [ ] P4.2 TASK-38, TASK-39 — без змін, відкладені.
- [ ] P4.3 Рішення Q щодо брудного робочого дерева на OrangePi (видалені book-файли).

## Методологія виконання (усталена в проєкті)
- `impact()` перед зміною high-blast-radius функцій.
- "Готово" = перевірено на живому пристрої, не "код скомпілювався".
- `termux-wake-lock` для будь-якого довгого процесу на телефоні.
- Коміт + TASKS.md + ai-memory після КОЖНОГО завершеного пункту.
- Невизначеність/високий ризик → зупинка і питання до Q.

## Обмеження сесії
Сьогодні останній день поточного рівня можливостей. Пріоритет: письмово зафіксований
прогрес (цей план + P0/P1 з реальним підтвердженням), не гонитва за повнотою.
Незавершене — документувати чесно і залишати наступній сесії.
